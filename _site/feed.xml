<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="http://0.0.0.0:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://0.0.0.0:4000/" rel="alternate" type="text/html" hreflang="ko" /><updated>2026-03-25T06:53:54+00:00</updated><id>http://0.0.0.0:4000/feed.xml</id><title type="html">Paul’s Archives</title><subtitle>성장하는 개발자, 소통하는 개발자, 빠른 적용을 최 우선으로 삼는 개발자. 다음을 항상 생각하며, 개발 속에서 가치를 만들어내는 것을 목표로 합니다.</subtitle><author><name>Paul2021-R</name></author><entry><title type="html">Project Nexus 후기</title><link href="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/03/25/next.html" rel="alternate" type="text/html" title="Project Nexus 후기" /><published>2026-03-25T00:00:00+00:00</published><updated>2026-03-25T00:00:00+00:00</updated><id>http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/03/25/next</id><content type="html" xml:base="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/03/25/next.html"><![CDATA[<h1 id="project-the-nexus-최종-보고서-및-인사이트-정리">Project The Nexus 최종 보고서 및 인사이트 정리</h1>

<p><img src="/assets/images/posts/2026-03/003.png" alt="" /></p>

<p><img src="/assets/images/posts/2026-03/004.png" alt="" /></p>

<h2 id="1-서론">1. 서론</h2>
<p>본 문서는 2026년 2월 8일부터 3월 12일까지 진행된 ‘Project The Nexus’의 구축 과정과 기술적 의사결정, 그리고 이를 통해 도출된 인사이트를 정리한 글이다.</p>

<p>기존에 진행했던 ‘Protostar’ 프로젝트는 Docker(도커) 기반으로 성공적인 서비스를 구현했다. 여러 현실적인 문제들로 쿠버네티스에서 바로 시작하지 못했던 해당 프로젝트는 실제 서비스로서 구현은 성공하였지만, 그럼에도  데몬 종속성으로 인한 단일 장애점(SPOF), 보안 취약성, 파편화된 스크립트 관리 등 고가용성 측면에서 명백한 한계가 있단 생각이 들었다.</p>

<p>이를 극복하고 처음부터 생각하고 있던 3년 차 수준의 엔터프라이즈급 인프라 역량을 증명하기 위해, Docker-free 환경과 Kubernetes(쿠버네티스, 이하 k8s) 기반의 GitOps(깃옵스) 서버 클러스터 구축을 목표로 본 프로젝트를 시작하였다.</p>

<h2 id="2-본론-핵심-아키텍처-및-구현-과정">2. 본론: 핵심 아키텍처 및 구현 과정</h2>

<h3 id="21-인프라-아키텍처-및-환경-구성">2.1. 인프라 아키텍처 및 환경 구성</h3>
<p>온프레미스(On-premise) 서버 2대를 활용하여 <strong>멀티 노드 단일 클러스터(Multi-node Single Cluster)</strong> 구조를 채택했다. 이렇게 한 이유는 다음과 같다.</p>

<ul>
  <li><strong>역할 분리</strong>: 안정성이 최우선인 마스터 노드는 Centre 서버에, 고성능 처리가 필요한 워커 노드는 A5 서버에 배치하여 물리적 자원의 효율성을 극대화했다. 기존에도 분명 이렇게 되어 있긴 했지만, 각 서버는 제어가 단일화는 아니었기에 역할만 분리하고 싶었다.</li>
  <li><strong>논리적 격리</strong>: <code class="language-plaintext highlighter-rouge">infra</code>와 <code class="language-plaintext highlighter-rouge">service</code>로 Namespace(네임스페이스)를 분리하고, <code class="language-plaintext highlighter-rouge">env</code> 및 <code class="language-plaintext highlighter-rouge">tier</code> 라벨을 통해 리소스의 역할을 명확히 정의했다. 이렇게 함으로써 마스터 노드에서 적절하게 논리적 표현만으로 자원을 분리할 수 있었다.</li>
  <li><strong>선언적 관리</strong>: Kustomize(커스터마이즈)를 도입하여 <code class="language-plaintext highlighter-rouge">base</code>와 <code class="language-plaintext highlighter-rouge">overlays</code> 구조로 Dev/Prod 환경의 YAML 스크립트를 중복 없이 관리할 수 있는 기반을 마련했다. Dev 서버에서의 테스트와 Production 서버의 적용을 용이하게 만들어냈다.</li>
</ul>

<p><img src="/assets/images/posts/2026-03/006.png" alt="" /></p>
<blockquote>
  <p>연결된 노드와 그 역할, Master노드의 경우 최초엔 아무것도 하질 못해, 이 역시 옵션을 수정했다</p>
</blockquote>

<h3 id="22-네트워크-라우팅-및-로드밸런싱-고도화">2.2. 네트워크 라우팅 및 로드밸런싱 고도화</h3>
<p>k3s에 기본 탑재된 Traefik(트래픽) 대신, 차세대 공식 스펙인 <strong>Gateway API</strong> 기반의 NGF(NGINX Gateway Fabric)를 도입하여 L7 라우팅을 구현했다. 이렇게 한 것은 기본은 쉽지만 k3s 에 묶여 있고, 간단한 프로젝트가 아닌 실무적 차원에서의 관리를 목표로 하였기에 트래픽은 적절하지 않다고 판단하였다. 뿐만 아니라 공인 IP가 한개 뿐인 상황에서 여러 서비스 운용을 위해선, 관리 차원의 포트와 서비스 차원의 포트를 분리할 필요가 있었는데, 이를 구현하기 위해선 트래픽은 다소 부족한 스펙이었다고 판단되었다.</p>
<ul>
  <li><strong>MetalLB 도입 (2026-02-23)</strong>: 트래픽 선택과 유사하게 온프레미스 환경에서 k3s 내장 ServiceLB(Klipper)가 두개의 물리적으로 구분된 노드들 사이에서 80/443 포트 충돌을 제어하지 못하는 구조적 한계를 발견했다. 이를 해결하기 위해 ServiceLB를 비활성화하고 MetalLB를 L2 모드로 도입하여, IPAddressPool을 통해 노드별로 명확한 트래픽 제어와 로드밸런싱을 구현했고, 이로써 각 장치(Centre, A5)는 독립적으로 80/443 포트를 점유할 수 있게 되었다.</li>
  <li><strong>HTTPS 및 보안</strong>: cert-manager와 PROXY Protocol을 연동하여 클라이언트 IP를 보존하면서도 완벽한 HTTPS 통신을 구현했다. 또한, 컨테이너 보안 컨텍스트(<code class="language-plaintext highlighter-rouge">allowPrivilegeEscalation: false</code>, <code class="language-plaintext highlighter-rouge">runAsNonRoot</code>)를 적용하여 기본 root 권한 실행을 차단했다.</li>
</ul>

<p><img src="/assets/images/posts/2026-03/005.png" alt="" /></p>
<blockquote>
  <p>MetalLB를 도입한 후의 네트워크 구성도</p>
</blockquote>

<h3 id="23-stateful-서비스의-안정적-관리">2.3. Stateful 서비스의 안정적 관리</h3>

<p>PostgreSQL과 Redis 같은 상태 저장 서비스는 Deployment가 아닌 <strong>StatefulSet</strong>과 <strong>Headless Service(<code class="language-plaintext highlighter-rouge">clusterIP: None</code>)</strong> 조합으로 배포했다.</p>

<ul>
  <li>
    <p><strong>고정 식별자 부여</strong>: 중간 프록시 없이 Pod IP를 직접 반환하는 Headless Service를 통해 각 Pod에 고유한 DNS를 부여하고, 개별 PVC(PersistentVolumeClaim)를 매핑하여 데이터의 영속성을 보장했다.</p>
  </li>
  <li>
    <p><strong>백업 및 복구</strong>: ConfigMap을 활용해 초기화 스크립트를 파일로 마운트하고, 커스텀 포맷(<code class="language-plaintext highlighter-rouge">-F c</code>) 기반의 <code class="language-plaintext highlighter-rouge">pg_dump</code> 및 <code class="language-plaintext highlighter-rouge">pg_restore</code> 파이프라인을 검증하여 실무 수준의 데이터 복구 전략을 수립해, 기존의 서비스 운용 시 있었던 데이터들을 유실 없이 마이그레이션이 가능했다. 향후에는 무중단으로 마이그레이션이 가능한 방법도 구비해두고자 생각했다.</p>
  </li>
</ul>

<h3 id="24-gitops-기반-cicd-및-관찰성observability-확보">2.4. GitOps 기반 CI/CD 및 관찰성(Observability) 확보</h3>
<p>수동 배포의 파편화를 막기 위해 소스코드 변경부터 클러스터 반영까지 이어지는 자동화 파이프라인을 구축했다.</p>

<ul>
  <li><strong>CI/CD 파이프라인 (2026-03-11)</strong>: Jenkins(젠킨스)의 Kaniko(카니코) 에이전트를 통해 이미지를 빌드 방법을 적용하여 빌드를 진행하고 GHCR에 푸시한 뒤, GitOps 전용 레포지토리의 태그를 업데이트하면 ArgoCD가 이를 감지하여 클러스터에 자동 동기화하는 무중단 배포 흐름을 완성했다.</li>
  <li><strong>운영 원칙 수립</strong>: “인프라 툴은 인프라 툴로 관리하지 않는다”는 원칙 하에 ArgoCD의 관리 범위를 <code class="language-plaintext highlighter-rouge">service</code> 네임스페이스로 한정하여, 장애 발생 시 진단 도구 자체가 다운되는 리스크를 원천 차단하고, 오로지 서비스 영역에 대해서만 ArgoCD가 관리할 수 있도록 설정하였다.</li>
  <li><strong>모니터링 스택</strong>: kube-prometheus-stack과 Loki(Monolithic 모드)를 도입하여 메트릭과 로그를 중앙 집중화하고 시스템 가시성을 확보했다. 또한 기존의 MinIO 등의 스토리지 역시 추가함으로써 모니터링 스펙이 S3에 의존하지 않도록 구현하였고, 반대로 호환성 역시 유지시켰다.</li>
</ul>

<p><img src="/assets/images/posts/2026-03/001.png" alt="" /></p>
<blockquote>
  <p>적용된 ArgoCD</p>
</blockquote>

<p><img src="/assets/images/posts/2026-03/002.png" alt="" /></p>
<blockquote>
  <p>적용된 모니터링 스택의 설정된 대시보드들</p>
</blockquote>

<h3 id="25-ai-워크로드를-위한-gpu-자원-통합">2.5. AI 워크로드를 위한 GPU 자원 통합</h3>
<p>차세대 AI 서비스 연동을 위해 k3s 클러스터에 GPU 자원을 도입 테스트를 진행했다.</p>

<ul>
  <li><strong>GPU Operator 도입</strong>: 초기 수동 배포 시 발생한 라이브러리 경로 및 CDI 설정 충돌 문제를 분석한 후, 공식 권장 방식인 NVIDIA GPU Operator(Helm)를 도입했다. 이를 통해 RTX 4080 SUPER 자원을 k8s 스케줄러가 인식할 수 있는 리소스로 성공적으로 노출시키고, 이를 기반으로 LLM 추론 워크로드를 서비스에 연결시키는 것을 실행해보았다. 실질 서비스 차원에서 사용은 불가능한 수준이었기에, 테스트 정도만 진행했으며, 향후 AI 서비스의 온프레미스 도입을 테스트 해본 정도로 의의를 가졌다.</li>
</ul>

<hr />

<h2 id="3-결론-및-후기">3. 결론 및 후기</h2>

<p>Project The Nexus는 단순한 기술 스택의 전환을 넘어, “할 줄 안다”에서 “제대로 한다”로 도약하기 위한 검증의 시간이었다. 그리고 처음엔 이직을 위한 <code class="language-plaintext highlighter-rouge">완성</code> 단계로 생각했다. Docker Compose로 컨테이너를 띄우는 수준을 벗어나, 네트워크, 스토리지, 권한(RBAC), 그리고 배포 파이프라인까지 아우르는 진정한 의미의 오케스트레이션 역량을 내재화하는 것이다.</p>

<p>과정 속에서 마주한 시행착오들은 시스템 엔지니어링의 본질을 깨닫게 해주었다. MetalLB로의 교체 과정이나 GPU Operator 도입 시 겪었던 의존성 충돌 문제들은, 개별 컴포넌트 단독으로 볼 때는 완벽해 보여도 전체 시스템으로 통합될 때는 예상치 못한 비극을 초래할 수 있다는 사실을 가르쳐 주었다. 하지만 이러한 부족함을 투명하게 마주하고, 공식 문서와 아키텍처의 원리를 파고들어 근본적인 원인을 분석해 낸 시간은 자신감과 확신, 그리고 여전히 벽이 높고 두텁다는 사실을 이해할 수 있었다.</p>

<p>이제 불완전 연소되었던 Protostar는 엔터프라이즈급 인프라 위에서 온전하게 동작하는 서비스로 재탄생했다.</p>

<p>그리고 처음엔 이직을 위한, 성공적인 결과물을 만드는게 내 목적이었다.</p>

<p>하지만 삶은 참 희안하다. 암이 찾아왔고, 깃헙을 처음 개설하고 7년, 개발을 배운다고 뜬 눈으로 지샌 시간들이 나에게 쉼을 요구해왔다. 간도 망가졌다는 소식은 생각보다 여러 생각을 들게 했다.</p>

<p>하지만 반대로 생각해보니 좋은 기회일지도 모른다고 생각했다. 기회를 스스로 만들지 않고 회사에 붙어 있을 수 만은 없고, 휴식도, 건강도, 삶의 패턴도 바꾸고 변화를 도모해야할 때라는 생각이 결론이 되었다. 많은 일들은 있었지만. 그러니 걸어 가려고 한다. 새롭게, 서비스도 개발해보고, 노마드 코더로 살아보자. 방법이 뭐든 있겠지.</p>

<p>다행인건, 이젠 진짜 어디 가서 최소한 풀스택이라고 이야기 할 만큼은 되고, 버그 못찾고 AI 에게 쫓아가야만 하는 에겐남도 아니지 않겠는가? 산전 수전에 AI까지 갖춘 개발자이자, 경험가이다. 그리고 안되고 망하면, 망하더라도 해본게 있는데 굶어 죽진 않겠지.</p>

<p>앞으로는 서비스를 만들어낼 것이다. 그저 개발 도구 리뷰하고 AI쩔어욧! 하고 어그로를 끄는게 아닌, 진짜 제대로 개발한 걸로 서비스로 취급받고, 돈 만들어내는, 앞으로 할 일이 참 많다.</p>]]></content><author><name>Paul2021-R</name></author><category term="문제해결" /><category term="Backend" /><category term="개발" /><category term="Protostar" /><category term="Project" /><category term="Review" /><category term="Nexus" /><summary type="html"><![CDATA[Project The Nexus 최종 보고서 및 인사이트 정리]]></summary></entry><entry><title type="html">Protostar review note - 12 - NestJS Knowledge Domain</title><link href="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/28/00-protostar-review.html" rel="alternate" type="text/html" title="Protostar review note - 12 - NestJS Knowledge Domain" /><published>2026-02-28T00:00:00+00:00</published><updated>2026-02-28T00:00:00+00:00</updated><id>http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/28/00-protostar-review</id><content type="html" xml:base="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/28/00-protostar-review.html"><![CDATA[<h1 id="protostar-review---12---nestjs-knowledge-domain">Protostar Review - 12 - NestJS Knowledge Domain</h1>

<blockquote>
  <p>[!info] 분석 대상 파일</p>
  <ul>
    <li><code class="language-plaintext highlighter-rouge">src/features/knowledge/knowledge.controller.ts</code></li>
    <li><code class="language-plaintext highlighter-rouge">src/features/knowledge/knowledge.service.ts</code></li>
    <li><code class="language-plaintext highlighter-rouge">src/features/knowledge/ai-task.service.ts</code></li>
    <li><code class="language-plaintext highlighter-rouge">src/features/knowledge/dto/rag-webhook.dto.ts</code></li>
    <li><code class="language-plaintext highlighter-rouge">src/features/knowledge/dto/update-knowledge.ts</code></li>
    <li><code class="language-plaintext highlighter-rouge">src/features/knowledge/knowledge.module.ts</code></li>
  </ul>
</blockquote>

<hr />

<h2 id="1-주요-개념">1. 주요 개념</h2>

<h3 id="1-1-knowledge-document-라이프사이클">1-1. Knowledge Document 라이프사이클</h3>

<p>Knowledge Domain은 사용자가 업로드한 마크다운 문서를 AI가 처리하여 RAG(Retrieval-Augmented Generation)에 활용하는 전체 흐름을 담당한다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[사용자 업로드]
      │
      ▼
 KnowledgeDoc 생성 (status: UPLOADED)
      │
      ▼
 AiTaskService @Cron (EVERY_MINUTE)
      │  → DB에서 UPLOADED 문서 조회 (take: 10)
      │  → Redis List에 job 발행 (rpush ai:job:queue)
      │  → DB status → PROCESSING
      ▼
 FastAPI AI Worker
      │  → 벡터화 처리
      │  → Webhook 콜백 (POST /knowledge/webhook)
      ▼
 RagWebhookDto 수신
      │  → status: COMPLETED / FAILED
      │  → resultMeta: chunkCount, embeddingModel, vectorStoreKey
      ▼
 KnowledgeDoc status 업데이트
</code></pre></div></div>

<h3 id="1-2-파일-중복-방지---sha-256-해시">1-2. 파일 중복 방지 - SHA-256 해시</h3>

<p>동일한 파일의 중복 업로드를 방지하기 위해 <strong>파일 버퍼의 SHA-256 해시</strong>를 DB에 저장한다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">hash</span> <span class="o">=</span> <span class="nf">createHash</span><span class="p">(</span><span class="dl">'</span><span class="s1">sha256</span><span class="dl">'</span><span class="p">).</span><span class="nf">update</span><span class="p">(</span><span class="nx">file</span><span class="p">.</span><span class="nx">buffer</span><span class="p">).</span><span class="nf">digest</span><span class="p">(</span><span class="dl">'</span><span class="s1">hex</span><span class="dl">'</span><span class="p">);</span>
<span class="c1">// DB에서 동일 hash 존재 여부 확인 → 중복이면 ConflictException</span>
</code></pre></div></div>

<ul>
  <li>파일명이 달라도 내용이 같으면 중복으로 간주</li>
  <li>사용자별로 scoped (userId + hash 조합으로 검사)</li>
</ul>

<h3 id="1-3-업로드-제한---personal_max_uploads">1-3. 업로드 제한 - PERSONAL_MAX_UPLOADS</h3>

<p><code class="language-plaintext highlighter-rouge">constants.ts</code>에 정의된 <code class="language-plaintext highlighter-rouge">PERSONAL_MAX_UPLOADS = 10</code>을 통해 사용자당 최대 문서 수를 제한한다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">count</span><span class="p">({</span> <span class="na">where</span><span class="p">:</span> <span class="p">{</span> <span class="nx">userId</span> <span class="p">}</span> <span class="p">});</span>
<span class="k">if </span><span class="p">(</span><span class="nx">count</span> <span class="o">&gt;=</span> <span class="nx">PERSONAL_MAX_UPLOADS</span><span class="p">)</span> <span class="k">throw</span> <span class="k">new</span> <span class="nc">ForbiddenException</span><span class="p">(...)</span>
</code></pre></div></div>

<h3 id="1-4-webhook-패턴---비동기-처리-결과-수신">1-4. Webhook 패턴 - 비동기 처리 결과 수신</h3>

<p>AI Worker(FastAPI)가 벡터화를 완료하면 NestJS의 Webhook 엔드포인트를 호출한다.</p>

<ul>
  <li>인증: 요청 헤더의 <code class="language-plaintext highlighter-rouge">x-webhook-secret</code>과 환경변수 <code class="language-plaintext highlighter-rouge">WEBHOOK_SECRET</code> 비교</li>
  <li><code class="language-plaintext highlighter-rouge">@IsEnum(['COMPLETED', 'FAILED'])</code> — Prisma <code class="language-plaintext highlighter-rouge">DocStatus</code> 타입에서 두 값만 허용</li>
  <li>중첩 객체 검증: <code class="language-plaintext highlighter-rouge">@ValidateNested()</code> + <code class="language-plaintext highlighter-rouge">@Type(() =&gt; RagResultMeataDto)</code></li>
</ul>

<h3 id="1-5-multermodule-파일-제한">1-5. MulterModule 파일 제한</h3>

<p><code class="language-plaintext highlighter-rouge">KnowledgeModule</code>에서 <code class="language-plaintext highlighter-rouge">MulterModule.registerAsync</code>로 파일 업로드 규칙을 정의한다.</p>

<ul>
  <li>파일 크기 제한: <strong>50MB</strong></li>
  <li>파일 형식 제한: <code class="language-plaintext highlighter-rouge">.md</code> (마크다운) 파일만 허용</li>
  <li><code class="language-plaintext highlighter-rouge">fileFilter</code> 콜백: 허용되지 않는 파일은 <code class="language-plaintext highlighter-rouge">callback(new Error(...), false)</code> 반환</li>
</ul>

<h3 id="1-6-replacefile---버전-관리">1-6. replaceFile - 버전 관리</h3>

<p>기존 파일 교체 시 단순 덮어쓰기가 아니라 <strong>version 필드를 increment</strong> 한다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">update</span><span class="p">({</span>
  <span class="na">data</span><span class="p">:</span> <span class="p">{</span>
    <span class="na">storageKey</span><span class="p">:</span> <span class="nx">newStorageKey</span><span class="p">,</span>
    <span class="na">hash</span><span class="p">:</span> <span class="nx">newHash</span><span class="p">,</span>
    <span class="na">status</span><span class="p">:</span> <span class="dl">'</span><span class="s1">UPLOADED</span><span class="dl">'</span><span class="p">,</span>
    <span class="na">version</span><span class="p">:</span> <span class="p">{</span> <span class="na">increment</span><span class="p">:</span> <span class="mi">1</span> <span class="p">},</span>
  <span class="p">}</span>
<span class="p">})</span>
</code></pre></div></div>

<p>MinIO의 기존 오브젝트를 삭제하고 새 파일을 업로드한다.</p>

<hr />

<h2 id="2-핵심-로직-흐름">2. 핵심 로직 흐름</h2>

<h3 id="2-1-단일-파일-업로드-post-knowledgeupload">2-1. 단일 파일 업로드 (POST /knowledge/upload)</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Request (multipart/form-data: file)
  → @UseInterceptors(FileInterceptor('file'))
  → @UseInterceptors(KnowledgeUploadBusyCheckInterceptor)  ← 큐 포화 차단
  → Controller: uploadKnowledge(user, file, dto)
  → KnowledgeService.uploadKnowledge(userId, file)
      ├─ PERSONAL_MAX_UPLOADS 체크
      ├─ SHA-256 해시 생성
      ├─ 중복 해시 검사 → ConflictException
      ├─ storageKey = `knowledge/${userId}/${uuid}-${originalname}`
      ├─ ObjectStorageService.uploadFile(buffer, storageKey, mimetype)
      └─ prisma.knowledgeDoc.create({ status: UPLOADED, hash, storageKey })
</code></pre></div></div>

<h3 id="2-2-복수-파일-업로드-post-knowledgeuploads">2-2. 복수 파일 업로드 (POST /knowledge/uploads)</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Request (multipart/form-data: files[])
  → @UseInterceptors(FilesInterceptor('files', 10))  ← 최대 10개
  → KnowledgeService.uploadKnowledges(userId, files[])
      └─ Promise.allSettled(files.map(f =&gt; uploadKnowledge(userId, f)))
         → 일부 실패해도 나머지 처리 계속
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Promise.allSettled</code> 사용으로 부분 성공/실패 처리 가능.</p>

<h3 id="2-3-aitaskservice-크론-작업">2-3. AiTaskService 크론 작업</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@Cron(CronExpression.EVERY_MINUTE)
dispatchAiJobs()
  → prisma.knowledgeDoc.findMany({ where: { status: 'UPLOADED' }, take: 10 })
  → for each doc:
      ├─ redis.rpush('ai:job:queue', JSON.stringify({ docId, storageKey, userId }))
      └─ prisma.knowledgeDoc.update({ status: 'PROCESSING' })
</code></pre></div></div>

<blockquote>
  <p>[!warning] Race Condition
<code class="language-plaintext highlighter-rouge">findMany</code> → <code class="language-plaintext highlighter-rouge">rpush</code> → <code class="language-plaintext highlighter-rouge">update</code> 가 트랜잭션이 아니므로 다중 인스턴스 환경에서 동일 문서가 중복 발행될 수 있다. PostgreSQL의 <code class="language-plaintext highlighter-rouge">SELECT ... FOR UPDATE SKIP LOCKED</code> 또는 Redis 분산 락으로 해결 필요.</p>
</blockquote>

<h3 id="2-4-webhook-수신-post-knowledgewebhook">2-4. Webhook 수신 (POST /knowledge/webhook)</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Request (JSON: RagWebhookDto)
  → Guard: x-webhook-secret 검증
  → ValidationPipe: RagWebhookDto 검증 (@ValidateNested로 resultMeta까지)
  → KnowledgeService.handleWebhook(dto)
      ├─ status === 'COMPLETED': update(chunkCount, embeddingModel, vectorStoreKey)
      └─ status === 'FAILED': update(errorMessage, status: FAILED)
</code></pre></div></div>

<hr />

<h2 id="3-구조적-취약점--개선-방향">3. 구조적 취약점 / 개선 방향</h2>

<h3 id="3-1-aitaskservice-race-condition-critical">3-1. AiTaskService Race Condition (Critical)</h3>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// ❌ 현재: findMany 후 개별 update (비원자적)</span>
<span class="kd">const</span> <span class="nx">docs</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">findMany</span><span class="p">({</span> <span class="na">where</span><span class="p">:</span> <span class="p">{</span> <span class="na">status</span><span class="p">:</span> <span class="dl">'</span><span class="s1">UPLOADED</span><span class="dl">'</span> <span class="p">},</span> <span class="na">take</span><span class="p">:</span> <span class="mi">10</span> <span class="p">});</span>
<span class="k">for </span><span class="p">(</span><span class="kd">const</span> <span class="nx">doc</span> <span class="k">of</span> <span class="nx">docs</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">await</span> <span class="nx">redis</span><span class="p">.</span><span class="nf">rpush</span><span class="p">(</span><span class="dl">'</span><span class="s1">ai:job:queue</span><span class="dl">'</span><span class="p">,</span> <span class="p">...);</span>
  <span class="k">await</span> <span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">update</span><span class="p">({</span> <span class="na">where</span><span class="p">:</span> <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="nx">doc</span><span class="p">.</span><span class="nx">id</span> <span class="p">},</span> <span class="na">data</span><span class="p">:</span> <span class="p">{</span> <span class="na">status</span><span class="p">:</span> <span class="dl">'</span><span class="s1">PROCESSING</span><span class="dl">'</span> <span class="p">}</span> <span class="p">});</span>
<span class="p">}</span>

<span class="c1">// ✅ 개선안 1: PostgreSQL FOR UPDATE SKIP LOCKED</span>
<span class="kd">const</span> <span class="nx">docs</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">prisma</span><span class="p">.</span><span class="nx">$queryRaw</span><span class="s2">`
  SELECT * FROM knowledge_docs
  WHERE status = 'UPLOADED'
  LIMIT 10
  FOR UPDATE SKIP LOCKED
`</span><span class="p">;</span>

<span class="c1">// ✅ 개선안 2: BullMQ 사용 (Redis 기반 분산 작업 큐)</span>
<span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">queue</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="dl">'</span><span class="s1">ai-task</span><span class="dl">'</span><span class="p">,</span> <span class="p">{</span> <span class="nx">docId</span><span class="p">,</span> <span class="nx">storageKey</span> <span class="p">});</span>
</code></pre></div></div>

<h3 id="3-2-personal_max_uploads-카운트-방식">3-2. PERSONAL_MAX_UPLOADS 카운트 방식</h3>

<p>현재 <code class="language-plaintext highlighter-rouge">count()</code> 호출 후 UPLOADED/PROCESSING 상태의 문서까지 포함하여 카운트한다. FAILED 상태 문서가 슬롯을 차지하는 문제가 있다. 상태별 카운트 분리 또는 소프트 삭제 패턴 도입이 필요하다.</p>

<h3 id="3-3-replacefile의-minio-삭제-후-업로드-패턴">3-3. replaceFile의 MinIO 삭제 후 업로드 패턴</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>deleteFile → uploadFile (순차)
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">deleteFile</code> 성공 후 <code class="language-plaintext highlighter-rouge">uploadFile</code> 실패 시 파일이 유실된다. 두 작업을 원자적으로 처리하려면 업로드 선행 후 삭제하는 방식이 더 안전하다.</p>

<h3 id="3-4-단일복수-파일-엔드포인트-분리">3-4. 단일/복수 파일 엔드포인트 분리</h3>

<p><code class="language-plaintext highlighter-rouge">/upload</code>(단건)와 <code class="language-plaintext highlighter-rouge">/uploads</code>(다건)가 별도 엔드포인트로 분리되어 있다. RESTful 관점에서는 <code class="language-plaintext highlighter-rouge">/upload</code>로 통일하고 Content-Type 기반으로 처리하는 것이 일반적이지만, NestJS Multer 인터셉터 선택의 단순성을 위해 분리한 것으로 보인다.</p>

<hr />

<h2 id="4-핵심-메서드-및-라이브러리-함수">4. 핵심 메서드 및 라이브러리 함수</h2>

<table>
  <thead>
    <tr>
      <th>메서드/데코레이터</th>
      <th>위치</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@UseInterceptors(FilesInterceptor('files', 10))</code></td>
      <td>controller</td>
      <td>multipart 복수 파일 파싱 (최대 10개)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@UseInterceptors(FileInterceptor('file'))</code></td>
      <td>controller</td>
      <td>multipart 단일 파일 파싱</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">createHash('sha256').update(buf).digest('hex')</code></td>
      <td>service</td>
      <td>파일 버퍼 SHA-256 해시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Cron(CronExpression.EVERY_MINUTE)</code></td>
      <td>ai-task</td>
      <td>1분 주기 크론 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">redis.rpush(key, value)</code></td>
      <td>ai-task</td>
      <td>Redis List에 우측 삽입 (FIFO 큐)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Promise.allSettled(promises)</code></td>
      <td>service</td>
      <td>부분 실패 허용 병렬 처리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@ValidateNested()</code></td>
      <td>DTO</td>
      <td>중첩 객체까지 class-validator 적용</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Type(() =&gt; TargetClass)</code></td>
      <td>DTO</td>
      <td>class-transformer 형변환 (필수 짝꿍)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">MulterModule.registerAsync</code></td>
      <td>module</td>
      <td>비동기 설정 기반 Multer 구성</td>
    </tr>
  </tbody>
</table>

<h3 id="validatenested--type-중첩-검증">@ValidateNested + @Type() 중첩 검증</h3>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">export</span> <span class="kd">class</span> <span class="nc">RagWebhookDto</span> <span class="p">{</span>
  <span class="p">@</span><span class="nd">IsOptional</span><span class="p">()</span>
  <span class="p">@</span><span class="nd">ValidateNested</span><span class="p">()</span>          <span class="c1">// 중첩 객체 검증 활성화</span>
  <span class="p">@</span><span class="nd">Type</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="nx">RagResultMeataDto</span><span class="p">)</span>  <span class="c1">// plain object → 클래스 인스턴스 변환</span>
  <span class="nx">resultMeta</span><span class="p">?:</span> <span class="nx">RagResultMeataDto</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@ValidateNested()</code>만 사용하면 plain object에 적용되지 않는다. 반드시 <code class="language-plaintext highlighter-rouge">@Type()</code>으로 인스턴스 변환이 선행되어야 한다.</p>

<hr />

<h2 id="5-대체-가능한-라이브러리-및-트레이드오프">5. 대체 가능한 라이브러리 및 트레이드오프</h2>

<h3 id="5-1-작업-큐-redis-raw-rpush-vs-bullmq">5-1. 작업 큐: Redis raw rpush vs BullMQ</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>Redis raw rpush (현재)</th>
      <th>BullMQ</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>구현 복잡도</td>
      <td>낮음</td>
      <td>중간</td>
    </tr>
    <tr>
      <td>재시도</td>
      <td>수동 구현 필요</td>
      <td>내장 (backoff 설정)</td>
    </tr>
    <tr>
      <td>우선순위 큐</td>
      <td>수동 ZSet 구현 필요</td>
      <td>내장</td>
    </tr>
    <tr>
      <td>지연 작업</td>
      <td>수동</td>
      <td>내장</td>
    </tr>
    <tr>
      <td>분산 환경 Race Condition</td>
      <td>발생 가능</td>
      <td>Worker 원자성 보장</td>
    </tr>
    <tr>
      <td>모니터링</td>
      <td>별도 구현</td>
      <td>Bull Board 등 UI 제공</td>
    </tr>
  </tbody>
</table>

<p>MVP 수준에서는 raw rpush가 간단하지만, 프로덕션 확장 시 BullMQ 전환이 권장된다.</p>

<h3 id="5-2-분산-크론-cron-vs-분산-락">5-2. 분산 크론: @Cron vs 분산 락</h3>

<p>현재 <code class="language-plaintext highlighter-rouge">@Cron</code>은 각 인스턴스마다 독립 실행된다. 수평 확장 시 중복 실행이 발생한다.</p>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Redis SETNX 락</td>
      <td>크론 실행 전 Redis 락 획득, 1분 TTL</td>
    </tr>
    <tr>
      <td>PostgreSQL SELECT FOR UPDATE SKIP LOCKED</td>
      <td>DB 레벨에서 중복 방지</td>
    </tr>
    <tr>
      <td>BullMQ Scheduler</td>
      <td>단일 스케줄러 추상화</td>
    </tr>
    <tr>
      <td>Kafka/RabbitMQ</td>
      <td>메시지 브로커로 단일 컨슈머 보장</td>
    </tr>
  </tbody>
</table>

<h3 id="5-3-파일-해시-sha-256-vs-파일명-기반">5-3. 파일 해시: SHA-256 vs 파일명 기반</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>SHA-256 해시 (현재)</th>
      <th>파일명 기반</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>중복 탐지 정확도</td>
      <td>높음 (내용 기반)</td>
      <td>낮음 (이름 변경 시 통과)</td>
    </tr>
    <tr>
      <td>성능</td>
      <td>버퍼 전체 해시 (50MB 최대)</td>
      <td>O(1)</td>
    </tr>
    <tr>
      <td>사용 목적에 적합성</td>
      <td>지식 베이스 문서 중복 방지에 적합</td>
      <td>부적합</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="6-개발자로서-알아야-할-영역">6. 개발자로서 알아야 할 영역</h2>

<h3 id="6-1-multer와-nestjs-통합">6-1. Multer와 NestJS 통합</h3>

<p>NestJS는 Express의 <code class="language-plaintext highlighter-rouge">multer</code> 미들웨어를 <code class="language-plaintext highlighter-rouge">@nestjs/platform-express</code>를 통해 래핑한다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 메모리 스토리지 (기본) - file.buffer 사용 가능</span>
<span class="c1">// 디스크 스토리지 - 대용량 처리에 적합</span>

<span class="c1">// 파일 접근</span>
<span class="p">@</span><span class="nd">UploadedFile</span><span class="p">()</span> <span class="nx">file</span><span class="p">:</span> <span class="nx">Express</span><span class="p">.</span><span class="nx">Multer</span><span class="p">.</span><span class="nx">File</span>
<span class="c1">// file.buffer, file.originalname, file.mimetype, file.size</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">fileFilter</code>에서 <code class="language-plaintext highlighter-rouge">callback(null, false)</code>를 반환하면 파일이 필드에 첨부되지 않고 <code class="language-plaintext highlighter-rouge">undefined</code>가 되므로, 컨트롤러에서 file 존재 여부를 별도로 검증해야 한다.</p>

<h3 id="6-2-webhook-보안-패턴">6-2. Webhook 보안 패턴</h3>

<p>내부 서비스 간 Webhook 인증 방법:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. Shared Secret (현재): Header x-webhook-secret 검증
2. HMAC-SHA256: payload + secret → 서명 검증
3. mTLS: 클라이언트 인증서 검증
4. API Key + IP 화이트리스트
</code></pre></div></div>

<p>현재 방식은 단순하지만, secret이 평문으로 헤더에 전송된다. HMAC 서명 방식이 더 안전하다.</p>

<h3 id="6-3-db-상태-머신-패턴">6-3. DB 상태 머신 패턴</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>UPLOADED → PROCESSING → COMPLETED
                      → FAILED
</code></pre></div></div>

<p>상태 전이를 명확히 정의하면:</p>
<ul>
  <li>잘못된 상태에서 작업 방지 가능 (<code class="language-plaintext highlighter-rouge">findMany({ status: 'UPLOADED' })</code>)</li>
  <li>모니터링이 용이 (<code class="language-plaintext highlighter-rouge">count({ where: { status: 'FAILED' } })</code>)</li>
  <li>재처리 로직 구현 단순화 (<code class="language-plaintext highlighter-rouge">update({ status: 'UPLOADED' })</code> 로 재큐잉)</li>
</ul>

<h3 id="6-4-class-validator-중첩-검증-필수-지식">6-4. class-validator 중첩 검증 필수 지식</h3>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// ValidationPipe 설정 (main.ts)</span>
<span class="nx">app</span><span class="p">.</span><span class="nf">useGlobalPipes</span><span class="p">(</span><span class="k">new</span> <span class="nc">ValidationPipe</span><span class="p">({</span> <span class="na">whitelist</span><span class="p">:</span> <span class="kc">true</span><span class="p">,</span> <span class="na">transform</span><span class="p">:</span> <span class="kc">true</span> <span class="p">}));</span>

<span class="c1">// DTO에서 중첩 객체 검증 시 필수 패턴</span>
<span class="p">@</span><span class="nd">ValidateNested</span><span class="p">()</span>
<span class="p">@</span><span class="nd">Type</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="nx">ChildDto</span><span class="p">)</span>  <span class="c1">// class-transformer 없으면 검증 무시됨</span>
<span class="nx">childObject</span><span class="p">?:</span> <span class="nx">ChildDto</span><span class="p">;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">transform: true</code> 설정과 <code class="language-plaintext highlighter-rouge">@Type()</code> 데코레이터가 함께 작동해야 중첩 검증이 동작한다.</p>

<hr />

<h2 id="핵심-요약-카드">핵심 요약 카드</h2>

<blockquote>
  <p>[!summary] Knowledge Domain 3줄 요약</p>
  <ol>
    <li><strong>파일 업로드</strong> → SHA-256 중복 검사 → MinIO 저장 → DB(UPLOADED) 기록</li>
    <li><strong>크론 디스패치</strong> → UPLOADED 문서 조회 → Redis Queue push → DB(PROCESSING) 갱신 (Race Condition 존재)</li>
    <li><strong>Webhook 수신</strong> → FastAPI 결과 콜백 → DB(COMPLETED/FAILED) 최종 반영</li>
  </ol>
</blockquote>

<blockquote>
  <p>[!danger] Race Condition 핵심 포인트
<code class="language-plaintext highlighter-rouge">AiTaskService</code>의 <code class="language-plaintext highlighter-rouge">findMany → rpush → update</code> 패턴은 다중 인스턴스 환경에서 동일 문서가 중복 발행될 수 있다.
해결책: <strong>PostgreSQL <code class="language-plaintext highlighter-rouge">FOR UPDATE SKIP LOCKED</code></strong> 또는 <strong>BullMQ</strong> 도입.</p>
</blockquote>

<blockquote>
  <p>[!tip] @ValidateNested 함정
<code class="language-plaintext highlighter-rouge">@ValidateNested()</code>는 반드시 <code class="language-plaintext highlighter-rouge">@Type(() =&gt; TargetClass)</code>와 함께 써야 동작한다.
<code class="language-plaintext highlighter-rouge">@Type()</code> 없이 <code class="language-plaintext highlighter-rouge">@ValidateNested()</code>만 쓰면 plain object는 클래스 인스턴스로 변환되지 않아 내부 검증이 무시된다.</p>
</blockquote>

<blockquote>
  <p>[!example] 상태 머신 이해
<code class="language-plaintext highlighter-rouge">DocStatus</code>: <code class="language-plaintext highlighter-rouge">UPLOADED</code> → <code class="language-plaintext highlighter-rouge">PROCESSING</code> → <code class="language-plaintext highlighter-rouge">COMPLETED</code> | <code class="language-plaintext highlighter-rouge">FAILED</code>
실패한 문서는 <code class="language-plaintext highlighter-rouge">status = UPLOADED</code>로 재설정하면 다음 크론에서 재처리된다.</p>
</blockquote>]]></content><author><name>Paul2021-R</name></author><category term="문제해결" /><category term="Backend" /><category term="개발" /><category term="Protostar" /><category term="Project" /><category term="Review" /><category term="NestJS" /><category term="AI" /><summary type="html"><![CDATA[Protostar Review - 12 - NestJS Knowledge Domain]]></summary></entry><entry><title type="html">Protostar review note - 13 - NestJS Common Services</title><link href="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/28/01-protostar-review.html" rel="alternate" type="text/html" title="Protostar review note - 13 - NestJS Common Services" /><published>2026-02-28T00:00:00+00:00</published><updated>2026-02-28T00:00:00+00:00</updated><id>http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/28/01-protostar-review</id><content type="html" xml:base="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/28/01-protostar-review.html"><![CDATA[<h1 id="protostar-review---13---nestjs-common-services">Protostar Review - 13 - NestJS Common Services</h1>

<blockquote>
  <p>[!info] 분석 대상 파일</p>
  <ul>
    <li><code class="language-plaintext highlighter-rouge">src/common/objectStorage/objectStorage.service.ts</code></li>
    <li><code class="language-plaintext highlighter-rouge">src/common/objectStorage/objectStorage.module.ts</code></li>
    <li><code class="language-plaintext highlighter-rouge">src/common/queue/queue.service.ts</code></li>
    <li><code class="language-plaintext highlighter-rouge">src/common/monitoring/ai-status-monitoring.service.ts</code></li>
    <li><code class="language-plaintext highlighter-rouge">src/common/monitoring/system-monitoring.service.ts</code></li>
    <li><code class="language-plaintext highlighter-rouge">src/common/monitoring/monitoring.module.ts</code></li>
    <li><code class="language-plaintext highlighter-rouge">src/common/constants.ts</code></li>
  </ul>
</blockquote>

<hr />

<h2 id="1-주요-개념">1. 주요 개념</h2>

<h3 id="1-1-전역-상수-모듈-패턴-constantsts">1-1. 전역 상수 모듈 패턴 (constants.ts)</h3>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">export</span> <span class="kd">const</span> <span class="nx">CHAT_MAX_CONNECTIONS</span> <span class="o">=</span> <span class="mi">600</span><span class="p">;</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">SSE_HEARTBEAT_INTERVAL</span> <span class="o">=</span> <span class="mi">30000</span><span class="p">;</span>  <span class="c1">// 30s</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">SSE_RETRY_SECONDS</span> <span class="o">=</span> <span class="mi">10</span><span class="p">;</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">CONCURRENCY</span> <span class="o">=</span> <span class="mi">3</span><span class="p">;</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">MAX_PENDING</span> <span class="o">=</span> <span class="mi">10</span><span class="p">;</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">PERSONAL_MAX_UPLOADS</span> <span class="o">=</span> <span class="mi">10</span><span class="p">;</span>
</code></pre></div></div>

<p>서비스 전반에 걸쳐 사용되는 매직 넘버를 단일 파일에 중앙화한다. 변경 시 한 곳만 수정하면 된다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">CHAT_MAX_CONNECTIONS = 600</code>: SSE 동시 연결 최대값 (Nginx 등 상위 계층에서도 동일하게 설정되어야 의미가 있다)</li>
  <li><code class="language-plaintext highlighter-rouge">CONCURRENCY = 3</code>: QueueService의 동시 처리 작업 수</li>
  <li><code class="language-plaintext highlighter-rouge">MAX_PENDING = 10</code>: QueueService의 대기 큐 크기</li>
</ul>

<h3 id="1-2-minio--s3-호환성-objectstorageservice">1-2. MinIO ↔ S3 호환성 (ObjectStorageService)</h3>

<p>MinIO는 AWS S3 API와 호환된다. 따라서 AWS SDK v3 (<code class="language-plaintext highlighter-rouge">@aws-sdk/client-s3</code>)를 그대로 사용할 수 있다.</p>

<p>핵심 설정:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">forcePathStyle: true</code> — MinIO는 Virtual-hosted style(<code class="language-plaintext highlighter-rouge">bucket.endpoint</code>)이 아닌 Path style(<code class="language-plaintext highlighter-rouge">endpoint/bucket</code>)을 사용하므로 필수</li>
  <li><code class="language-plaintext highlighter-rouge">endpoint: MINIO_ENDPOINT</code> — AWS 기본 엔드포인트 대신 MinIO 서버 URL 지정</li>
  <li><code class="language-plaintext highlighter-rouge">region: MINIO_REGION</code> — MinIO는 region 의미가 없지만 SDK 필수값이므로 임의 설정</li>
</ul>

<h3 id="1-3-queueservice---인메모리-동시성-제어">1-3. QueueService - 인메모리 동시성 제어</h3>

<p>Node.js는 싱글 스레드이지만 비동기 I/O로 인해 동시성이 발생한다. CPU-bound 작업 또는 외부 리소스 보호를 위해 동시 실행 수를 제한하는 큐가 필요하다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>activeCount ≤ CONCURRENCY(3): 즉시 실행
activeCount &gt; CONCURRENCY(3), pendingQueue.length &lt; MAX_PENDING(10): 대기 큐 추가
pendingQueue.length ≥ MAX_PENDING(10): 즉시 거부 (ServiceUnavailableException)
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">isBusy()</code> 메서드는 <code class="language-plaintext highlighter-rouge">KnowledgeUploadBusyCheckInterceptor</code>에서 사전 차단 용도로 사용된다.</p>

<h3 id="1-4-이중-모니터링-구조-monitoringmodule">1-4. 이중 모니터링 구조 (MonitoringModule)</h3>

<table>
  <thead>
    <tr>
      <th>서비스</th>
      <th>감시 대상</th>
      <th>주기</th>
      <th>저장소</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">AiStatusMonitoringService</code></td>
      <td>FastAPI AI Worker 헬스</td>
      <td>@Cron 1초</td>
      <td>Redis ZSet</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">SystemMonitoringService</code></td>
      <td>Node.js 프로세스 메모리</td>
      <td>setInterval 5초</td>
      <td>Logger(Winston)</td>
    </tr>
  </tbody>
</table>

<p>두 서비스가 하나의 <code class="language-plaintext highlighter-rouge">MonitoringModule</code>로 묶여 있으며, <code class="language-plaintext highlighter-rouge">AiCircuitGuard</code>도 함께 exports되어 다른 모듈에서 임포트하면 자동으로 사용 가능하다.</p>

<h3 id="1-5-objectstorageservice-라이프사이클">1-5. ObjectStorageService 라이프사이클</h3>

<p><code class="language-plaintext highlighter-rouge">OnModuleInit</code> / <code class="language-plaintext highlighter-rouge">OnModuleDestroy</code>를 구현하여 애플리케이션 시작 시 연결을 검증하고 종료 시 리소스를 해제한다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>onModuleInit():
  HeadBucketCommand → 버킷 존재 확인
    ├─ 성공: "✅ Connected &amp; Bucket Found"
    ├─ 404 NotFound: createBucket() → 버킷 자동 생성
    └─ 기타 에러: throw → 앱 부트스트랩 실패 (의도적)

onModuleDestroy():
  s3Client.destroy() → SDK 내부 연결 풀 해제
</code></pre></div></div>

<hr />

<h2 id="2-핵심-로직-흐름">2. 핵심 로직 흐름</h2>

<h3 id="2-1-objectstorageservice-파일-업로드삭제">2-1. ObjectStorageService 파일 업로드/삭제</h3>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 업로드</span>
<span class="k">async</span> <span class="nf">uploadFile</span><span class="p">(</span><span class="nx">buffer</span><span class="p">,</span> <span class="nx">fileName</span><span class="p">,</span> <span class="nx">mimeType</span><span class="p">,</span> <span class="nx">specificBucketName</span><span class="p">?)</span>
  <span class="err">→</span> <span class="nc">PutObjectCommand</span><span class="p">({</span> <span class="nx">Bucket</span><span class="p">,</span> <span class="na">Key</span><span class="p">:</span> <span class="nx">fileName</span><span class="p">,</span> <span class="na">Body</span><span class="p">:</span> <span class="nx">buffer</span><span class="p">,</span> <span class="nx">ContentType</span><span class="p">,</span> <span class="na">ACL</span><span class="p">:</span> <span class="dl">'</span><span class="s1">public-read</span><span class="dl">'</span> <span class="p">})</span>
  <span class="err">→</span> <span class="k">return</span> <span class="nx">fileName</span>  <span class="c1">// storageKey 반환</span>

<span class="c1">// 삭제</span>
<span class="k">async</span> <span class="nf">deleteFile</span><span class="p">(</span><span class="nx">fileName</span><span class="p">,</span> <span class="nx">specificBucketName</span><span class="p">?)</span>
  <span class="err">→</span> <span class="nc">DeleteObjectCommand</span><span class="p">({</span> <span class="nx">Bucket</span><span class="p">,</span> <span class="na">Key</span><span class="p">:</span> <span class="nx">fileName</span> <span class="p">})</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">specificBucketName</code>이 제공되면 기본 버킷 대신 해당 버킷을 사용한다. <code class="language-plaintext highlighter-rouge">null</code>도 허용하는 <code class="language-plaintext highlighter-rouge">null coalescing</code> 패턴을 사용한다.</p>

<h3 id="2-2-queueservice-흐름">2-2. QueueService 흐름</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>enqueue(task: () =&gt; Promise&lt;T&gt;): Promise&lt;T&gt;
  ├─ activeCount &lt; CONCURRENCY → execute(task) 즉시 실행
  └─ pendingQueue.length &lt; MAX_PENDING → Promise를 대기열에 추가
       └─ 실행 중인 작업 완료 시 → next() 호출로 다음 작업 실행
  └─ 대기열 포화 → ServiceUnavailableException 즉시 throw

execute(task):
  activeCount++
  try { return await task() }
  finally { activeCount--; next() }  // 항상 다음 작업 실행 시도

isBusy(): pendingQueue.length &gt;= MAX_PENDING
</code></pre></div></div>

<h3 id="2-3-aistatusmonitoringservice-흐름">2-3. AiStatusMonitoringService 흐름</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@Cron('* * * * * *')  // 1초마다
checkAiWorkerStatus():
  → redis.zremrangebyscore('ai:worker:heartbeat', 0, now-10000)  // 10초 지난 heartbeat 제거
  → redis.zcard('ai:worker:heartbeat')  // 현재 살아있는 워커 수
  → count &gt; 0 → isAvailable = true
  → count === 0 → isAvailable = false

isAvailable(): boolean  // AiCircuitGuard에서 호출
</code></pre></div></div>

<p>FastAPI 워커는 주기적으로 <code class="language-plaintext highlighter-rouge">zadd ai:worker:heartbeat SCORE(timestamp) MEMBER(workerId)</code>를 호출한다. 10초 이상 heartbeat이 없으면 오프라인으로 판단한다.</p>

<h3 id="2-4-systemmonitoringservice-흐름">2-4. SystemMonitoringService 흐름</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>onModuleInit():
  process.env.NODE_ENV === 'production' 이면만 시작
    → setInterval(logSystemMetrics, 5000)

logSystemMetrics():
  → process.memoryUsage()
  → logger.log({ type, rss, heapTotal, heapUsed, external, arrayBuffers, timestamp })
</code></pre></div></div>

<p>Winston logger가 구조화된 JSON 로그를 출력하며, 외부 로그 수집기(Loki, Elasticsearch 등)에서 메트릭 기반 알림 설정에 활용된다.</p>

<hr />

<h2 id="3-구조적-취약점--개선-방향">3. 구조적 취약점 / 개선 방향</h2>

<h3 id="3-1-objectstorageservice---acl-public-read-하드코딩">3-1. ObjectStorageService - ACL ‘public-read’ 하드코딩</h3>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// ❌ 현재: 모든 파일이 공개 접근 가능</span>
<span class="nx">ACL</span><span class="p">:</span> <span class="dl">'</span><span class="s1">public-read</span><span class="dl">'</span>

<span class="c1">// ✅ 개선안: Pre-signed URL 패턴</span>
<span class="kd">const</span> <span class="nx">url</span> <span class="o">=</span> <span class="k">await</span> <span class="nf">getSignedUrl</span><span class="p">(</span><span class="nx">s3Client</span><span class="p">,</span> <span class="k">new</span> <span class="nc">GetObjectCommand</span><span class="p">({</span> <span class="nx">Bucket</span><span class="p">,</span> <span class="nx">Key</span> <span class="p">}),</span> <span class="p">{</span> <span class="na">expiresIn</span><span class="p">:</span> <span class="mi">3600</span> <span class="p">});</span>
</code></pre></div></div>

<p>현재 MinIO에 업로드된 지식 문서가 public-read이므로 URL을 알면 누구나 접근 가능하다. 민감한 문서의 경우 private ACL + Pre-signed URL이 적합하다.</p>

<h3 id="3-2-queueservice---수평-확장-불가">3-2. QueueService - 수평 확장 불가</h3>

<p>인메모리 큐이므로 <strong>단일 인스턴스에서만 동작</strong>한다. NestJS 인스턴스가 2개 이상 실행되면:</p>
<ul>
  <li>각 인스턴스가 독립적인 큐를 갖게 됨</li>
  <li>전체 동시 처리 수가 인스턴스 수 × CONCURRENCY로 늘어남</li>
  <li>FastAPI AI Worker 부하 계산이 복잡해짐</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>✅ 개선안: BullMQ (Redis 기반 분산 큐)
- 여러 인스턴스가 하나의 Redis 큐를 공유
- Worker 수, 동시성, 재시도 모두 중앙 관리 가능
</code></pre></div></div>

<h3 id="3-3-systemmonitoringservice---setinterval-vs-cron">3-3. SystemMonitoringService - setInterval vs @Cron</h3>

<p>현재 <code class="language-plaintext highlighter-rouge">setInterval</code>을 사용하지만 NestJS에서는 <code class="language-plaintext highlighter-rouge">@Cron</code>이 더 적합하다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>setInterval (현재)</th>
      <th>@Cron</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>NestJS 라이프사이클 통합</td>
      <td>수동 clearInterval 필요</td>
      <td>자동 정리</td>
    </tr>
    <tr>
      <td>테스트 용이성</td>
      <td>낮음</td>
      <td>높음 (TestingModule에서 제어)</td>
    </tr>
    <tr>
      <td>동적 비활성화</td>
      <td>어려움</td>
      <td>SchedulerRegistry로 가능</td>
    </tr>
    <tr>
      <td>코드 일관성</td>
      <td>낮음 (AiStatus는 @Cron)</td>
      <td>높음</td>
    </tr>
  </tbody>
</table>

<h3 id="3-4-chat_max_connections-미사용">3-4. CHAT_MAX_CONNECTIONS 미사용</h3>

<p><code class="language-plaintext highlighter-rouge">constants.ts</code>에 <code class="language-plaintext highlighter-rouge">CHAT_MAX_CONNECTIONS = 600</code>이 정의되어 있지만, 현재 코드에서 실제로 이 값을 검사하는 로직이 없다. Nginx의 upstream 설정이나 SSE 연결 수 제한과 연계되어야 의미가 있다.</p>

<hr />

<h2 id="4-핵심-메서드-및-라이브러리-함수">4. 핵심 메서드 및 라이브러리 함수</h2>

<h3 id="objectstorageservice-aws-sdk-v3">ObjectStorageService (AWS SDK v3)</h3>

<table>
  <thead>
    <tr>
      <th>커맨드</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">HeadBucketCommand</code></td>
      <td>버킷 존재 여부만 확인 (내용 조회 없이 메타데이터만)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">CreateBucketCommand</code></td>
      <td>새 버킷 생성</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">PutObjectCommand</code></td>
      <td>파일 업로드 (Body: Buffer)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">DeleteObjectCommand</code></td>
      <td>파일 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">s3Client.send(command)</code></td>
      <td>커맨드 실행 (Command Pattern)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">s3Client.destroy()</code></td>
      <td>내부 HTTP 연결 풀 종료</td>
    </tr>
  </tbody>
</table>

<h3 id="processmemoryusage-반환값">process.memoryUsage() 반환값</h3>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
  <span class="nl">rss</span><span class="p">:</span> <span class="kr">number</span><span class="p">,</span>        <span class="c1">// Resident Set Size - OS가 프로세스에 할당한 총 메모리</span>
  <span class="nx">heapTotal</span><span class="p">:</span> <span class="kr">number</span><span class="p">,</span>  <span class="c1">// V8 힙 총 할당량</span>
  <span class="nx">heapUsed</span><span class="p">:</span> <span class="kr">number</span><span class="p">,</span>   <span class="c1">// V8 힙 실제 사용량</span>
  <span class="nx">external</span><span class="p">:</span> <span class="kr">number</span><span class="p">,</span>   <span class="c1">// V8 외부 C++ 객체 메모리 (Buffer 등)</span>
  <span class="nx">arrayBuffers</span><span class="p">:</span> <span class="kr">number</span> <span class="c1">// ArrayBuffer/SharedArrayBuffer 메모리</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">heapUsed / heapTotal</code>로 힙 사용률을 계산하며, <code class="language-plaintext highlighter-rouge">rss</code>가 지속적으로 증가하면 메모리 누수를 의심한다.</p>

<h3 id="monitoringmodule-구조">MonitoringModule 구조</h3>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Module</span><span class="p">({</span>
  <span class="na">imports</span><span class="p">:</span> <span class="p">[</span>
    <span class="nx">ScheduleModule</span><span class="p">.</span><span class="nf">forRoot</span><span class="p">(),</span>  <span class="c1">// @Cron 사용을 위한 스케줄러 초기화</span>
    <span class="nx">RedisModule</span><span class="p">,</span>               <span class="c1">// AiStatusMonitoringService가 Redis 사용</span>
  <span class="p">],</span>
  <span class="na">providers</span><span class="p">:</span> <span class="p">[</span><span class="nx">AiStatusMonitoringService</span><span class="p">,</span> <span class="nx">SystemMonitoringService</span><span class="p">,</span> <span class="nx">AiCircuitGuard</span><span class="p">],</span>
  <span class="na">exports</span><span class="p">:</span>   <span class="p">[</span><span class="nx">AiStatusMonitoringService</span><span class="p">,</span> <span class="nx">SystemMonitoringService</span><span class="p">,</span> <span class="nx">AiCircuitGuard</span><span class="p">],</span>
<span class="p">})</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">AiCircuitGuard</code>를 providers와 exports 양쪽에 등록함으로써, MonitoringModule을 import하는 모듈에서 별도 선언 없이 DI 가능하다.</p>

<hr />

<h2 id="5-대체-가능한-라이브러리-및-트레이드오프">5. 대체 가능한 라이브러리 및 트레이드오프</h2>

<h3 id="5-1-minio-sdk-vs-aws-sdk-v3">5-1. MinIO SDK vs AWS SDK v3</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>AWS SDK v3 (현재)</th>
      <th>MinIO JS SDK</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>S3 호환성</td>
      <td>완전 호환</td>
      <td>MinIO 특화 기능 제공</td>
    </tr>
    <tr>
      <td>AWS 전환 용이성</td>
      <td>✅ 설정만 변경</td>
      <td>❌ SDK 교체 필요</td>
    </tr>
    <tr>
      <td>presigned URL</td>
      <td>GetSignedUrl 사용</td>
      <td>presignedGetObject</td>
    </tr>
    <tr>
      <td>번들 크기</td>
      <td>트리셰이킹 지원</td>
      <td>상대적으로 무거움</td>
    </tr>
    <tr>
      <td>**결론**</td>
      <td>이식성 우선 시 권장</td>
      <td>MinIO 전용 기능 필요 시</td>
    </tr>
  </tbody>
</table>

<p>AWS SDK v3는 AWS → MinIO 또는 MinIO → S3 전환 시 endpoint 설정만 바꾸면 된다는 장점이 크다.</p>

<h3 id="5-2-메모리-모니터링-winston-로그-vs-prom-client">5-2. 메모리 모니터링: Winston 로그 vs prom-client</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>Winston 로그 (현재)</th>
      <th>prom-client</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Prometheus 연동</td>
      <td>별도 파싱 필요</td>
      <td>직접 /metrics 노출</td>
    </tr>
    <tr>
      <td>Grafana 연동</td>
      <td>Loki → 쿼리 복잡</td>
      <td>Prometheus → 직접 연동</td>
    </tr>
    <tr>
      <td>구현 복잡도</td>
      <td>낮음</td>
      <td>중간</td>
    </tr>
    <tr>
      <td>실시간 알림</td>
      <td>로그 기반 지연</td>
      <td>AlertManager 즉시</td>
    </tr>
    <tr>
      <td>**결론**</td>
      <td>단순 로깅에 적합</td>
      <td>프로덕션 모니터링에 권장</td>
    </tr>
  </tbody>
</table>

<h3 id="5-3-인메모리-queueservice-vs-bullmq">5-3. 인메모리 QueueService vs BullMQ</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>QueueService (현재)</th>
      <th>BullMQ</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>수평 확장</td>
      <td>불가</td>
      <td>Redis 기반 공유 큐</td>
    </tr>
    <tr>
      <td>재시도</td>
      <td>없음</td>
      <td>내장 (backoff)</td>
    </tr>
    <tr>
      <td>모니터링 UI</td>
      <td>없음</td>
      <td>Bull Board</td>
    </tr>
    <tr>
      <td>영속성</td>
      <td>없음 (재시작 시 유실)</td>
      <td>Redis에 영속</td>
    </tr>
    <tr>
      <td>구현 복잡도</td>
      <td>낮음</td>
      <td>중간</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="6-개발자로서-알아야-할-영역">6. 개발자로서 알아야 할 영역</h2>

<h3 id="6-1-s3-호환-스토리지-개념">6-1. S3 호환 스토리지 개념</h3>

<p>AWS S3의 핵심 개념:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Bucket: 최상위 컨테이너 (폴더와 유사)
Object Key: 버킷 내 고유 식별자 (경로 포함: "knowledge/userId/uuid-filename.md")
ACL: 접근 제어 (private, public-read, public-read-write)
Pre-signed URL: 임시 서명된 접근 URL (만료 시간 설정 가능)
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">forcePathStyle</code>은 MinIO, LocalStack 등 자체 호스팅 S3 호환 서비스에 반드시 필요하다.</p>

<h3 id="6-2-nodejs-단일-스레드와-동시성">6-2. Node.js 단일 스레드와 동시성</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Node.js Event Loop
  ├─ 단일 스레드: JS 코드 실행 (CPU-bound 작업 블로킹)
  └─ libuv thread pool: I/O, DNS, 암호화 등 비동기 처리

동시성이 발생하는 상황:
  - await 키워드 사용 시 이벤트 루프가 다음 콜백 처리
  - 여러 async 함수가 await 없이 실행되면 동시 진행
</code></pre></div></div>

<p>QueueService는 이 동시성으로 인해 외부 API(AI Worker, MinIO) 호출이 동시에 여러 개 진행될 수 있는 상황을 제어한다.</p>

<h3 id="6-3-global-모듈-패턴">6-3. @Global() 모듈 패턴</h3>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Global</span><span class="p">()</span>
<span class="p">@</span><span class="nd">Module</span><span class="p">({</span>
  <span class="na">providers</span><span class="p">:</span> <span class="p">[</span><span class="nx">ObjectStorageService</span><span class="p">],</span>
  <span class="na">exports</span><span class="p">:</span> <span class="p">[</span><span class="nx">ObjectStorageService</span><span class="p">],</span>
<span class="p">})</span>
<span class="k">export</span> <span class="kd">class</span> <span class="nc">ObjectStorageModule</span> <span class="p">{}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@Global()</code> 모듈은 <code class="language-plaintext highlighter-rouge">AppModule</code>에서 한 번만 등록하면, 다른 모든 모듈에서 imports 없이 DI 가능하다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>AppModule
  └─ imports: [ObjectStorageModule]  ← 한 번만 등록

KnowledgeModule
  └─ providers: [KnowledgeService]

KnowledgeService
  └─ constructor(private readonly objectStorage: ObjectStorageService)  ← imports 없어도 주입됨
</code></pre></div></div>

<p>과용하면 결합도가 높아지므로, 진정으로 전역적인 유틸리티(DB, Cache, Storage 등)에만 적용한다.</p>

<h3 id="6-4-nestjs-라이프사이클-훅-활용">6-4. NestJS 라이프사이클 훅 활용</h3>

<table>
  <thead>
    <tr>
      <th>훅</th>
      <th>타이밍</th>
      <th>용도</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">OnModuleInit</code></td>
      <td>모듈 초기화 완료 후</td>
      <td>DB 연결, 버킷 확인, 초기 데이터 로드</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">OnModuleDestroy</code></td>
      <td>앱 종료 신호 수신 시</td>
      <td>커넥션 해제, 인터벌 정리, 플러시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">OnApplicationShutdown</code></td>
      <td>SIGTERM 등 수신 시</td>
      <td>진행 중인 작업 완료 대기</td>
    </tr>
  </tbody>
</table>

<p>Graceful Shutdown을 위해 <code class="language-plaintext highlighter-rouge">app.enableShutdownHooks()</code>를 main.ts에 추가하면 <code class="language-plaintext highlighter-rouge">OnModuleDestroy</code>가 종료 신호에 반응한다.</p>

<hr />

<h2 id="핵심-요약-카드">핵심 요약 카드</h2>

<blockquote>
  <p>[!summary] Common Services 3줄 요약</p>
  <ol>
    <li><strong>ObjectStorageService</strong>: AWS SDK v3로 MinIO를 S3 호환 방식으로 연결, onModuleInit에서 버킷 자동 생성</li>
    <li><strong>QueueService</strong>: 인메모리 동시성 큐(CONCURRENCY=3, MAX_PENDING=10), 단일 인스턴스 한정</li>
    <li><strong>MonitoringModule</strong>: AI Worker 헬스(Redis ZSet @Cron 1s) + Node.js 메모리(setInterval 5s) 이중 감시</li>
  </ol>
</blockquote>

<blockquote>
  <p>[!tip] MinIO-S3 호환의 핵심 설정</p>
  <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">new</span> <span class="nc">S3Client</span><span class="p">({</span>
  <span class="na">endpoint</span><span class="p">:</span> <span class="dl">'</span><span class="s1">http://minio:9000</span><span class="dl">'</span><span class="p">,</span>  <span class="c1">// MinIO 서버 URL</span>
  <span class="na">forcePathStyle</span><span class="p">:</span> <span class="kc">true</span><span class="p">,</span>           <span class="c1">// ← 이게 없으면 연결 실패</span>
  <span class="na">region</span><span class="p">:</span> <span class="dl">'</span><span class="s1">us-east-1</span><span class="dl">'</span><span class="p">,</span>           <span class="c1">// 임의값이지만 SDK 필수</span>
<span class="p">})</span>
</code></pre></div>  </div>
</blockquote>

<blockquote>
  <p>[!warning] QueueService 수평 확장 불가
<code class="language-plaintext highlighter-rouge">activeCount</code>와 <code class="language-plaintext highlighter-rouge">pendingQueue</code>는 인메모리 변수다.
인스턴스가 2개 실행되면 각자 독립적인 큐를 가지며, 전체 동시 처리 수가 2배로 늘어난다.
분산 환경에서는 <strong>BullMQ + Redis</strong> 로 대체해야 한다.</p>
</blockquote>

<blockquote>
  <p>[!example] process.memoryUsage() 핵심 지표</p>
  <ul>
    <li><code class="language-plaintext highlighter-rouge">heapUsed / heapTotal</code> → 힙 사용률 (70% 이상이면 경고)</li>
    <li><code class="language-plaintext highlighter-rouge">rss</code> 지속 증가 → 메모리 누수 의심</li>
    <li><code class="language-plaintext highlighter-rouge">external</code> 증가 → Buffer/Stream 누수 의심</li>
  </ul>
</blockquote>

<blockquote>
  <p>[!abstract] @Global() 사용 기준
DB, Cache, Storage처럼 앱 전반에서 쓰이는 인프라 레이어에만 적용.
도메인 서비스에 @Global() 남용 시 모듈 의존성 그래프가 불명확해진다.</p>
</blockquote>]]></content><author><name>Paul2021-R</name></author><category term="문제해결" /><category term="Backend" /><category term="개발" /><category term="Protostar" /><category term="Project" /><category term="Review" /><category term="NestJS" /><category term="AI" /><summary type="html"><![CDATA[Protostar Review - 13 - NestJS Common Services]]></summary></entry><entry><title type="html">Protostar review note - 11 - NestJS Chat Domain (SSE + Redis PubSub)</title><link href="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/27/00-protostar-review.html" rel="alternate" type="text/html" title="Protostar review note - 11 - NestJS Chat Domain (SSE + Redis PubSub)" /><published>2026-02-27T00:00:00+00:00</published><updated>2026-02-27T00:00:00+00:00</updated><id>http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/27/00-protostar-review</id><content type="html" xml:base="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/27/00-protostar-review.html"><![CDATA[<h1 id="nestjs-chat-도메인-분석">NestJS Chat 도메인 분석</h1>
<h2 id="sse--redis-pubsub--rxjs">SSE + Redis Pub/Sub + RxJS</h2>

<blockquote>
  <p>이 파일은 실시간 AI 채팅 스트리밍을 담당하는 Chat 도메인을 다룬다.</p>

  <table>
    <thead>
      <tr>
        <th>파일</th>
        <th>역할</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code class="language-plaintext highlighter-rouge">chat.controller.ts</code></td>
        <td>SSE 스트림 엔드포인트, 메시지 디스패치</td>
      </tr>
      <tr>
        <td><code class="language-plaintext highlighter-rouge">chat.service.ts</code></td>
        <td>Redis Pub/Sub, RxJS Subject, 연결 관리</td>
      </tr>
      <tr>
        <td><code class="language-plaintext highlighter-rouge">chat.module.ts</code></td>
        <td>MonitoringModule 의존, 모듈 조립</td>
      </tr>
      <tr>
        <td><code class="language-plaintext highlighter-rouge">dto/create-chat.dto.ts</code></td>
        <td>채팅 메시지 요청 검증 (조건부 검증 포함)</td>
      </tr>
      <tr>
        <td><code class="language-plaintext highlighter-rouge">interface/ChatMessage.ts</code></td>
        <td>SSE 스트림 메시지 타입 계약</td>
      </tr>
    </tbody>
  </table>
</blockquote>

<hr />

<h1 id="1-주요-개념">1. 주요 개념</h1>

<h2 id="왜-websocket이-아닌-sse인가">왜 WebSocket이 아닌 SSE인가</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>SSE (현재)</th>
      <th>WebSocket</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**방향**</td>
      <td>서버 → 클라이언트 단방향</td>
      <td>양방향</td>
    </tr>
    <tr>
      <td>**프로토콜**</td>
      <td>HTTP/1.1 위에서 동작</td>
      <td>별도 WS 프로토콜</td>
    </tr>
    <tr>
      <td>**Nginx 설정**</td>
      <td><code class="language-plaintext highlighter-rouge">X-Accel-Buffering: no</code> 만 추가</td>
      <td>Upgrade 헤더 프록시 설정 필요</td>
    </tr>
    <tr>
      <td>**재연결**</td>
      <td>브라우저가 자동 재연결</td>
      <td>클라이언트가 직접 구현</td>
    </tr>
    <tr>
      <td>**적합한 경우**</td>
      <td>AI 스트리밍, 알림, 로그</td>
      <td>실시간 채팅, 게임, 협업 도구</td>
    </tr>
  </tbody>
</table>

<p>AI 응답은 <strong>서버 → 클라이언트 단방향</strong>이므로 SSE가 구조적으로 더 적합하다. WebSocket의 양방향성은 이 서비스에서 불필요한 복잡도다.</p>

<h2 id="두-단계-채팅-흐름">두 단계 채팅 흐름</h2>

<p>이 프로젝트의 채팅은 <strong>연결(SSE)과 요청(POST)이 분리</strong>된 비동기 구조다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Step 1: GET /api/v1/chat/stream/:sessionId  → SSE 연결 수립 (롱 커넥션)
Step 2: POST /api/v1/chat/message           → 메시지 전송 (일반 HTTP)
Step 3: FastAPI 워커가 처리 후 Redis Publish
Step 4: NestJS가 수신 → SSE로 클라이언트에게 스트리밍
</code></pre></div></div>

<p>POST와 SSE가 분리된 이유: SSE는 서버가 일방적으로 데이터를 밀어내는 구조라 클라이언트가 요청 본문을 SSE 연결로 보낼 수 없다.</p>

<h2 id="rxjs-subject--observable-패턴">RxJS Subject + Observable 패턴</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>streamMap: Map&lt;string, Subject&lt;ChatMessage&gt;&gt;
  ↑ 유저 연결 시 Subject 생성·저장
  ↓ Redis에서 메시지 수신 시 Subject.next() 호출
  → Subject.asObservable() → SSE 스트림으로 전송
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Subject</code>는 Observable이면서 동시에 Observer다. 외부에서 <code class="language-plaintext highlighter-rouge">.next()</code>로 데이터를 밀어 넣으면 구독자(SSE 연결)에게 즉시 전달된다.</p>

<h2 id="redis-pubsub---구독자-커넥션-분리">Redis Pub/Sub - 구독자 커넥션 분리</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">constructor</span><span class="p">(@</span><span class="nd">Inject</span><span class="p">(</span><span class="nx">REDIS_CLIENT</span><span class="p">)</span> <span class="k">private</span> <span class="k">readonly</span> <span class="nx">redisPublisher</span><span class="p">:</span> <span class="nx">Redis</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">this</span><span class="p">.</span><span class="nx">redisSubscriber</span> <span class="o">=</span> <span class="nx">redisPublisher</span><span class="p">.</span><span class="nf">duplicate</span><span class="p">();</span>  <span class="c1">// 별도 커넥션</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Redis 규칙: <strong>Pub/Sub 모드에 진입한 커넥션은 SUBSCRIBE 명령어만 수신 가능</strong>하다. 일반 GET/SET 명령을 함께 쓰려면 반드시 커넥션을 분리해야 한다. <code class="language-plaintext highlighter-rouge">duplicate()</code>는 동일 설정으로 새 커넥션을 생성한다.</p>

<hr />

<h1 id="2-핵심-로직-흐름">2. 핵심 로직 흐름</h1>

<h2 id="전체-흐름-시퀀스">전체 흐름 시퀀스</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Client]
  │
  ├─ GET /stream/:sessionId?uuid={uuid}
  │    │
  │    ├─ AiCircuitGuard: AI 서비스 가용 여부 확인
  │    ├─ ChatService.isOkayToConnect(): 최대 연결 수 확인
  │    ├─ ChatService.addClient(uuid, sessionId)
  │    │    └─ streamMap.set(`${uuid}-${sessionId}`, new Subject())
  │    │
  │    └─ Observable 반환 (merge)
  │         ├─ initEvent:      { type: 'init', uuid, sessionId }   즉시 1회
  │         ├─ heartbeatEvent: { type: 'heartbeat' }               매 N초
  │         └─ messageStream:  Subject.asObservable()              Redis 메시지 수신 시
  │
  ├─ POST /message  { uuid, sessionId, mode, content, context }
  │    │
  │    ├─ AiCircuitGuard: AI 가용 여부 확인
  │    ├─ ChatService.dispatchJob(dto)
  │    │    ├─ 1. hasActiveStream(): uuid-sessionId 가 streamMap에 있는지 확인
  │    │    ├─ 2. mode=page_context → sanitize(context)   XSS 제거
  │    │    ├─ 3. jobId = uuidv4()
  │    │    └─ 4. Redis pipeline
  │    │          ├─ SET  chat:task:{jobId}  payload  EX 300
  │    │          └─ RPUSH chat:job:queue    jobId
  │    │
  │    └─ return { success, status: 'queued', jobId }
  │
[FastAPI 워커]
  │  chat:job:queue 소비 → LLM 처리 → 토큰 스트리밍
  │  → PUBLISH chat:stream:{uuid}-{sessionId}  메시지
  │
[ChatService - Redis Subscriber]
  │  pmessage 이벤트 수신
  │  → routeMessageToUser(channel, message)
  │      └─ streamMap.get(sessionId).next(payload)
  │
[Client SSE]
     Subject.next() → Observable → SSE Event 전송
</code></pre></div></div>

<h2 id="sse-엔드포인트-상세">SSE 엔드포인트 상세</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Public</span><span class="p">()</span>
<span class="p">@</span><span class="nd">Get</span><span class="p">(</span><span class="dl">'</span><span class="s1">stream/:sessionId</span><span class="dl">'</span><span class="p">)</span>
<span class="p">@</span><span class="nd">Sse</span><span class="p">(</span><span class="dl">'</span><span class="s1">stream/:sessionId</span><span class="dl">'</span><span class="p">)</span>            <span class="c1">// NestJS SSE 데코레이터</span>
<span class="p">@</span><span class="nd">UseGuards</span><span class="p">(</span><span class="nx">AiCircuitGuard</span><span class="p">)</span>
<span class="p">@</span><span class="nd">SkipThrottle</span><span class="p">()</span>                      <span class="c1">// 장기 연결 - Rate Limit 카운트 제외</span>
<span class="p">@</span><span class="nd">Header</span><span class="p">(</span><span class="dl">'</span><span class="s1">X-Accel-Buffering</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">no</span><span class="dl">'</span><span class="p">)</span>  <span class="c1">// Nginx 버퍼링 비활성화 (실시간 전송)</span>
<span class="p">@</span><span class="nd">Header</span><span class="p">(</span><span class="dl">'</span><span class="s1">Cache-Control</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">no-cache</span><span class="dl">'</span><span class="p">)</span>
<span class="p">@</span><span class="nd">Header</span><span class="p">(</span><span class="dl">'</span><span class="s1">Connection</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">keep-alive</span><span class="dl">'</span><span class="p">)</span>
<span class="nf">stream</span><span class="p">(@</span><span class="nd">Param</span><span class="p">(</span><span class="dl">'</span><span class="s1">sessionId</span><span class="dl">'</span><span class="p">)</span> <span class="nx">sessionId</span><span class="p">:</span> <span class="kr">string</span><span class="p">,</span> <span class="p">@</span><span class="nd">Query</span><span class="p">(</span><span class="dl">'</span><span class="s1">uuid</span><span class="dl">'</span><span class="p">)</span> <span class="nx">earlyUUID</span><span class="p">?:</span> <span class="kr">string</span><span class="p">)</span> <span class="p">{</span>

  <span class="kd">const</span> <span class="nx">targetUuid</span> <span class="o">=</span> <span class="nx">earlyUUID</span> <span class="o">||</span> <span class="nf">uuidv4</span><span class="p">();</span>  <span class="c1">// 클라이언트가 UUID를 미리 알고 싶을 때</span>

  <span class="kd">const</span> <span class="nx">userStreamSubject</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">chatService</span><span class="p">.</span><span class="nf">addClient</span><span class="p">(</span><span class="nx">targetUuid</span><span class="p">,</span> <span class="nx">sessionId</span><span class="p">);</span>

  <span class="kd">const</span> <span class="nx">initEvent</span> <span class="o">=</span> <span class="k">from</span><span class="p">([{</span> <span class="na">data</span><span class="p">:</span> <span class="p">{</span> <span class="na">type</span><span class="p">:</span> <span class="dl">'</span><span class="s1">init</span><span class="dl">'</span><span class="p">,</span> <span class="na">uuid</span><span class="p">:</span> <span class="nx">targetUuid</span> <span class="p">}</span> <span class="p">}]);</span>

  <span class="kd">const</span> <span class="nx">heartbeatEvent</span> <span class="o">=</span> <span class="nf">interval</span><span class="p">(</span><span class="nx">SSE_HEARTBEAT_INTERVAL</span><span class="p">).</span><span class="nf">pipe</span><span class="p">(</span>
    <span class="nf">map</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">({</span> <span class="na">data</span><span class="p">:</span> <span class="p">{</span> <span class="na">type</span><span class="p">:</span> <span class="dl">'</span><span class="s1">heartbeat</span><span class="dl">'</span><span class="p">,</span> <span class="na">timestamp</span><span class="p">:</span> <span class="k">new</span> <span class="nc">Date</span><span class="p">().</span><span class="nf">toISOString</span><span class="p">()</span> <span class="p">}</span> <span class="p">}))</span>
  <span class="p">);</span>

  <span class="kd">const</span> <span class="nx">messageStream</span> <span class="o">=</span> <span class="nx">userStreamSubject</span><span class="p">.</span><span class="nf">asObservable</span><span class="p">().</span><span class="nf">pipe</span><span class="p">(</span>
    <span class="nf">map</span><span class="p">(</span><span class="nx">payload</span> <span class="o">=&gt;</span> <span class="p">({</span> <span class="na">data</span><span class="p">:</span> <span class="nx">payload</span> <span class="p">}))</span>
  <span class="p">);</span>

  <span class="k">return</span> <span class="nf">merge</span><span class="p">(</span><span class="nx">initEvent</span><span class="p">,</span> <span class="nx">heartbeatEvent</span><span class="p">,</span> <span class="nx">messageStream</span><span class="p">).</span><span class="nf">pipe</span><span class="p">(</span>
    <span class="nf">finalize</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
      <span class="k">this</span><span class="p">.</span><span class="nx">chatService</span><span class="p">.</span><span class="nf">removeClient</span><span class="p">(</span><span class="nx">targetUuid</span><span class="p">,</span> <span class="nx">sessionId</span><span class="p">);</span>
      <span class="c1">// TODO: Redis Unsubscribe Logic</span>
    <span class="p">})</span>
  <span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="dispatchjob-내부-처리"><code class="language-plaintext highlighter-rouge">dispatchJob</code> 내부 처리</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="nf">dispatchJob</span><span class="p">(</span><span class="nx">dto</span><span class="p">:</span> <span class="nx">CreateChatDto</span><span class="p">)</span> <span class="p">{</span>
  <span class="c1">// 1. UUID-sessionId 쌍이 실제 연결 중인지 확인 (보안)</span>
  <span class="k">if </span><span class="p">(</span><span class="o">!</span><span class="k">this</span><span class="p">.</span><span class="nf">hasActiveStream</span><span class="p">(</span><span class="nx">dto</span><span class="p">.</span><span class="nx">uuid</span><span class="p">,</span> <span class="nx">dto</span><span class="p">.</span><span class="nx">sessionId</span><span class="p">))</span> <span class="p">{</span>
    <span class="k">throw</span> <span class="k">new</span> <span class="nc">Error</span><span class="p">(</span><span class="dl">'</span><span class="s1">Unauthorized</span><span class="dl">'</span><span class="p">);</span>  <span class="c1">// SSE 없이 POST만 보내는 시도 차단</span>
  <span class="p">}</span>

  <span class="c1">// 2. page_context 모드일 때만 context XSS 제거</span>
  <span class="kd">let</span> <span class="nx">cleanContext</span><span class="p">:</span> <span class="kr">string</span> <span class="o">|</span> <span class="kc">null</span> <span class="o">=</span> <span class="kc">null</span><span class="p">;</span>
  <span class="k">if </span><span class="p">(</span><span class="nx">dto</span><span class="p">.</span><span class="nx">mode</span> <span class="o">===</span> <span class="dl">'</span><span class="s1">page_context</span><span class="dl">'</span> <span class="o">&amp;&amp;</span> <span class="nx">dto</span><span class="p">.</span><span class="nx">context</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">cleanContext</span> <span class="o">=</span> <span class="nf">sanitize</span><span class="p">(</span><span class="nx">dto</span><span class="p">.</span><span class="nx">context</span><span class="p">,</span> <span class="p">{</span>
      <span class="na">allowedTags</span><span class="p">:</span> <span class="p">[],</span>          <span class="c1">// 모든 HTML 태그 제거</span>
      <span class="na">allowedAttributes</span><span class="p">:</span> <span class="p">{},</span>    <span class="c1">// 모든 속성 제거</span>
      <span class="na">disallowedTagsMode</span><span class="p">:</span> <span class="dl">'</span><span class="s1">discard</span><span class="dl">'</span><span class="p">,</span>
    <span class="p">});</span>
    <span class="nx">cleanContext</span> <span class="o">=</span> <span class="nx">cleanContext</span><span class="p">.</span><span class="nf">trim</span><span class="p">();</span>
  <span class="p">}</span>

  <span class="c1">// 3. Redis pipeline으로 원자적 2-step 저장</span>
  <span class="kd">const</span> <span class="nx">jobId</span> <span class="o">=</span> <span class="nf">uuidv4</span><span class="p">();</span>
  <span class="kd">const</span> <span class="nx">pipeline</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">redisPublisher</span><span class="p">.</span><span class="nf">pipeline</span><span class="p">();</span>
  <span class="nx">pipeline</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="s2">`chat:task:</span><span class="p">${</span><span class="nx">jobId</span><span class="p">}</span><span class="s2">`</span><span class="p">,</span> <span class="nx">JSON</span><span class="p">.</span><span class="nf">stringify</span><span class="p">(</span><span class="nx">payload</span><span class="p">),</span> <span class="dl">'</span><span class="s1">EX</span><span class="dl">'</span><span class="p">,</span> <span class="mi">300</span><span class="p">);</span>
  <span class="nx">pipeline</span><span class="p">.</span><span class="nf">rpush</span><span class="p">(</span><span class="dl">'</span><span class="s1">chat:job:queue</span><span class="dl">'</span><span class="p">,</span> <span class="nx">jobId</span><span class="p">);</span>
  <span class="k">await</span> <span class="nx">pipeline</span><span class="p">.</span><span class="nf">exec</span><span class="p">();</span>

  <span class="k">return</span> <span class="nx">jobId</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="redis-패턴-구독-psubscribe">Redis 패턴 구독 (<code class="language-plaintext highlighter-rouge">psubscribe</code>)</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">redisSubscriber</span><span class="p">.</span><span class="nf">psubscribe</span><span class="p">(</span><span class="dl">'</span><span class="s1">chat:stream:*</span><span class="dl">'</span><span class="p">);</span>
<span class="c1">// 패턴 매칭: chat:stream:uuid-sessionId 형태의 모든 채널 구독</span>

<span class="k">this</span><span class="p">.</span><span class="nx">redisSubscriber</span><span class="p">.</span><span class="nf">on</span><span class="p">(</span><span class="dl">'</span><span class="s1">pmessage</span><span class="dl">'</span><span class="p">,</span> <span class="p">(</span><span class="nx">pattern</span><span class="p">,</span> <span class="nx">channel</span><span class="p">,</span> <span class="nx">message</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="c1">// pattern: 'chat:stream:*'</span>
  <span class="c1">// channel: 'chat:stream:abc123-session456'</span>
  <span class="c1">// message: JSON 문자열</span>
  <span class="k">this</span><span class="p">.</span><span class="nf">routeMessageToUser</span><span class="p">(</span><span class="nx">channel</span><span class="p">,</span> <span class="nx">message</span><span class="p">);</span>
<span class="p">});</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">subscribe</code>는 정확한 채널명, <code class="language-plaintext highlighter-rouge">psubscribe</code>는 <strong>글로브 패턴</strong>으로 구독한다. 유저별로 동적 채널이 생성되는 구조에서는 <code class="language-plaintext highlighter-rouge">psubscribe</code>가 필수다.</p>

<h2 id="연결-수-제어-static-activeconnections">연결 수 제어 (<code class="language-plaintext highlighter-rouge">static activeConnections</code>)</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">private</span> <span class="k">static</span> <span class="nx">activeConnections</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>  <span class="c1">// 클래스 레벨 공유 변수</span>

<span class="k">public</span> <span class="nf">incrementActiveConnections</span><span class="p">():</span> <span class="nx">boolean</span> <span class="p">{</span>
  <span class="k">if </span><span class="p">(</span><span class="nx">ChatService</span><span class="p">.</span><span class="nx">activeConnections</span> <span class="o">&gt;=</span> <span class="nx">CHAT_MAX_CONNECTIONS</span><span class="p">)</span> <span class="k">return</span> <span class="kc">false</span><span class="p">;</span>
  <span class="nx">ChatService</span><span class="p">.</span><span class="nx">activeConnections</span><span class="o">++</span><span class="p">;</span>
  <span class="k">return</span> <span class="kc">true</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">static</code> 변수를 사용하는 이유: NestJS에서 서비스는 기본 Singleton이므로 인스턴스 변수(<code class="language-plaintext highlighter-rouge">this.activeConnections</code>)로도 충분하다. 다만 <code class="language-plaintext highlighter-rouge">static</code>으로 선언하면 클래스 레벨에서 절대적으로 공유됨을 명시적으로 표현한다.</p>

<hr />

<h1 id="3-구조적-취약점--개선-방향">3. 구조적 취약점 / 개선 방향</h1>

<h2 id="chatcontroller-todo-redis-unsubscribe-미구현">[ChatController] TODO: Redis Unsubscribe 미구현</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">return</span> <span class="nf">merge</span><span class="p">(</span><span class="nx">initEvent</span><span class="p">,</span> <span class="nx">heartbeatEvent</span><span class="p">,</span> <span class="nx">messageStream</span><span class="p">).</span><span class="nf">pipe</span><span class="p">(</span>
  <span class="nf">finalize</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="k">this</span><span class="p">.</span><span class="nx">chatService</span><span class="p">.</span><span class="nf">removeClient</span><span class="p">(</span><span class="nx">targetUuid</span><span class="p">,</span> <span class="nx">sessionId</span><span class="p">);</span>
    <span class="c1">//TODO: Implement Redis Unsubscribe Logic</span>
  <span class="p">})</span>
<span class="p">);</span>
</code></pre></div></div>

<p>현재 SSE 연결이 끊겨도 <code class="language-plaintext highlighter-rouge">chat:stream:*</code> 패턴 구독 자체는 유지된다. 연결된 유저가 없는 채널로 메시지가 들어오면 <code class="language-plaintext highlighter-rouge">routeMessageToUser</code>에서 <code class="language-plaintext highlighter-rouge">streamMap</code>에 해당 키가 없어 무시되므로 기능상 문제는 없다. 하지만 연결 수가 많아지면 불필요한 Redis 메시지를 수신해 CPU를 소모한다.</p>

<p><strong>개선 방향</strong>: 유저별 채널 구독(<code class="language-plaintext highlighter-rouge">subscribe</code>)으로 전환하고, 연결 종료 시 해당 채널 <code class="language-plaintext highlighter-rouge">unsubscribe</code>.</p>

<h2 id="chatservice-streammap-키-충돌-가능성">[ChatService] streamMap 키 충돌 가능성</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">this</span><span class="p">.</span><span class="nx">streamMap</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="s2">`</span><span class="p">${</span><span class="nx">uuid</span><span class="p">}</span><span class="s2">-</span><span class="p">${</span><span class="nx">sessionId</span><span class="p">}</span><span class="s2">`</span><span class="p">,</span> <span class="nx">subject</span><span class="p">);</span>
</code></pre></div></div>

<p>UUID + sessionId를 <code class="language-plaintext highlighter-rouge">-</code>로 단순 연결한다. 두 값 모두 클라이언트가 임의로 전송할 수 있으므로, 키 충돌이 발생하면 기존 Subject가 덮어씌워진다. 분리자를 더 안전하게 구성하거나 UUID 형식을 서버에서 강제하면 위험을 줄일 수 있다.</p>

<h2 id="chatservice-routemessagetouser-채널-파싱-취약성">[ChatService] <code class="language-plaintext highlighter-rouge">routeMessageToUser</code> 채널 파싱 취약성</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">private</span> <span class="nf">routeMessageToUser</span><span class="p">(</span><span class="nx">channel</span><span class="p">:</span> <span class="kr">string</span><span class="p">,</span> <span class="nx">messageString</span><span class="p">:</span> <span class="kr">string</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">sessionId</span> <span class="o">=</span> <span class="nx">channel</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="dl">'</span><span class="s1">:</span><span class="dl">'</span><span class="p">).</span><span class="nf">pop</span><span class="p">();</span>  <span class="c1">// 마지막 세그먼트</span>
  <span class="c1">// channel: "chat:stream:uuid-sessionId"</span>
  <span class="c1">// → split(':') → ['chat', 'stream', 'uuid-sessionId']</span>
  <span class="c1">// → pop() → 'uuid-sessionId'</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">channel.split(':').pop()</code>으로 <code class="language-plaintext highlighter-rouge">uuid-sessionId</code> 전체를 꺼내서 streamMap 키로 사용하고 있다. 그런데 <code class="language-plaintext highlighter-rouge">streamMap</code>에는 <code class="language-plaintext highlighter-rouge">${uuid}-${sessionId}</code> 형태로 저장하므로 FastAPI 워커가 채널명을 정확히 <code class="language-plaintext highlighter-rouge">chat:stream:{uuid}-{sessionId}</code> 형태로 발행해야 한다. 이 <strong>채널 이름 형식이 암묵적 계약</strong>이 되어 있어, 한쪽이 변경되면 전체가 깨진다.</p>

<h2 id="createchatdto-mode를-prisma-enum이-아닌-리터럴-유니온으로-선언">[CreateChatDto] <code class="language-plaintext highlighter-rouge">mode</code>를 Prisma enum이 아닌 리터럴 유니온으로 선언</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">IsEnum</span><span class="p">([</span><span class="dl">'</span><span class="s1">general</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">page_context</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">test</span><span class="dl">'</span><span class="p">])</span>
<span class="nx">mode</span><span class="p">:</span> <span class="dl">'</span><span class="s1">general</span><span class="dl">'</span> <span class="o">|</span> <span class="dl">'</span><span class="s1">page_context</span><span class="dl">'</span> <span class="o">|</span> <span class="dl">'</span><span class="s1">test</span><span class="dl">'</span><span class="p">;</span>
</code></pre></div></div>

<p>배열 리터럴로 enum을 직접 정의하고 있다. <code class="language-plaintext highlighter-rouge">'test'</code> 모드가 프로덕션에 그대로 노출되어 있다. 내부 테스트용 모드는 프로덕션 빌드에서 제거하거나 별도 가드로 막아야 한다.</p>

<h2 id="chatservice-정적-연결-수와-streammap-크기의-불일치-가능성">[ChatService] 정적 연결 수와 streamMap 크기의 불일치 가능성</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">public</span> <span class="nf">getStreamMapSize</span><span class="p">()</span> <span class="p">{</span> <span class="k">return</span> <span class="k">this</span><span class="p">.</span><span class="nx">streamMap</span><span class="p">.</span><span class="nx">size</span><span class="p">;</span> <span class="p">}</span>
<span class="k">public</span> <span class="nf">getActiveConnections</span><span class="p">()</span> <span class="p">{</span> <span class="k">return</span> <span class="nx">ChatService</span><span class="p">.</span><span class="nx">activeConnections</span><span class="p">;</span> <span class="p">}</span>
</code></pre></div></div>

<p>두 값이 항상 일치해야 하지만, 예외 상황(예: <code class="language-plaintext highlighter-rouge">addClient</code> 이후 <code class="language-plaintext highlighter-rouge">incrementActiveConnections</code> 실패 경로 등)에서 불일치가 발생할 수 있다. 두 상태를 <code class="language-plaintext highlighter-rouge">streamMap.size</code>로 단일화하면 불일치 자체를 없앨 수 있다.</p>

<h2 id="dispatchjob-hasactivestream-인가-우회-가능성">[dispatchJob] <code class="language-plaintext highlighter-rouge">hasActiveStream</code> 인가 우회 가능성</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if </span><span class="p">(</span><span class="o">!</span><span class="k">this</span><span class="p">.</span><span class="nf">hasActiveStream</span><span class="p">(</span><span class="nx">dto</span><span class="p">.</span><span class="nx">uuid</span><span class="p">,</span> <span class="nx">dto</span><span class="p">.</span><span class="nx">sessionId</span><span class="p">))</span> <span class="p">{</span>
  <span class="k">throw</span> <span class="k">new</span> <span class="nc">Error</span><span class="p">(</span><span class="dl">'</span><span class="s1">Unauthorized</span><span class="dl">'</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>uuid와 sessionId가 모두 클라이언트 제공값이다. 공격자가 타인의 uuid-sessionId를 알면 그 유저의 SSE 스트림에 메시지를 삽입할 수 있다. 현재는 uuid가 서버에서 발급되거나 클라이언트가 생성하는 임의값이어서 예측이 어렵지만, JWT 기반 유저 식별과 연동하면 더 안전해진다.</p>

<hr />

<h1 id="4-핵심-메서드-및-라이브러리-함수-설명">4. 핵심 메서드 및 라이브러리 함수 설명</h1>

<h2 id="sse-데코레이터"><code class="language-plaintext highlighter-rouge">@Sse()</code> 데코레이터</h2>

<p>NestJS에서 SSE 엔드포인트를 선언하는 데코레이터. 반환값이 반드시 <code class="language-plaintext highlighter-rouge">Observable&lt;MessageEvent&gt;</code>이어야 한다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// MessageEvent 형식</span>
<span class="p">{</span> <span class="nl">data</span><span class="p">:</span> <span class="kr">any</span> <span class="p">}</span>  <span class="c1">// SSE의 data: 필드에 직렬화되어 전송</span>
</code></pre></div></div>

<p>브라우저는 <code class="language-plaintext highlighter-rouge">EventSource</code> API로 SSE를 소비하고, 연결이 끊기면 자동으로 재연결을 시도한다.</p>

<h2 id="rxjs-merge">RxJS <code class="language-plaintext highlighter-rouge">merge()</code></h2>

<p>여러 Observable을 하나로 합치는 연산자. 각 소스에서 값이 발행될 때마다 즉시 다운스트림으로 전달한다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">return</span> <span class="nf">merge</span><span class="p">(</span><span class="nx">initEvent</span><span class="p">,</span> <span class="nx">heartbeatEvent</span><span class="p">,</span> <span class="nx">messageStream</span><span class="p">)</span>
<span class="c1">// initEvent: 즉시 1회 발행 후 완료</span>
<span class="c1">// heartbeatEvent: N초마다 계속 발행</span>
<span class="c1">// messageStream: Redis 메시지 수신 시 발행</span>
<span class="c1">// → 세 스트림을 하나의 SSE 스트림으로 병합</span>
</code></pre></div></div>

<h2 id="rxjs-finalize">RxJS <code class="language-plaintext highlighter-rouge">finalize()</code></h2>

<p>Observable이 <strong>완료되거나 에러로 종료될 때</strong> 반드시 실행되는 콜백. <code class="language-plaintext highlighter-rouge">try/finally</code>의 Observable 버전.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">.</span><span class="nf">pipe</span><span class="p">(</span>
  <span class="nf">finalize</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="k">this</span><span class="p">.</span><span class="nx">chatService</span><span class="p">.</span><span class="nf">removeClient</span><span class="p">(</span><span class="nx">uuid</span><span class="p">,</span> <span class="nx">sessionId</span><span class="p">);</span>
    <span class="c1">// SSE 연결 종료(정상/비정상 모두)될 때 Subject 정리 + 연결 수 감소</span>
  <span class="p">})</span>
<span class="p">)</span>
</code></pre></div></div>

<h2 id="rxjs-subject">RxJS <code class="language-plaintext highlighter-rouge">Subject</code></h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">subject</span> <span class="o">=</span> <span class="k">new</span> <span class="nx">Subject</span><span class="o">&lt;</span><span class="nx">ChatMessage</span><span class="o">&gt;</span><span class="p">();</span>
<span class="k">this</span><span class="p">.</span><span class="nx">streamMap</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="s2">`</span><span class="p">${</span><span class="nx">uuid</span><span class="p">}</span><span class="s2">-</span><span class="p">${</span><span class="nx">sessionId</span><span class="p">}</span><span class="s2">`</span><span class="p">,</span> <span class="nx">subject</span><span class="p">);</span>

<span class="c1">// 외부에서 데이터 밀어 넣기</span>
<span class="nx">subject</span><span class="p">.</span><span class="nf">next</span><span class="p">(</span><span class="nx">payload</span><span class="p">);</span>         <span class="c1">// Redis 메시지 수신 시</span>

<span class="c1">// SSE에 연결</span>
<span class="nx">subject</span><span class="p">.</span><span class="nf">asObservable</span><span class="p">()</span>         <span class="c1">// 읽기 전용 Observable로 노출</span>
  <span class="p">.</span><span class="nf">pipe</span><span class="p">(</span><span class="nf">map</span><span class="p">(</span><span class="nx">payload</span> <span class="o">=&gt;</span> <span class="p">({</span> <span class="na">data</span><span class="p">:</span> <span class="nx">payload</span> <span class="p">})))</span>

<span class="c1">// 연결 종료 시</span>
<span class="nx">subject</span><span class="p">.</span><span class="nf">complete</span><span class="p">();</span>            <span class="c1">// 스트림 종료 신호 → SSE 연결 끊김</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Subject</code>를 직접 외부에 노출하지 않고 <code class="language-plaintext highlighter-rouge">asObservable()</code>로 래핑하는 이유: 외부에서 <code class="language-plaintext highlighter-rouge">.next()</code>를 호출하지 못하도록 <strong>쓰기 권한을 제한</strong>하기 위해서다.</p>

<h2 id="rxjs-intervalms">RxJS <code class="language-plaintext highlighter-rouge">interval(ms)</code></h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">heartbeatEvent</span> <span class="o">=</span> <span class="nf">interval</span><span class="p">(</span><span class="nx">SSE_HEARTBEAT_INTERVAL</span><span class="p">).</span><span class="nf">pipe</span><span class="p">(</span>
  <span class="nf">map</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">({</span> <span class="na">data</span><span class="p">:</span> <span class="p">{</span> <span class="na">type</span><span class="p">:</span> <span class="dl">'</span><span class="s1">heartbeat</span><span class="dl">'</span><span class="p">,</span> <span class="p">...</span> <span class="p">}</span> <span class="p">}))</span>
<span class="p">);</span>
</code></pre></div></div>

<p>지정된 밀리초마다 숫자(0, 1, 2, …)를 발행하는 Observable. <code class="language-plaintext highlighter-rouge">map</code>으로 heartbeat 페이로드로 변환한다. Heartbeat 목적: 프록시(Nginx)나 브라우저가 유휴 연결을 끊지 않도록 유지시키는 keepalive 신호.</p>

<h2 id="sanitize-html"><code class="language-plaintext highlighter-rouge">sanitize-html</code></h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">cleanContext</span> <span class="o">=</span> <span class="nf">sanitize</span><span class="p">(</span><span class="nx">dto</span><span class="p">.</span><span class="nx">context</span><span class="p">,</span> <span class="p">{</span>
  <span class="na">allowedTags</span><span class="p">:</span> <span class="p">[],</span>
  <span class="na">allowedAttributes</span><span class="p">:</span> <span class="p">{},</span>
  <span class="na">disallowedTagsMode</span><span class="p">:</span> <span class="dl">'</span><span class="s1">discard</span><span class="dl">'</span><span class="p">,</span>
<span class="p">});</span>
</code></pre></div></div>

<p>HTML 문자열에서 허용되지 않은 태그와 속성을 제거한다. <code class="language-plaintext highlighter-rouge">allowedTags: []</code>는 <strong>모든 HTML 태그를 제거</strong>한다는 의미. <code class="language-plaintext highlighter-rouge">page_context</code> 모드에서 페이지 본문 텍스트를 그대로 LLM에 전달하는데, 악의적인 <code class="language-plaintext highlighter-rouge">&lt;script&gt;</code> 태그나 이벤트 핸들러가 프롬프트 인젝션으로 악용될 수 있어 세탁이 필요하다.</p>

<h2 id="redis-pipeline">Redis <code class="language-plaintext highlighter-rouge">pipeline()</code></h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">pipeline</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">redisPublisher</span><span class="p">.</span><span class="nf">pipeline</span><span class="p">();</span>
<span class="nx">pipeline</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="nx">taskKey</span><span class="p">,</span> <span class="nx">taskPayload</span><span class="p">,</span> <span class="dl">'</span><span class="s1">EX</span><span class="dl">'</span><span class="p">,</span> <span class="mi">300</span><span class="p">);</span>
<span class="nx">pipeline</span><span class="p">.</span><span class="nf">rpush</span><span class="p">(</span><span class="dl">'</span><span class="s1">chat:job:queue</span><span class="dl">'</span><span class="p">,</span> <span class="nx">jobId</span><span class="p">);</span>
<span class="k">await</span> <span class="nx">pipeline</span><span class="p">.</span><span class="nf">exec</span><span class="p">();</span>
</code></pre></div></div>

<p>여러 Redis 명령을 하나의 네트워크 왕복으로 묶어 전송하는 최적화. 각각 <code class="language-plaintext highlighter-rouge">await</code>하면 RTT(Round-Trip Time)가 명령 수만큼 발생하지만, pipeline은 한 번에 전송하고 한 번에 응답을 받는다. 단, 트랜잭션(MULTI/EXEC)은 아니므로 개별 명령 실패 시 rollback은 없다.</p>

<h2 id="validateif-조건부-검증"><code class="language-plaintext highlighter-rouge">@ValidateIf</code> 조건부 검증</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">ValidateIf</span><span class="p">((</span><span class="nx">o</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">o</span><span class="p">.</span><span class="nx">mode</span> <span class="o">===</span> <span class="dl">'</span><span class="s1">page_context</span><span class="dl">'</span><span class="p">)</span>  <span class="c1">// mode가 page_context일 때만 검증</span>
<span class="p">@</span><span class="nd">IsString</span><span class="p">()</span>
<span class="p">@</span><span class="nd">IsNotEmpty</span><span class="p">()</span>
<span class="nx">context</span><span class="p">?:</span> <span class="kr">string</span> <span class="o">|</span> <span class="kc">null</span><span class="p">;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">mode</code>가 <code class="language-plaintext highlighter-rouge">'page_context'</code>일 때만 <code class="language-plaintext highlighter-rouge">context</code> 필드를 필수로 검증한다. 다른 모드에서는 <code class="language-plaintext highlighter-rouge">context</code>가 없어도 통과한다. 복잡한 조건부 유효성 검증을 선언적으로 표현하는 class-validator 패턴이다.</p>

<h2 id="from---단발성-observable"><code class="language-plaintext highlighter-rouge">from([])</code> - 단발성 Observable</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">initEvent</span> <span class="o">=</span> <span class="k">from</span><span class="p">([</span>
  <span class="p">{</span> <span class="na">data</span><span class="p">:</span> <span class="p">{</span> <span class="na">type</span><span class="p">:</span> <span class="dl">'</span><span class="s1">init</span><span class="dl">'</span><span class="p">,</span> <span class="na">uuid</span><span class="p">:</span> <span class="nx">targetUuid</span> <span class="p">}</span> <span class="p">}</span>
<span class="p">]);</span>
</code></pre></div></div>

<p>배열에서 Observable을 생성한다. 배열의 모든 요소를 순서대로 발행하고 즉시 완료된다. <code class="language-plaintext highlighter-rouge">merge</code>에 포함되면 연결 즉시 init 이벤트를 한 번 보내고 그 소스는 종료되지만, 다른 소스(heartbeat, messageStream)가 계속 살아있으므로 전체 Observable은 유지된다.</p>

<hr />

<h1 id="5-대체-가능한-라이브러리-및-트레이드오프">5. 대체 가능한 라이브러리 및 트레이드오프</h1>

<h2 id="sse-vs-websocket-vs-long-polling">SSE vs WebSocket vs Long Polling</h2>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>방향</th>
      <th>복잡도</th>
      <th>적합한 경우</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**SSE** (현재)</td>
      <td>단방향(서버→클라이언트)</td>
      <td>낮음</td>
      <td>AI 스트리밍, 알림, 피드</td>
    </tr>
    <tr>
      <td>**WebSocket**</td>
      <td>양방향</td>
      <td>높음</td>
      <td>실시간 채팅, 게임, 협업</td>
    </tr>
    <tr>
      <td>**Long Polling**</td>
      <td>단방향(흉내)</td>
      <td>중간</td>
      <td>SSE 미지원 환경, 레거시 브라우저</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>SSE는 HTTP/2 위에서 멀티플렉싱되어 성능이 더 좋다. HTTP/1.1에서는 브라우저당 동일 도메인 연결 수 제한(6개)이 있어 탭이 많으면 문제가 될 수 있다.</p>
</blockquote>

<h2 id="redis-pubsub-vs-대안">Redis Pub/Sub vs 대안</h2>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**Redis Pub/Sub** (현재)</td>
      <td>구현 단순, 기존 Redis 재사용</td>
      <td>메시지 영속성 없음, 미연결 시 소실</td>
    </tr>
    <tr>
      <td>**Redis Streams**</td>
      <td>메시지 영속성, Consumer Group</td>
      <td>구현 복잡도 증가</td>
    </tr>
    <tr>
      <td>**Kafka**</td>
      <td>대용량, 재처리 가능, 내구성</td>
      <td>별도 인프라, 운영 복잡도</td>
    </tr>
    <tr>
      <td>**BullMQ**</td>
      <td>NestJS 통합 용이, 재시도 내장</td>
      <td>Pub/Sub보다 큐 개념에 가까움</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>현재 구조에서 FastAPI 워커가 응답을 Publish할 때 NestJS가 연결되어 있지 않으면 메시지가 소실된다. <strong>Redis Streams</strong>로 전환하면 재처리가 가능하지만 구현 복잡도가 높아진다. 서비스 규모를 고려할 때 현재 방식이 합리적인 선택이다.</p>
</blockquote>

<h2 id="rxjs-subject-vs-대안">RxJS <code class="language-plaintext highlighter-rouge">Subject</code> vs 대안</h2>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**Subject** (현재)</td>
      <td>간결, RxJS 생태계 통합</td>
      <td>Cold/Hot Observable 개념 이해 필요</td>
    </tr>
    <tr>
      <td>**EventEmitter**</td>
      <td>Node.js 내장, 친숙</td>
      <td>RxJS 파이프라인과 통합 어려움</td>
    </tr>
    <tr>
      <td>**BehaviorSubject**</td>
      <td>최신 값 캐싱 가능</td>
      <td>AI 스트리밍에는 불필요</td>
    </tr>
    <tr>
      <td>**ReplaySubject**</td>
      <td>이전 값 재전송 가능</td>
      <td>메모리 사용 증가</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>AI 스트리밍은 실시간 토큰 전달이 목적이므로 이전 값 캐싱이 필요 없다. <code class="language-plaintext highlighter-rouge">Subject</code>가 가장 적합하다.</p>
</blockquote>

<h2 id="sanitize-html-vs-대안"><code class="language-plaintext highlighter-rouge">sanitize-html</code> vs 대안</h2>

<table>
  <thead>
    <tr>
      <th>라이브러리</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**sanitize-html** (현재)</td>
      <td>설정 유연, 태그·속성 화이트리스트</td>
      <td>번들 크기 다소 큼</td>
    </tr>
    <tr>
      <td>**DOMPurify**</td>
      <td>브라우저 표준 DOM 파서 사용, 가장 강력</td>
      <td>서버사이드(Node.js)에서 jsdom 필요</td>
    </tr>
    <tr>
      <td>**xss**</td>
      <td>경량, 빠름</td>
      <td>세밀한 설정 제한적</td>
    </tr>
  </tbody>
</table>

<hr />

<h1 id="6-개발자로서-알아야-할-영역">6. 개발자로서 알아야 할 영역</h1>

<h2 id="a-sse-프로토콜-이해">A. SSE 프로토콜 이해</h2>

<p>SSE는 HTTP 응답을 끊지 않고 지속적으로 데이터를 보내는 방식이다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HTTP/1.1 200 OK
Content-Type: text/event-stream
Cache-Control: no-cache
Connection: keep-alive

data: {"type":"init","uuid":"..."}

data: {"type":"heartbeat"}

data: {"type":"message","content":"AI 응답..."}
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">data:</code> 접두사가 붙은 줄이 이벤트 데이터</li>
  <li>빈 줄(<code class="language-plaintext highlighter-rouge">\n\n</code>)이 이벤트 구분자</li>
  <li><code class="language-plaintext highlighter-rouge">@Sse()</code> 데코레이터가 <code class="language-plaintext highlighter-rouge">Content-Type: text/event-stream</code> 헤더를 자동 설정</li>
</ul>

<h2 id="b-x-accel-buffering-no가-필요한-이유">B. <code class="language-plaintext highlighter-rouge">X-Accel-Buffering: no</code>가 필요한 이유</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Header</span><span class="p">(</span><span class="dl">'</span><span class="s1">X-Accel-Buffering</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">no</span><span class="dl">'</span><span class="p">)</span>
</code></pre></div></div>

<p>Nginx는 기본적으로 업스트림(NestJS) 응답을 <strong>버퍼링</strong>했다가 클라이언트에게 보낸다. SSE는 실시간 전송이 목적이므로 버퍼링이 쌓이면 데이터가 지연된다. <code class="language-plaintext highlighter-rouge">X-Accel-Buffering: no</code>는 Nginx에게 “이 응답은 버퍼링하지 말고 즉시 전달하라”고 지시한다.</p>

<h2 id="c-비동기-채팅-아키텍처의-설계-이유">C. 비동기 채팅 아키텍처의 설계 이유</h2>

<p>일반적인 채팅은 <code class="language-plaintext highlighter-rouge">요청 → 응답</code>의 동기 HTTP 구조다. 이 프로젝트가 <strong>SSE 연결 + POST 분리</strong> 구조를 택한 이유:</p>

<ol>
  <li><strong>AI 처리 시간이 불확실</strong>: LLM 추론은 수초~수십 초 소요. HTTP 타임아웃 위험.</li>
  <li><strong>토큰 스트리밍</strong>: AI가 토큰을 생성하는 즉시 사용자에게 보내야 UX가 좋다.</li>
  <li><strong>워커 분리</strong>: NestJS가 AI 추론을 직접 하지 않고 FastAPI 워커에 위임. NestJS는 트래픽 관리에 집중.</li>
</ol>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>동기 방식의 문제:
  POST /chat → [30초 대기] → 응답  (타임아웃 위험, 스트리밍 불가)

현재 비동기 방식:
  POST /chat → { jobId: '...' }     (즉시 응답)
  SSE 연결 → 토큰 하나씩 실시간 수신  (스트리밍 가능)
</code></pre></div></div>

<h2 id="d-hot-observable-vs-cold-observable">D. Hot Observable vs Cold Observable</h2>

<p>RxJS에서 Subject는 <strong>Hot Observable</strong>이다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Cold Observable: 구독할 때마다 처음부터 실행 (HTTP 요청, 타이머 등)
Hot Observable:  이미 실행 중인 스트림을 공유 (Subject, 이벤트 등)
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Subject</code>에 구독 전 발행된 값은 받지 못한다. 이 프로젝트에서 SSE 연결 전에 FastAPI 워커가 응답을 Publish하면 <strong>해당 메시지는 유실</strong>된다. init 이벤트와 heartbeat으로 “연결됨”을 클라이언트에게 알리고, 그 이후 POST로 메시지를 보내도록 유도하는 이유가 여기 있다.</p>

<h2 id="e-redis-pubsub에서-subscriber가-별도-커넥션이어야-하는-이유">E. Redis Pub/Sub에서 Subscriber가 별도 커넥션이어야 하는 이유</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">this</span><span class="p">.</span><span class="nx">redisSubscriber</span> <span class="o">=</span> <span class="nx">redisPublisher</span><span class="p">.</span><span class="nf">duplicate</span><span class="p">();</span>
</code></pre></div></div>

<p>Redis 공식 문서: “Once the client enters the subscribed state it is not supposed to issue any other commands, except for additional SUBSCRIBE, PSUBSCRIBE, UNSUBSCRIBE, PUNSUBSCRIBE, PING, RESET and QUIT commands.”</p>

<p>구독 모드에 진입한 커넥션은 <strong>일반 Redis 명령(GET, SET, RPUSH 등)을 실행할 수 없다</strong>. <code class="language-plaintext highlighter-rouge">duplicate()</code>로 분리하지 않으면 Pub/Sub과 일반 명령을 같은 커넥션에서 쓰다가 에러가 발생한다.</p>

<h2 id="f-프롬프트-인젝션과-sanitize의-한계">F. 프롬프트 인젝션과 sanitize의 한계</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">cleanContext</span> <span class="o">=</span> <span class="nf">sanitize</span><span class="p">(</span><span class="nx">dto</span><span class="p">.</span><span class="nx">context</span><span class="p">,</span> <span class="p">{</span> <span class="na">allowedTags</span><span class="p">:</span> <span class="p">[],</span> <span class="p">...</span> <span class="p">});</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">sanitize-html</code>은 <strong>HTML 기반 XSS를 방어</strong>한다. 하지만 LLM에 대한 <strong>프롬프트 인젝션</strong>(예: “이전 지시를 무시하고…“)은 HTML이 아닌 자연어 공격이므로 sanitize로는 막을 수 없다. 프롬프트 인젝션 방어는 시스템 프롬프트 설계 레벨에서 다뤄야 하는 별도 영역이다.</p>

<hr />

<h1 id="핵심-요약-카드">핵심 요약 카드</h1>

<blockquote>
  <p>[!abstract] Chat 도메인 핵심 구조</p>
  <ul>
    <li><strong>SSE</strong> : 서버 → 클라이언트 단방향 실시간 스트리밍</li>
    <li><strong>Subject</strong> : Redis 메시지를 SSE 스트림으로 라우팅하는 브리지</li>
    <li><strong>Redis Pub/Sub</strong> : NestJS ↔ FastAPI 워커 간 실시간 메시지 전달</li>
    <li><strong>두 단계 분리</strong> : POST(메시지 전송) + SSE(응답 수신) 비동기 구조</li>
  </ul>
</blockquote>

<blockquote>
  <p>[!abstract] 핵심 보안 설계</p>
  <ul>
    <li><code class="language-plaintext highlighter-rouge">hasActiveStream</code> 확인 → SSE 없이 POST만 보내는 시도 차단</li>
    <li><code class="language-plaintext highlighter-rouge">sanitize-html</code> → page_context의 HTML 태그 제거</li>
    <li><code class="language-plaintext highlighter-rouge">@ValidateIf</code> → 모드에 따른 조건부 필드 필수화</li>
    <li><code class="language-plaintext highlighter-rouge">CHAT_MAX_CONNECTIONS</code> → 서버 연결 수 상한 제어</li>
  </ul>
</blockquote>

<blockquote>
  <p>[!question] 설명할 수 있어야 하는 것</p>
  <ol>
    <li>WebSocket 대신 SSE를 선택한 이유</li>
    <li>Redis Subscriber가 Publisher와 <strong>반드시</strong> 별도 커넥션이어야 하는 이유</li>
    <li><code class="language-plaintext highlighter-rouge">Subject</code>와 <code class="language-plaintext highlighter-rouge">BehaviorSubject</code>의 차이와 이 프로젝트에서 Subject를 선택한 이유</li>
    <li><code class="language-plaintext highlighter-rouge">merge(initEvent, heartbeat, messageStream)</code>에서 initEvent가 완료돼도 전체 스트림이 유지되는 이유</li>
    <li><code class="language-plaintext highlighter-rouge">X-Accel-Buffering: no</code>가 없으면 어떤 문제가 발생하는가</li>
    <li><code class="language-plaintext highlighter-rouge">pipeline()</code>이 트랜잭션(MULTI/EXEC)과 다른 점</li>
  </ol>
</blockquote>]]></content><author><name>Paul2021-R</name></author><category term="문제해결" /><category term="Backend" /><category term="개발" /><category term="Protostar" /><category term="Project" /><category term="Review" /><category term="NestJS" /><category term="AI" /><summary type="html"><![CDATA[NestJS Chat 도메인 분석 SSE + Redis Pub/Sub + RxJS]]></summary></entry><entry><title type="html">Protostar review note - 10 - NestJS Auth Domain</title><link href="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/26/00-protostar-review.html" rel="alternate" type="text/html" title="Protostar review note - 10 - NestJS Auth Domain" /><published>2026-02-26T00:00:00+00:00</published><updated>2026-02-26T00:00:00+00:00</updated><id>http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/26/00-protostar-review</id><content type="html" xml:base="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/26/00-protostar-review.html"><![CDATA[<h1 id="nestjs-auth-도메인-분석">NestJS Auth 도메인 분석</h1>

<blockquote>
  <p>이 파일은 인증(Authentication) 전체를 담당하는 Auth 도메인을 다룬다.</p>

  <table>
    <thead>
      <tr>
        <th>파일</th>
        <th>역할</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code class="language-plaintext highlighter-rouge">auth.controller.ts</code></td>
        <td>signup / signin 엔드포인트 노출</td>
      </tr>
      <tr>
        <td><code class="language-plaintext highlighter-rouge">auth.service.ts</code></td>
        <td>회원가입·로그인 비즈니스 로직</td>
      </tr>
      <tr>
        <td><code class="language-plaintext highlighter-rouge">auth.module.ts</code></td>
        <td>JwtModule 비동기 설정, 모듈 조립</td>
      </tr>
      <tr>
        <td><code class="language-plaintext highlighter-rouge">dto/signup.dto.ts</code></td>
        <td>회원가입 요청 유효성 검증</td>
      </tr>
      <tr>
        <td><code class="language-plaintext highlighter-rouge">dto/signin.dto.ts</code></td>
        <td>로그인 요청 유효성 검증</td>
      </tr>
      <tr>
        <td><code class="language-plaintext highlighter-rouge">dto/user.dto.ts</code></td>
        <td>DB User → 응답 변환, password 제거</td>
      </tr>
      <tr>
        <td><code class="language-plaintext highlighter-rouge">common/strategy/jwt.strategy.ts</code></td>
        <td>Passport JWT 검증 전략</td>
      </tr>
      <tr>
        <td><code class="language-plaintext highlighter-rouge">common/guards/jwt-auth.guard.ts</code></td>
        <td>전역 JWT 인증 가드</td>
      </tr>
    </tbody>
  </table>
</blockquote>

<hr />

<h1 id="1-주요-개념">1. 주요 개념</h1>

<h2 id="인증-vs-인가">인증 vs 인가</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>영어</th>
      <th>이 프로젝트에서의 구현</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**인증** (Authentication)</td>
      <td>"너 누구야?"</td>
      <td>JWT 토큰 검증, bcrypt 비밀번호 비교</td>
    </tr>
    <tr>
      <td>**인가** (Authorization)</td>
      <td>"너 이거 해도 돼?"</td>
      <td>Role enum (ADMIN / STARGAZER / PROTOSTAR) + roleKey</td>
    </tr>
  </tbody>
</table>

<p>현재 코드에서 인가(Role 기반 접근 제어)는 회원가입 시 ADMIN 역할 부여에만 적용되어 있고, 엔드포인트별 Role 가드는 구현되어 있지 않다.</p>

<h2 id="stateless-jwt">Stateless JWT</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// JwtStrategy.validate()</span>
<span class="k">async</span> <span class="nf">validate</span><span class="p">(</span><span class="nx">payload</span><span class="p">:</span> <span class="nx">JwtPayload</span><span class="p">):</span> <span class="nb">Promise</span><span class="o">&lt;</span><span class="nb">Partial</span><span class="o">&lt;</span><span class="nx">User</span><span class="o">&gt;&gt;</span> <span class="p">{</span>
  <span class="c1">// stateless: DB 조회 없이 토큰 payload만으로 유저 정보 반환</span>
  <span class="k">return</span> <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="nx">payload</span><span class="p">.</span><span class="nx">sub</span><span class="p">,</span> <span class="na">email</span><span class="p">:</span> <span class="nx">payload</span><span class="p">.</span><span class="nx">email</span><span class="p">,</span> <span class="na">role</span><span class="p">:</span> <span class="nx">payload</span><span class="p">.</span><span class="nx">role</span> <span class="p">};</span>
<span class="p">}</span>
</code></pre></div></div>

<p>토큰이 유효하면 DB를 거치지 않고 payload에서 바로 유저 정보를 꺼낸다. 장점은 빠른 인증(DB I/O 없음)이고, 단점은 토큰 발급 후 계정 정지·역할 변경 등이 즉시 반영되지 않는다는 점이다.</p>

<h2 id="role-기반-접근-제어-rbac">Role 기반 접근 제어 (RBAC)</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kr">enum</span> <span class="nx">Role</span> <span class="p">{</span>
  <span class="nx">ADMIN</span>       <span class="c1">// 관리자 (roleKey 필요)</span>
  <span class="nx">STARGAZER</span>   <span class="c1">// 일반 사용자 (기본값)</span>
  <span class="nx">PROTOSTAR</span>   <span class="c1">// 프리미엄 사용자</span>
<span class="p">}</span>
</code></pre></div></div>

<p>ADMIN 회원가입 시 <code class="language-plaintext highlighter-rouge">roleKey</code>라는 비밀 키를 함께 제출해야 한다. 이는 관리자 계정 무단 생성을 막는 단순하지만 실용적인 방어 수단이다.</p>

<h2 id="public-데코레이터"><code class="language-plaintext highlighter-rouge">@Public()</code> 데코레이터</h2>

<p>Auth 엔드포인트는 로그인 전이므로 JWT 토큰이 없다. 전역 <code class="language-plaintext highlighter-rouge">JwtAuthGuard</code>를 우회하기 위해 <code class="language-plaintext highlighter-rouge">@Public()</code>을 붙인다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Public</span><span class="p">()</span>   <span class="c1">// JwtAuthGuard 전역 인증 우회</span>
<span class="p">@</span><span class="nd">Post</span><span class="p">(</span><span class="dl">'</span><span class="s1">signup</span><span class="dl">'</span><span class="p">)</span>
<span class="k">async</span> <span class="nf">signup</span><span class="p">(@</span><span class="nd">Body</span><span class="p">()</span> <span class="nx">dto</span><span class="p">:</span> <span class="nx">SignupDto</span><span class="p">)</span> <span class="p">{</span> <span class="p">...</span> <span class="p">}</span>
</code></pre></div></div>

<hr />

<h1 id="2-핵심-로직-흐름">2. 핵심 로직 흐름</h1>

<h2 id="전체-auth-흐름">전체 Auth 흐름</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[회원가입]
Client → POST /api/v1/auth/signup
  │
  ├─ ValidationPipe: SignupDto 검증
  │    @IsEmail / @MinLength(8) / @IsEnum(Role) / @IsOptional
  │
  ├─ AuthService.signup()
  │    ├─ prisma.user.findUnique({ email })  ← 중복 이메일 체크
  │    ├─ role === ADMIN → roleKey 검증 (ConfigService.get('ROLE_KEY'))
  │    ├─ bcrypt.hash(password, 10)          ← 비밀번호 해싱
  │    └─ prisma.user.create(...)            ← DB 저장
  │
  └─ return new UserDto(user)               ← password @Exclude 응답

[로그인]
Client → POST /api/v1/auth/signin
  │
  ├─ ValidationPipe: SigninDto 검증
  │
  ├─ AuthService.signin()
  │    ├─ prisma.user.findUnique({ email })  ← 유저 조회
  │    ├─ bcrypt.compare(password, hash)     ← 비밀번호 검증
  │    └─ jwtService.sign({ sub, email, role }) ← JWT 발급
  │
  └─ return { user: UserDto, accessToken }

[이후 모든 요청]
Client → Authorization: Bearer &lt;token&gt;
  │
  └─ JwtAuthGuard → JwtStrategy.validate(payload)
       → request.user = { id, email, role }
       → @ValidateUser() 로 꺼내서 사용
</code></pre></div></div>

<h2 id="authmodulets---jwtmodule-비동기-설정"><code class="language-plaintext highlighter-rouge">auth.module.ts</code> - JwtModule 비동기 설정</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Module</span><span class="p">({</span>
  <span class="na">imports</span><span class="p">:</span> <span class="p">[</span>
    <span class="nx">JwtModule</span><span class="p">.</span><span class="nf">registerAsync</span><span class="p">({</span>
      <span class="na">imports</span><span class="p">:</span> <span class="p">[</span><span class="nx">ConfigModule</span><span class="p">],</span>
      <span class="na">inject</span><span class="p">:</span> <span class="p">[</span><span class="nx">ConfigService</span><span class="p">],</span>
      <span class="na">useFactory</span><span class="p">:</span> <span class="k">async </span><span class="p">(</span><span class="na">config</span><span class="p">:</span> <span class="nx">ConfigService</span><span class="p">):</span> <span class="nb">Promise</span><span class="o">&lt;</span><span class="nx">JwtModuleOptions</span><span class="o">&gt;</span> <span class="o">=&gt;</span> <span class="p">({</span>
        <span class="na">secret</span><span class="p">:</span> <span class="nx">config</span><span class="p">.</span><span class="kd">get</span><span class="o">&lt;</span><span class="kr">string</span><span class="o">&gt;</span><span class="p">(</span><span class="dl">'</span><span class="s1">JWT_SECRET</span><span class="dl">'</span><span class="p">),</span>
        <span class="na">signOptions</span><span class="p">:</span> <span class="p">{</span>
          <span class="na">expiresIn</span><span class="p">:</span> <span class="nc">Number</span><span class="p">(</span><span class="nx">config</span><span class="p">.</span><span class="kd">get</span><span class="o">&lt;</span><span class="kr">string</span><span class="o">&gt;</span><span class="p">(</span><span class="dl">'</span><span class="s1">JWT_EXPIRATION</span><span class="dl">'</span><span class="p">)),</span>
        <span class="p">},</span>
      <span class="p">}),</span>
    <span class="p">}),</span>
  <span class="p">],</span>
  <span class="na">controllers</span><span class="p">:</span> <span class="p">[</span><span class="nx">AuthController</span><span class="p">],</span>
  <span class="na">providers</span><span class="p">:</span> <span class="p">[</span><span class="nx">AuthService</span><span class="p">,</span> <span class="nx">JwtStrategy</span><span class="p">],</span>
  <span class="na">exports</span><span class="p">:</span> <span class="p">[</span><span class="nx">AuthService</span><span class="p">,</span> <span class="nx">JwtModule</span><span class="p">],</span>   <span class="c1">// JwtModule export → 다른 모듈에서 JwtService 사용 가능</span>
<span class="p">})</span>
<span class="k">export</span> <span class="kd">class</span> <span class="nc">AuthModule</span> <span class="p">{}</span>
</code></pre></div></div>

<h2 id="signupdto-검증-흐름">SignupDto 검증 흐름</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">export</span> <span class="kd">class</span> <span class="nc">SignupDto</span> <span class="p">{</span>
  <span class="p">@</span><span class="nd">IsEmail</span><span class="p">()</span>               <span class="c1">// 이메일 형식 검증 (RFC 5322)</span>
  <span class="nx">email</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>

  <span class="p">@</span><span class="nd">IsString</span><span class="p">()</span>
  <span class="p">@</span><span class="nd">MinLength</span><span class="p">(</span><span class="mi">8</span><span class="p">)</span>            <span class="c1">// 최소 8자 (brute force 최소 방어)</span>
  <span class="nx">password</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>

  <span class="p">@</span><span class="nd">IsOptional</span><span class="p">()</span>            <span class="c1">// 없으면 DB default(STARGAZER) 적용</span>
  <span class="p">@</span><span class="nd">IsEnum</span><span class="p">(</span><span class="nx">Role</span><span class="p">)</span>            <span class="c1">// 'ADMIN' | 'STARGAZER' | 'PROTOSTAR' 외 값 차단</span>
  <span class="nx">role</span><span class="p">?:</span> <span class="nx">Role</span><span class="p">;</span>

  <span class="p">@</span><span class="nd">IsOptional</span><span class="p">()</span>
  <span class="p">@</span><span class="nd">IsString</span><span class="p">()</span>
  <span class="nx">roleKey</span><span class="p">?:</span> <span class="kr">string</span><span class="p">;</span>        <span class="c1">// ADMIN 역할 신청 시 비밀 키</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ValidationPipe({ whitelist: true })</code>와 함께 동작하므로, 위 필드 외의 값은 자동 제거된다.</p>

<h2 id="admin-rolekey-검증-흐름">ADMIN roleKey 검증 흐름</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="nf">signup</span><span class="p">(</span><span class="nx">dto</span><span class="p">:</span> <span class="nx">SignupDto</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">if </span><span class="p">(</span><span class="nx">role</span> <span class="o">===</span> <span class="nx">Role</span><span class="p">.</span><span class="nx">ADMIN</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">roleKey</span> <span class="o">!==</span> <span class="k">this</span><span class="p">.</span><span class="nx">configService</span><span class="p">.</span><span class="kd">get</span><span class="o">&lt;</span><span class="kr">string</span><span class="o">&gt;</span><span class="p">(</span><span class="dl">'</span><span class="s1">ROLE_KEY</span><span class="dl">'</span><span class="p">))</span> <span class="p">{</span>
      <span class="k">this</span><span class="p">.</span><span class="nx">logger</span><span class="p">.</span><span class="nf">warn</span><span class="p">(</span><span class="s2">`Failed admin signup attempt for </span><span class="p">${</span><span class="nx">email</span><span class="p">}</span><span class="s2">`</span><span class="p">);</span>
      <span class="k">throw</span> <span class="k">new</span> <span class="nc">ForbiddenException</span><span class="p">(</span><span class="dl">'</span><span class="s1">Invalid Admin role key</span><span class="dl">'</span><span class="p">);</span>
    <span class="p">}</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<ul>
  <li>ADMIN 이외 역할(STARGAZER, PROTOSTAR)은 roleKey 없이 가입 가능</li>
  <li>키가 틀릴 경우 <code class="language-plaintext highlighter-rouge">ForbiddenException(403)</code> 반환</li>
  <li>시도 자체를 <code class="language-plaintext highlighter-rouge">logger.warn</code>으로 기록 → Loki로 수집되어 이상 접근 감지 가능</li>
</ul>

<h2 id="userdto---응답에서-password-제거">UserDto - 응답에서 password 제거</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">export</span> <span class="kd">class</span> <span class="nc">UserDto</span> <span class="p">{</span>
  <span class="p">@</span><span class="nd">Expose</span><span class="p">()</span> <span class="nx">id</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
  <span class="p">@</span><span class="nd">Expose</span><span class="p">()</span> <span class="nx">email</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
  <span class="p">@</span><span class="nd">Expose</span><span class="p">()</span> <span class="nx">name</span><span class="p">?:</span> <span class="kr">string</span><span class="p">;</span>
  <span class="p">@</span><span class="nd">Exclude</span><span class="p">()</span> <span class="nx">password</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>   <span class="c1">// ClassSerializerInterceptor가 직렬화 시 제거</span>
  <span class="p">@</span><span class="nd">Expose</span><span class="p">()</span> <span class="nx">role</span><span class="p">:</span> <span class="nx">Role</span><span class="p">;</span>
  <span class="p">@</span><span class="nd">Expose</span><span class="p">()</span> <span class="nx">createdAt</span><span class="p">:</span> <span class="nb">Date</span><span class="p">;</span>

  <span class="nf">constructor</span><span class="p">(</span><span class="nx">dto</span><span class="p">:</span> <span class="nx">User</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">this</span><span class="p">.</span><span class="nx">id</span> <span class="o">=</span> <span class="nx">dto</span><span class="p">.</span><span class="nx">id</span><span class="p">;</span>
    <span class="k">this</span><span class="p">.</span><span class="nx">email</span> <span class="o">=</span> <span class="nx">dto</span><span class="p">.</span><span class="nx">email</span><span class="p">;</span>
    <span class="k">this</span><span class="p">.</span><span class="nx">password</span> <span class="o">=</span> <span class="nx">dto</span><span class="p">.</span><span class="nx">password</span><span class="p">;</span>  <span class="c1">// 할당은 하되, 직렬화 시 자동 제거</span>
    <span class="k">this</span><span class="p">.</span><span class="nx">name</span> <span class="o">=</span> <span class="nx">dto</span><span class="p">.</span><span class="nx">name</span> <span class="o">||</span> <span class="kc">undefined</span><span class="p">;</span>
    <span class="k">this</span><span class="p">.</span><span class="nx">role</span> <span class="o">=</span> <span class="nx">dto</span><span class="p">.</span><span class="nx">role</span><span class="p">;</span>
    <span class="k">this</span><span class="p">.</span><span class="nx">createdAt</span> <span class="o">=</span> <span class="nx">dto</span><span class="p">.</span><span class="nx">createdAt</span><span class="p">;</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<blockquote>
  <p>[!note] 왜 할당하고 @Exclude 하는가
TypeScript strict 모드에서 <code class="language-plaintext highlighter-rouge">password</code>를 초기화하지 않으면 컴파일 에러가 발생한다.
생성자에서 할당은 하되, <code class="language-plaintext highlighter-rouge">ClassSerializerInterceptor</code>가 JSON 직렬화 시 <code class="language-plaintext highlighter-rouge">@Exclude()</code> 필드를 제거한다.
응답 JSON에는 절대 포함되지 않는다.</p>
</blockquote>

<hr />

<h1 id="3-구조적-취약점--개선-방향">3. 구조적 취약점 / 개선 방향</h1>

<h2 id="authcontroller-logger가-선언만-되고-사용되지-않음">[AuthController] Logger가 선언만 되고 사용되지 않음</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">export</span> <span class="kd">class</span> <span class="nc">AuthController</span> <span class="p">{</span>
  <span class="k">private</span> <span class="k">readonly</span> <span class="nx">logger</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Logger</span><span class="p">(</span><span class="nx">AuthController</span><span class="p">.</span><span class="nx">name</span><span class="p">);</span>
  <span class="c1">// 실제 사용 없음</span>
<span class="p">}</span>
</code></pre></div></div>

<p>컨트롤러에서 로그를 남기지 않는다. 회원가입·로그인 시도(성공/실패 모두)는 보안 감사(Audit Log) 관점에서 기록이 필요하다. 현재는 <code class="language-plaintext highlighter-rouge">AuthService</code>에서 ADMIN 실패만 warn으로 남기고 있다.</p>

<h2 id="signupdto-비밀번호-복잡도-검증-없음">[SignupDto] 비밀번호 복잡도 검증 없음</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">MinLength</span><span class="p">(</span><span class="mi">8</span><span class="p">)</span>
<span class="nx">password</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
<span class="c1">// MaxLength, 특수문자, 대소문자 조합 등 미검증</span>
</code></pre></div></div>

<p>최소 길이(8자)만 검증한다. 프로덕션에서는 <code class="language-plaintext highlighter-rouge">@Matches(/정규식/)</code> 또는 별도 검증 로직으로 복잡도를 강제해야 한다.</p>

<p><strong>개선 예시:</strong></p>
<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Matches</span><span class="p">(</span><span class="sr">/^</span><span class="se">(?=</span><span class="sr">.*</span><span class="se">[</span><span class="sr">a-z</span><span class="se">])(?=</span><span class="sr">.*</span><span class="se">[</span><span class="sr">A-Z</span><span class="se">])(?=</span><span class="sr">.*</span><span class="se">\d)(?=</span><span class="sr">.*</span><span class="se">[</span><span class="sr">@$!%*?&amp;</span><span class="se">])</span><span class="sr">.</span><span class="se">{8,}</span><span class="sr">$/</span><span class="p">,</span> <span class="p">{</span>
  <span class="na">message</span><span class="p">:</span> <span class="dl">'</span><span class="s1">비밀번호는 대소문자, 숫자, 특수문자를 포함해야 합니다</span><span class="dl">'</span><span class="p">,</span>
<span class="p">})</span>
<span class="nx">password</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
</code></pre></div></div>

<h2 id="authservice-admin-rolekey-평문-비교">[AuthService] ADMIN roleKey 평문 비교</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if </span><span class="p">(</span><span class="nx">roleKey</span> <span class="o">!==</span> <span class="k">this</span><span class="p">.</span><span class="nx">configService</span><span class="p">.</span><span class="kd">get</span><span class="o">&lt;</span><span class="kr">string</span><span class="o">&gt;</span><span class="p">(</span><span class="dl">'</span><span class="s1">ROLE_KEY</span><span class="dl">'</span><span class="p">))</span> <span class="p">{</span> <span class="p">...</span> <span class="p">}</span>
</code></pre></div></div>

<p>환경변수의 roleKey를 평문으로 직접 비교한다. 타이밍 공격(Timing Attack) 가능성이 이론적으로 존재하지만, 이 키는 비밀번호가 아니라 정적 관리자 키이므로 실용적 위험도는 낮다. <code class="language-plaintext highlighter-rouge">crypto.timingSafeEqual()</code>로 개선 가능하다.</p>

<h2 id="auth-전체-토큰-갱신refresh-token-미구현">[Auth 전체] 토큰 갱신(Refresh Token) 미구현</h2>

<p>현재는 <code class="language-plaintext highlighter-rouge">accessToken</code> 하나만 발급한다. 만료 시 재로그인이 필요하다. 프로덕션 수준에서는 단기 <code class="language-plaintext highlighter-rouge">accessToken</code> + 장기 <code class="language-plaintext highlighter-rouge">refreshToken</code> 구조가 표준이다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>현재: accessToken (만료 시 재로그인)

개선:
  accessToken  (단기, 15분~1시간)
  refreshToken (장기, 7~30일, DB/Redis에 저장)
  → /auth/refresh 엔드포인트로 갱신
</code></pre></div></div>

<h2 id="auth-전체-계정-잠금--로그인-시도-제한-없음">[Auth 전체] 계정 잠금 / 로그인 시도 제한 없음</h2>

<p><code class="language-plaintext highlighter-rouge">bcrypt.compare</code>가 실패해도 별다른 제한 없이 무제한 시도 가능하다. <code class="language-plaintext highlighter-rouge">ThrottlerGuard</code>가 전역으로 초당 2000 req를 허용하고 있으므로 Credential Stuffing 공격에 취약하다.</p>

<p><strong>개선 방향</strong>: Redis에 <code class="language-plaintext highlighter-rouge">login:fail:{email}</code> 카운터를 두고, N회 초과 시 계정 임시 잠금.</p>

<h2 id="jwtmodule-expiresin-타입-변환-주의">[JwtModule] expiresIn 타입 변환 주의</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">expiresIn</span><span class="p">:</span> <span class="nc">Number</span><span class="p">(</span><span class="nx">config</span><span class="p">.</span><span class="kd">get</span><span class="o">&lt;</span><span class="kr">string</span><span class="o">&gt;</span><span class="p">(</span><span class="dl">'</span><span class="s1">JWT_EXPIRATION</span><span class="dl">'</span><span class="p">)),</span>
</code></pre></div></div>

<p>환경변수는 문자열로 읽히므로 <code class="language-plaintext highlighter-rouge">Number()</code> 변환이 필요하다. 환경변수가 숫자가 아닌 문자열(<code class="language-plaintext highlighter-rouge">'7d'</code> 같은 값)이 들어오면 <code class="language-plaintext highlighter-rouge">NaN</code>이 되어 토큰이 즉시 만료된다.</p>

<hr />

<h1 id="4-핵심-메서드-및-라이브러리-함수-설명">4. 핵심 메서드 및 라이브러리 함수 설명</h1>

<h2 id="bcrypthashpassword-saltrounds"><code class="language-plaintext highlighter-rouge">bcrypt.hash(password, saltRounds)</code></h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">hashedPassword</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">bcrypt</span><span class="p">.</span><span class="nf">hash</span><span class="p">(</span><span class="nx">password</span><span class="p">,</span> <span class="mi">10</span><span class="p">);</span>
<span class="c1">// saltRounds: 10 → 2^10 = 1024번 반복 해싱</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">saltRounds</code>가 높을수록 안전하지만 연산 비용 증가</li>
  <li>기본값 10은 일반 서버에서 약 100ms 소요 → 브루트포스 1초에 10회 시도 제한 효과</li>
  <li><strong>랜덤 salt 자동 생성·포함</strong>: 같은 비밀번호도 매번 다른 해시 생성 → Rainbow Table 공격 방지</li>
</ul>

<h2 id="bcryptcompareplaintext-hash"><code class="language-plaintext highlighter-rouge">bcrypt.compare(plaintext, hash)</code></h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">await</span> <span class="nx">bcrypt</span><span class="p">.</span><span class="nf">compare</span><span class="p">(</span><span class="nx">password</span><span class="p">,</span> <span class="nx">user</span><span class="p">.</span><span class="nx">password</span><span class="p">)</span>  <span class="c1">// boolean</span>
</code></pre></div></div>

<ul>
  <li><strong>상수 시간(Constant-time) 비교</strong>: 문자열 길이나 내용에 무관하게 항상 동일한 시간 소요</li>
  <li>일반 <code class="language-plaintext highlighter-rouge">===</code> 비교는 첫 번째 다른 문자에서 즉시 반환 → 타이밍 공격으로 해시 추론 가능</li>
  <li><code class="language-plaintext highlighter-rouge">bcrypt.compare</code>는 이를 방지하도록 설계됨</li>
</ul>

<h2 id="jwtservicesignpayload"><code class="language-plaintext highlighter-rouge">jwtService.sign(payload)</code></h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">token</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">jwtService</span><span class="p">.</span><span class="nf">sign</span><span class="p">({</span>
  <span class="na">sub</span><span class="p">:</span> <span class="nx">user</span><span class="p">.</span><span class="nx">id</span><span class="p">,</span>     <span class="c1">// JWT 표준 클레임: subject (유저 ID)</span>
  <span class="na">email</span><span class="p">:</span> <span class="nx">user</span><span class="p">.</span><span class="nx">email</span><span class="p">,</span>
  <span class="na">role</span><span class="p">:</span> <span class="nx">user</span><span class="p">.</span><span class="nx">role</span><span class="p">,</span>
<span class="p">});</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">sub</code>: JWT 표준에서 주체(subject) 식별자. 유저 ID를 관례상 여기에 넣는다.</li>
  <li>서명에는 <code class="language-plaintext highlighter-rouge">JWT_SECRET</code> 환경변수 사용 (HS256 알고리즘)</li>
  <li>검증 시 <code class="language-plaintext highlighter-rouge">JwtStrategy</code>가 자동으로 서명 유효성 + 만료 여부를 확인</li>
</ul>

<h2 id="passportstrategystrategy---jwtstrategy"><code class="language-plaintext highlighter-rouge">PassportStrategy(Strategy)</code> - JwtStrategy</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">export</span> <span class="kd">class</span> <span class="nc">JwtStrategy</span> <span class="kd">extends</span> <span class="nc">PassportStrategy</span><span class="p">(</span><span class="nx">Strategy</span><span class="p">)</span> <span class="p">{</span>
  <span class="nf">constructor</span><span class="p">(</span><span class="nx">config</span><span class="p">:</span> <span class="nx">ConfigService</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">super</span><span class="p">({</span>
      <span class="na">jwtFromRequest</span><span class="p">:</span> <span class="nx">ExtractJwt</span><span class="p">.</span><span class="nf">fromAuthHeaderAsBearerToken</span><span class="p">(),</span>
      <span class="c1">// Authorization: Bearer &lt;token&gt; 헤더에서 추출</span>
      <span class="na">ignoreExpiration</span><span class="p">:</span> <span class="kc">false</span><span class="p">,</span>  <span class="c1">// 만료된 토큰 거부</span>
      <span class="nx">secretOrKey</span><span class="p">,</span>              <span class="c1">// 서명 검증 키</span>
    <span class="p">});</span>
  <span class="p">}</span>

  <span class="k">async</span> <span class="nf">validate</span><span class="p">(</span><span class="nx">payload</span><span class="p">:</span> <span class="nx">JwtPayload</span><span class="p">):</span> <span class="nb">Promise</span><span class="o">&lt;</span><span class="nb">Partial</span><span class="o">&lt;</span><span class="nx">User</span><span class="o">&gt;&gt;</span> <span class="p">{</span>
    <span class="k">return</span> <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="nx">payload</span><span class="p">.</span><span class="nx">sub</span><span class="p">,</span> <span class="na">email</span><span class="p">:</span> <span class="nx">payload</span><span class="p">.</span><span class="nx">email</span><span class="p">,</span> <span class="na">role</span><span class="p">:</span> <span class="nx">payload</span><span class="p">.</span><span class="nx">role</span> <span class="p">};</span>
    <span class="c1">// 반환값 → request.user 에 자동 세팅</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">validate()</code>의 반환값이 <code class="language-plaintext highlighter-rouge">request.user</code>가 된다. 이후 <code class="language-plaintext highlighter-rouge">@ValidateUser()</code> 커스텀 데코레이터로 꺼내 사용한다.</p>

<h2 id="class-validator-데코레이터"><code class="language-plaintext highlighter-rouge">class-validator</code> 데코레이터</h2>

<table>
  <thead>
    <tr>
      <th>데코레이터</th>
      <th>검증 내용</th>
      <th>사용처</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@IsEmail()</code></td>
      <td>RFC 5322 이메일 형식</td>
      <td>email 필드</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@IsString()</code></td>
      <td>문자열 타입</td>
      <td>password, roleKey</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@MinLength(8)</code></td>
      <td>최소 8자</td>
      <td>password</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@IsEnum(Role)</code></td>
      <td>허용된 enum 값만 통과</td>
      <td>role</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@IsOptional()</code></td>
      <td>필드 없어도 허용</td>
      <td>role, roleKey</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">ValidationPipe</code>가 이 데코레이터들을 읽어 검증 실패 시 <code class="language-plaintext highlighter-rouge">400 BadRequestException</code>을 자동 반환한다.</p>

<hr />

<h1 id="5-대체-가능한-라이브러리-및-트레이드오프">5. 대체 가능한 라이브러리 및 트레이드오프</h1>

<h2 id="passport-jwt-vs-직접-구현">Passport JWT vs 직접 구현</h2>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**passport-jwt** (현재)</td>
      <td>NestJS 공식 통합, 전략 패턴으로 OAuth 등 확장 용이</td>
      <td>추상화 레이어 많아 흐름 파악 어려움</td>
    </tr>
    <tr>
      <td>**jose**</td>
      <td>웹 표준(RFC) 기반, 엣지 환경 지원, 경량</td>
      <td>Passport 생태계 없음, 직접 미들웨어 구현 필요</td>
    </tr>
    <tr>
      <td>**jsonwebtoken**</td>
      <td>간단하고 투명한 구현</td>
      <td>단일 JWT만 지원, 전략 확장 없음</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>OAuth2, Google/GitHub 소셜 로그인 등을 나중에 추가할 계획이라면 Passport가 유리하다. JWT만 쓸 것이라면 <code class="language-plaintext highlighter-rouge">jose</code>가 더 현대적인 선택이다.</p>
</blockquote>

<h2 id="bcrypt-vs-argon2">bcrypt vs Argon2</h2>

<table>
  <thead>
    <tr>
      <th>라이브러리</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**bcrypt** (현재)</td>
      <td>가장 검증된 방식, 광범위한 채택</td>
      <td>GPU 병렬 공격에 상대적으로 취약</td>
    </tr>
    <tr>
      <td>**Argon2id**</td>
      <td>OWASP 1순위 권장, 메모리 하드 함수 (GPU 저항)</td>
      <td>상대적으로 낮은 범용 채택률</td>
    </tr>
    <tr>
      <td>**scrypt**</td>
      <td>Node.js 내장 지원</td>
      <td>파라미터 설정 복잡</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>OWASP Password Storage Cheat Sheet(2024)는 <strong>Argon2id</strong>를 1순위로 권장한다. 신규 프로젝트에서는 <code class="language-plaintext highlighter-rouge">argon2</code> 패키지 도입이 더 권장된다.</p>
</blockquote>

<h2 id="토큰-저장-방식">토큰 저장 방식</h2>

<table>
  <thead>
    <tr>
      <th>방식</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**응답 body → 클라이언트 저장** (현재)</td>
      <td>구현 단순</td>
      <td>XSS 공격으로 localStorage 탈취 위험</td>
    </tr>
    <tr>
      <td>**HttpOnly Cookie**</td>
      <td>XSS 방어 (JS 접근 불가)</td>
      <td>CSRF 공격 가능성, SameSite 설정 필요</td>
    </tr>
    <tr>
      <td>**Refresh Token Rotation**</td>
      <td>토큰 탈취 감지 가능</td>
      <td>구현 복잡도 증가</td>
    </tr>
  </tbody>
</table>

<hr />

<h1 id="6-개발자로서-알아야-할-영역">6. 개발자로서 알아야 할 영역</h1>

<h2 id="a-jwt-토큰-구조-이해">A. JWT 토큰 구조 이해</h2>

<p>JWT는 <code class="language-plaintext highlighter-rouge">Header.Payload.Signature</code> 세 부분으로 구성된다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>eyJhbGciOiJIUzI1NiJ9          ← Header (알고리즘)
.eyJzdWIiOiJ1c2VyLWlkIn0      ← Payload (데이터, Base64, 평문!)
.SflKxwRJSMeKKF2QT4fwpMeJf36  ← Signature (위변조 방지)
</code></pre></div></div>

<blockquote>
  <p>[!warning] Payload는 암호화가 아니다
JWT Payload는 <strong>Base64 인코딩</strong>일 뿐, 누구나 디코딩할 수 있다.
<code class="language-plaintext highlighter-rouge">email</code>, <code class="language-plaintext highlighter-rouge">role</code> 같은 민감하지 않은 정보만 담아야 한다.
비밀번호, 개인정보 등을 Payload에 넣으면 안 된다.</p>
</blockquote>

<p>이 프로젝트의 Payload: <code class="language-plaintext highlighter-rouge">{ sub: userId, email, role }</code> → 적절한 수준.</p>

<h2 id="b-stateless-jwt의-한계와-대응">B. Stateless JWT의 한계와 대응</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>문제 시나리오:
1. 유저 A가 로그인 → accessToken 발급 (7일 유효)
2. 관리자가 유저 A 계정 정지
3. 유저 A는 7일 동안 여전히 API 사용 가능
   → DB는 정지됐지만 토큰은 유효하므로 JwtStrategy가 DB 조회 없이 통과
</code></pre></div></div>

<p>해결 방법:</p>
<ol>
  <li><strong>토큰 만료 시간 단축</strong> (15분) + Refresh Token 구조</li>
  <li><strong>Redis 블랙리스트</strong>: 로그아웃/정지된 토큰의 <code class="language-plaintext highlighter-rouge">jti</code>(JWT ID)를 Redis에 저장, 검증 시 조회</li>
  <li><strong>DB 조회 방식으로 전환</strong>: validate()에서 DB 조회 (Stateless 장점 포기)</li>
</ol>

<h2 id="c-isenumrole--prisma-enum-연동">C. <code class="language-plaintext highlighter-rouge">@IsEnum(Role)</code> + Prisma enum 연동</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// DTO</span>
<span class="p">@</span><span class="nd">IsEnum</span><span class="p">(</span><span class="nx">Role</span><span class="p">)</span>  <span class="c1">// Prisma가 생성한 Role enum 재사용</span>
<span class="nx">role</span><span class="p">?:</span> <span class="nx">Role</span><span class="p">;</span>

<span class="c1">// Prisma schema</span>
<span class="kr">enum</span> <span class="nx">Role</span> <span class="p">{</span>
  <span class="nx">ADMIN</span>
  <span class="nx">STARGAZER</span>
  <span class="nx">PROTOSTAR</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Prisma가 생성한 <code class="language-plaintext highlighter-rouge">Role</code> enum을 DTO에서 그대로 사용한다. 스키마가 단일 출처(Single Source of Truth)가 되어 enum 값이 추가되면 DTO까지 자동으로 반영된다.</p>

<h2 id="d-validationpipe와-dto의-관계">D. ValidationPipe와 DTO의 관계</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Request Body (JSON)
      │
      ▼
ValidationPipe
  1. JSON → SignupDto 클래스 인스턴스로 변환 (transform: true)
  2. 각 @Is* 데코레이터 검증 실행
  3. @IsOptional() 필드는 없어도 통과
  4. whitelist: true → DTO에 없는 필드 제거
  5. 검증 실패 → 400 BadRequestException 자동 반환
      │
      ▼
Controller (@Body() dto: SignupDto)
</code></pre></div></div>

<h2 id="e-모듈-exports-설계">E. 모듈 exports 설계</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// auth.module.ts</span>
<span class="nx">exports</span><span class="p">:</span> <span class="p">[</span><span class="nx">AuthService</span><span class="p">,</span> <span class="nx">JwtModule</span><span class="p">]</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">JwtModule</code>을 export하는 이유: 다른 모듈에서 <code class="language-plaintext highlighter-rouge">JwtService</code>를 주입받아 토큰 발급·검증이 필요할 때를 대비한 것이다. 현재 코드에서 실제로 사용되는 모듈은 없지만, 확장성을 위한 설계이다.</p>

<h2 id="f-로그인-실패-응답-보안-원칙">F. 로그인 실패 응답 보안 원칙</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 현재 코드</span>
<span class="k">if </span><span class="p">(</span><span class="o">!</span><span class="nx">user</span> <span class="o">||</span> <span class="o">!</span><span class="p">(</span><span class="k">await</span> <span class="nx">bcrypt</span><span class="p">.</span><span class="nf">compare</span><span class="p">(</span><span class="nx">password</span><span class="p">,</span> <span class="nx">user</span><span class="p">.</span><span class="nx">password</span><span class="p">)))</span> <span class="p">{</span>
  <span class="k">throw</span> <span class="k">new</span> <span class="nc">UnauthorizedException</span><span class="p">(</span><span class="dl">'</span><span class="s1">Invalid credentials</span><span class="dl">'</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>이메일이 없는 경우와 비밀번호가 틀린 경우를 <strong>동일한 메시지</strong>로 응답한다. 이는 의도적인 설계다.</p>

<p>“이메일이 존재하지 않습니다” vs “비밀번호가 틀립니다”로 구분하면 공격자가 이메일 존재 여부를 확인하는 <strong>계정 열거 공격(Account Enumeration)</strong>이 가능해진다. 항상 동일한 에러 메시지를 반환해야 한다.</p>

<hr />

<h1 id="핵심-요약-카드">핵심 요약 카드</h1>

<blockquote>
  <p>[!abstract] Auth 도메인 흐름 한 줄 요약</p>
  <ul>
    <li><strong>signup</strong>: DTO 검증 → 중복 체크 → bcrypt 해싱 → DB 저장 → UserDto 반환</li>
    <li><strong>signin</strong>: DTO 검증 → 유저 조회 → bcrypt 비교 → JWT 발급 → UserDto + token 반환</li>
    <li><strong>이후 요청</strong>: Bearer token → JwtStrategy.validate() → request.user → @ValidateUser()</li>
  </ul>
</blockquote>

<blockquote>
  <p>[!abstract] 보안 설계 포인트</p>
  <ul>
    <li><code class="language-plaintext highlighter-rouge">@Public()</code> : Auth 엔드포인트만 전역 JWT 가드 우회</li>
    <li>ADMIN roleKey : 관리자 계정 무단 생성 방지</li>
    <li>bcrypt.compare : 상수 시간 비교로 타이밍 공격 방지</li>
    <li>동일 에러 메시지 : 계정 열거 공격 방지</li>
    <li>@Exclude() password : 응답에서 비밀번호 자동 제거</li>
  </ul>
</blockquote>

<blockquote>
  <p>[!question] 설명할 수 있어야 하는 것</p>
  <ol>
    <li>JWT Payload가 암호화가 아닌 이유와 담아도 되는 정보의 기준</li>
    <li>Stateless JWT의 한계 - 계정 정지가 즉시 반영되지 않는 이유</li>
    <li><code class="language-plaintext highlighter-rouge">bcrypt.compare</code>가 <code class="language-plaintext highlighter-rouge">===</code> 비교보다 안전한 이유</li>
    <li>로그인 실패 시 이메일/비밀번호 구분 없이 동일 메시지를 주는 이유</li>
    <li><code class="language-plaintext highlighter-rouge">@IsOptional()</code> + <code class="language-plaintext highlighter-rouge">@IsEnum(Role)</code> 조합이 어떻게 동작하는가</li>
    <li><code class="language-plaintext highlighter-rouge">JwtModule</code>을 exports에 포함한 이유</li>
  </ol>
</blockquote>]]></content><author><name>Paul2021-R</name></author><category term="문제해결" /><category term="Backend" /><category term="개발" /><category term="Protostar" /><category term="Project" /><category term="Review" /><category term="NestJS" /><category term="AI" /><summary type="html"><![CDATA[NestJS Auth 도메인 분석]]></summary></entry><entry><title type="html">Protostar review note - 09 - NestJS DB Layer (Prisma, PostgreSQL, pgvector, Redis)</title><link href="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/23/00-protostar-review.html" rel="alternate" type="text/html" title="Protostar review note - 09 - NestJS DB Layer (Prisma, PostgreSQL, pgvector, Redis)" /><published>2026-02-23T00:00:00+00:00</published><updated>2026-02-23T00:00:00+00:00</updated><id>http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/23/00-protostar-review</id><content type="html" xml:base="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/23/00-protostar-review.html"><![CDATA[<h1 id="nestjs-db-layer-분석">NestJS DB Layer 분석</h1>
<h2 id="prisma--postgresqlpgvector--redis">Prisma + PostgreSQL(pgvector) + Redis</h2>

<blockquote>
  <p>이 파일은 데이터 저장·조회에 관여하는 모든 파일을 대상으로 한다.</p>

  <table>
    <thead>
      <tr>
        <th>분류</th>
        <th>파일</th>
        <th>역할</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>ORM 설정</td>
        <td><code class="language-plaintext highlighter-rouge">prisma/schema.prisma</code></td>
        <td>스키마 선언, 모델 정의</td>
      </tr>
      <tr>
        <td>ORM 설정</td>
        <td><code class="language-plaintext highlighter-rouge">prisma/migrations/</code></td>
        <td>DDL 변경 이력</td>
      </tr>
      <tr>
        <td>ORM 서비스</td>
        <td><code class="language-plaintext highlighter-rouge">common/prisma/prisma.module.ts</code></td>
        <td>전역 모듈 등록</td>
      </tr>
      <tr>
        <td>ORM 서비스</td>
        <td><code class="language-plaintext highlighter-rouge">common/prisma/prisma.service.ts</code></td>
        <td>커넥션 풀 + 생명주기 관리</td>
      </tr>
      <tr>
        <td>Redis</td>
        <td><code class="language-plaintext highlighter-rouge">common/redis/redis.module.ts</code></td>
        <td>Redis 클라이언트 전역 공급</td>
      </tr>
      <tr>
        <td>도메인</td>
        <td><code class="language-plaintext highlighter-rouge">features/auth/auth.service.ts</code></td>
        <td>User CRUD + 인증</td>
      </tr>
      <tr>
        <td>도메인</td>
        <td><code class="language-plaintext highlighter-rouge">features/knowledge/knowledge.service.ts</code></td>
        <td>KnowledgeDoc CRUD</td>
      </tr>
      <tr>
        <td>도메인</td>
        <td><code class="language-plaintext highlighter-rouge">features/knowledge/ai-task.service.ts</code></td>
        <td>DB 폴링 → Redis 큐 디스패치</td>
      </tr>
      <tr>
        <td>응답 제어</td>
        <td><code class="language-plaintext highlighter-rouge">features/auth/dto/user.dto.ts</code></td>
        <td>DB 모델 → 응답 DTO 변환</td>
      </tr>
    </tbody>
  </table>
</blockquote>

<hr />

<h1 id="1-주요-개념">1. 주요 개념</h1>

<h2 id="데이터-저장소-구성">데이터 저장소 구성</h2>

<p>이 프로젝트는 두 개의 데이터 저장소를 역할에 따라 분리한다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PostgreSQL (영구 저장소)
  ├── users          - 사용자 계정, 인증 정보
  ├── knowledge_docs - 업로드된 원본 문서 메타데이터
  └── vectorized_docs - 문서 청크 + 임베딩 벡터 (pgvector)

Redis (휘발성 / 중간 계층)
  ├── chat:job:queue       - 채팅 작업 큐 (List)
  ├── chat:task:{jobId}    - 작업 페이로드 (String, TTL 300s)
  ├── chat:stream:{uuid}   - SSE 채널 (Pub/Sub)
  ├── ai:job:queue         - 문서 벡터화 큐 (List)
  ├── cluster:heartbeats   - AI 워커 생존 신호 (ZSet)
  └── throttler:*          - Rate Limit 카운터
</code></pre></div></div>

<h2 id="prisma-orm">Prisma ORM</h2>

<ul>
  <li>TypeScript 네이티브 ORM. <code class="language-plaintext highlighter-rouge">schema.prisma</code>에서 모델을 선언하면 자동으로 타입 안전한 클라이언트를 생성함.</li>
  <li><strong>Prisma Migrate</strong>: 스키마 변경 → SQL 마이그레이션 파일 자동 생성 → <code class="language-plaintext highlighter-rouge">prisma migrate deploy</code>로 적용</li>
  <li><strong>Prisma Client</strong>: 생성된 타입을 기반으로 <code class="language-plaintext highlighter-rouge">prisma.user.findUnique()</code> 같은 타입 안전 쿼리 제공</li>
</ul>

<h2 id="pgvector-확장">pgvector 확장</h2>

<p>PostgreSQL에 벡터 연산을 추가하는 확장. 별도 벡터 DB(Pinecone, Qdrant) 없이 <strong>RDBMS 안에서 임베딩 저장·유사도 검색</strong>이 가능하다.</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CREATE</span> <span class="n">EXTENSION</span> <span class="n">IF</span> <span class="k">NOT</span> <span class="k">EXISTS</span> <span class="nv">"vector"</span><span class="p">;</span>
<span class="c1">-- 1536차원 벡터 컬럼 (OpenAI text-embedding-3-small 출력 차원)</span>
<span class="nv">"embedding"</span> <span class="n">vector</span><span class="p">(</span><span class="mi">1536</span><span class="p">)</span>
<span class="c1">-- HNSW 인덱스: 근사 최근접 이웃(ANN) 검색</span>
<span class="k">USING</span> <span class="n">hnsw</span> <span class="p">(</span><span class="nv">"embedding"</span> <span class="n">vector_cosine_ops</span><span class="p">);</span>
</code></pre></div></div>

<h2 id="스키마-모델-구조">스키마 모델 구조</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User (1) ─────────────── (N) KnowledgeDoc
  │                              │
  │                              │ (1 to N, Cascade Delete)
  └── (1) ──────────── (N) VectorizedDoc
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>모델</th>
      <th>PK 타입</th>
      <th>주요 특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">User</code></td>
      <td>UUID (String)</td>
      <td>email unique, role enum(ADMIN/STARGAZER/PROTOSTAR)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">KnowledgeDoc</code></td>
      <td>UUID (String)</td>
      <td>status 상태머신, contentHash(SHA-256), version 증가</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">VectorizedDoc</code></td>
      <td>UUID (String)</td>
      <td><code class="language-plaintext highlighter-rouge">vector(1536)</code> 타입, chunkIndex, tokenCount</td>
    </tr>
  </tbody>
</table>

<h2 id="redis-모듈-redismodulets">Redis 모듈 (<code class="language-plaintext highlighter-rouge">redis.module.ts</code>)</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Global</span><span class="p">()</span>   <span class="c1">// 전역 모듈 - 모든 모듈에서 import 없이 주입 가능</span>
<span class="p">@</span><span class="nd">Module</span><span class="p">({</span>
  <span class="na">providers</span><span class="p">:</span> <span class="p">[{</span>
    <span class="na">provide</span><span class="p">:</span> <span class="nx">REDIS_CLIENT</span><span class="p">,</span>       <span class="c1">// 커스텀 토큰</span>
    <span class="na">useFactory</span><span class="p">:</span> <span class="k">async </span><span class="p">(</span><span class="nx">config</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="k">new</span> <span class="nc">Redis</span><span class="p">({</span>
      <span class="na">commandTimeout</span><span class="p">:</span> <span class="mi">5000</span><span class="p">,</span>      <span class="c1">// 크론 작업 무한 대기 방지 핵심 설정</span>
    <span class="p">}),</span>
  <span class="p">}],</span>
  <span class="na">exports</span><span class="p">:</span> <span class="p">[</span><span class="nx">REDIS_CLIENT</span><span class="p">],</span>
<span class="p">})</span>
</code></pre></div></div>

<hr />

<h1 id="2-핵심-로직-흐름">2. 핵심 로직 흐름</h1>

<h2 id="prismaservice-생명주기">PrismaService 생명주기</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">PrismaService</span> <span class="kd">extends</span> <span class="nc">PrismaClient</span> <span class="k">implements</span> <span class="nx">OnModuleInit</span><span class="p">,</span> <span class="nx">OnModuleDestroy</span> <span class="p">{</span>
  <span class="nf">constructor</span><span class="p">()</span> <span class="p">{</span>
    <span class="c1">// pg Pool 생성 → PrismaPg 어댑터 → PrismaClient에 주입</span>
    <span class="kd">const</span> <span class="nx">pool</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Pool</span><span class="p">({</span> <span class="na">connectionString</span><span class="p">:</span> <span class="nx">DATABASE_URL</span> <span class="p">});</span>
    <span class="kd">const</span> <span class="nx">adapter</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">PrismaPg</span><span class="p">(</span><span class="nx">pool</span><span class="p">);</span>
    <span class="k">super</span><span class="p">({</span> <span class="nx">adapter</span> <span class="p">});</span>   <span class="c1">// PrismaClient에 커넥션 풀 위임</span>
  <span class="p">}</span>

  <span class="k">async</span> <span class="nf">onModuleInit</span><span class="p">()</span> <span class="p">{</span>
    <span class="c1">// 1. URL에서 비밀번호 마스킹 (로그 보안)</span>
    <span class="c1">// 2. $connect() → SELECT 1 (헬스체크)</span>
    <span class="c1">// 3. 성공/실패 로그</span>
  <span class="p">}</span>

  <span class="k">async</span> <span class="nf">onModuleDestroy</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nf">$disconnect</span><span class="p">();</span>  <span class="c1">// Prisma 연결 종료</span>
    <span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">pool</span><span class="p">.</span><span class="nf">end</span><span class="p">();</span>     <span class="c1">// pg Pool 종료 (pool.end 누락 시 프로세스 행 발생)</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<blockquote>
  <p>[!note] 왜 <code class="language-plaintext highlighter-rouge">PrismaPg</code> 어댑터를 쓰는가?
Prisma 기본 모드는 자체 바이너리 엔진을 사용한다. <code class="language-plaintext highlighter-rouge">@prisma/adapter-pg</code>는 이를 <strong>Node.js pg 드라이버로 대체</strong>한다.
장점: 서버리스/엣지 환경 대응, 커넥션 풀(<code class="language-plaintext highlighter-rouge">pg.Pool</code>) 직접 제어 가능.
이 프로젝트에서는 pgvector 연동 안정성과 커넥션 풀 직접 관리를 위해 선택.</p>
</blockquote>

<h2 id="문서-업로드--벡터화-전체-흐름">문서 업로드 → 벡터화 전체 흐름</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Client]
  │
  ├─ POST /api/v1/upload/knowledge-docs
  │     │
  │     ├─ KnowledgeUploadBusyCheckInterceptor (큐 포화 확인)
  │     ├─ FilesInterceptor (Multer 파싱)
  │     └─ KnowledgeService.uploadFiles()
  │           ├─ 업로드 한도 확인 (prisma.knowledgeDoc.count)
  │           ├─ QueueService.add() → processSingleFileUpload()
  │           │     ├─ SHA-256 contentHash 계산
  │           │     ├─ MinIO에 파일 업로드
  │           │     └─ prisma.knowledgeDoc.create (status: UPLOADED)
  │           └─ 응답 반환
  │
[AiTaskService] @Cron (매 1분)
  │  prisma.knowledgeDoc.findMany({ status: UPLOADED, take: 10 })
  │  → redis.rpush('ai:job:queue', payload)
  │  → prisma.knowledgeDoc.update({ status: PROCESSING })
  │
[FastAPI 워커]
  │  ai:job:queue 소비 → 임베딩 생성 → VectorizedDoc INSERT
  │  → POST /api/v1/upload/knowledge-docs/webhook (완료 알림)
  │
[KnowledgeService.updateDocStatusViaWebhook()]
     prisma.knowledgeDoc.update({ status: COMPLETED, metaData: {...} })
</code></pre></div></div>

<h2 id="auth-흐름에서의-db-사용">Auth 흐름에서의 DB 사용</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// signup</span>
<span class="nx">prisma</span><span class="p">.</span><span class="nx">user</span><span class="p">.</span><span class="nf">findUnique</span><span class="p">({</span> <span class="na">where</span><span class="p">:</span> <span class="p">{</span> <span class="nx">email</span> <span class="p">}</span> <span class="p">})</span>  <span class="c1">// 중복 체크</span>
<span class="nx">prisma</span><span class="p">.</span><span class="nx">user</span><span class="p">.</span><span class="nf">create</span><span class="p">({</span> <span class="na">data</span><span class="p">:</span> <span class="p">{</span> <span class="nx">email</span><span class="p">,</span> <span class="na">password</span><span class="p">:</span> <span class="nx">bcrypt</span><span class="p">.</span><span class="nf">hash</span><span class="p">(...),</span> <span class="nx">role</span> <span class="p">}</span> <span class="p">})</span>

<span class="c1">// signin</span>
<span class="nx">prisma</span><span class="p">.</span><span class="nx">user</span><span class="p">.</span><span class="nf">findUnique</span><span class="p">({</span> <span class="na">where</span><span class="p">:</span> <span class="p">{</span> <span class="nx">email</span> <span class="p">}</span> <span class="p">})</span>  <span class="c1">// 유저 조회</span>
<span class="nx">bcrypt</span><span class="p">.</span><span class="nf">compare</span><span class="p">(</span><span class="nx">password</span><span class="p">,</span> <span class="nx">user</span><span class="p">.</span><span class="nx">password</span><span class="p">)</span>        <span class="c1">// 해시 비교</span>
<span class="nx">jwtService</span><span class="p">.</span><span class="nf">sign</span><span class="p">({</span> <span class="na">sub</span><span class="p">:</span> <span class="nx">user</span><span class="p">.</span><span class="nx">id</span><span class="p">,</span> <span class="nx">email</span><span class="p">,</span> <span class="nx">role</span> <span class="p">})</span> <span class="c1">// JWT 발급</span>
</code></pre></div></div>

<h2 id="docstatus-상태머신">DocStatus 상태머신</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>UPLOADED → PROCESSING → COMPLETED
                    └──→ FAILED
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>상태</th>
      <th>전환 주체</th>
      <th>시점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">UPLOADED</code></td>
      <td><code class="language-plaintext highlighter-rouge">KnowledgeService</code></td>
      <td>파일 업로드 완료 직후</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">PROCESSING</code></td>
      <td><code class="language-plaintext highlighter-rouge">AiTaskService</code> (크론)</td>
      <td>Redis 큐에 디스패치 시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">COMPLETED</code></td>
      <td>웹훅 (<code class="language-plaintext highlighter-rouge">updateDocStatusViaWebhook</code>)</td>
      <td>FastAPI 워커 처리 완료</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">FAILED</code></td>
      <td>웹훅 (<code class="language-plaintext highlighter-rouge">updateDocStatusViaWebhook</code>)</td>
      <td>FastAPI 워커 처리 실패</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>[!note] 마이그레이션과 스키마의 상태 불일치
<code class="language-plaintext highlighter-rouge">migration.sql</code>에 <code class="language-plaintext highlighter-rouge">DocStatus</code> 에 <code class="language-plaintext highlighter-rouge">QUEUED</code>가 있지만, 현재 <code class="language-plaintext highlighter-rouge">schema.prisma</code>에는 없다.
이후 마이그레이션에서 제거한 것으로 보이며, <code class="language-plaintext highlighter-rouge">AiTaskService</code>가 크론으로 <code class="language-plaintext highlighter-rouge">UPLOADED</code>를 직접 폴링하는 방식으로 QUEUED 상태를 대체한 설계.</p>
</blockquote>

<h2 id="userdto---db-모델--응답-변환">UserDto - DB 모델 → 응답 변환</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">export</span> <span class="kd">class</span> <span class="nc">UserDto</span> <span class="p">{</span>
  <span class="p">@</span><span class="nd">Expose</span><span class="p">()</span> <span class="nx">id</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
  <span class="p">@</span><span class="nd">Expose</span><span class="p">()</span> <span class="nx">email</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>
  <span class="p">@</span><span class="nd">Exclude</span><span class="p">()</span> <span class="nx">password</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>   <span class="c1">// 절대 외부 노출 금지</span>
  <span class="p">@</span><span class="nd">Expose</span><span class="p">()</span> <span class="nx">role</span><span class="p">:</span> <span class="nx">Role</span><span class="p">;</span>

  <span class="nf">constructor</span><span class="p">(</span><span class="nx">dto</span><span class="p">:</span> <span class="nx">User</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">this</span><span class="p">.</span><span class="nx">password</span> <span class="o">=</span> <span class="nx">dto</span><span class="p">.</span><span class="nx">password</span><span class="p">;</span>  <span class="c1">// 할당은 하되, @Exclude로 직렬화 시 제거</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ClassSerializerInterceptor</code>가 응답을 직렬화할 때 <code class="language-plaintext highlighter-rouge">@Exclude()</code> 필드를 자동으로 제거한다. <code class="language-plaintext highlighter-rouge">password</code>를 생성자에서 할당하는 이유는 TypeScript 컴파일러의 strict 체크를 통과하기 위함이며, 실제 응답에는 포함되지 않는다.</p>

<hr />

<h1 id="3-구조적-취약점--개선-방향">3. 구조적 취약점 / 개선 방향</h1>

<h2 id="aitaskservice-race-condition---직접-코드에-todo로-명시">[AiTaskService] Race Condition - 직접 코드에 TODO로 명시</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/**
 * TODO: 현재 상태는 데이터의 상태에 따라 race condition 발생 여지가 있음.
 * 동시성을 고려한 설계로 개선될 필요 있음.
 */</span>
<span class="p">@</span><span class="nd">Cron</span><span class="p">(</span><span class="nx">CronExpression</span><span class="p">.</span><span class="nx">EVERY_MINUTE</span><span class="p">)</span>
<span class="k">async</span> <span class="nf">dispatchPendingTasks</span><span class="p">()</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">targets</span> <span class="o">=</span> <span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">findMany</span><span class="p">({</span>
    <span class="na">where</span><span class="p">:</span> <span class="p">{</span> <span class="na">status</span><span class="p">:</span> <span class="nx">DocStatus</span><span class="p">.</span><span class="nx">UPLOADED</span> <span class="p">},</span>  <span class="c1">// 1. 조회</span>
    <span class="na">take</span><span class="p">:</span> <span class="mi">10</span><span class="p">,</span>
  <span class="p">});</span>
  <span class="c1">// ... 처리 후</span>
  <span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">update</span><span class="p">({</span>
    <span class="na">data</span><span class="p">:</span> <span class="p">{</span> <span class="na">status</span><span class="p">:</span> <span class="nx">DocStatus</span><span class="p">.</span><span class="nx">PROCESSING</span> <span class="p">},</span>  <span class="c1">// 2. 상태 변경</span>
  <span class="p">});</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>문제</strong>: <code class="language-plaintext highlighter-rouge">findMany</code> 이후 <code class="language-plaintext highlighter-rouge">update</code> 전 사이에 다른 인스턴스(수평 확장 시)가 동일 문서를 가져갈 수 있다. 결과: <strong>같은 문서가 중복 처리</strong>됨.</p>

<p><strong>개선 방향</strong>:</p>
<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- PostgreSQL의 SELECT FOR UPDATE SKIP LOCKED 사용</span>
<span class="c1">-- Prisma에서는 $queryRaw로 직접 실행</span>
<span class="k">SELECT</span> <span class="n">id</span> <span class="k">FROM</span> <span class="n">knowledge_docs</span>
<span class="k">WHERE</span> <span class="n">status</span> <span class="o">=</span> <span class="s1">'UPLOADED'</span>
<span class="k">ORDER</span> <span class="k">BY</span> <span class="n">created_at</span> <span class="k">ASC</span>
<span class="k">LIMIT</span> <span class="mi">10</span>
<span class="k">FOR</span> <span class="k">UPDATE</span> <span class="n">SKIP</span> <span class="n">LOCKED</span><span class="p">;</span>
</code></pre></div></div>
<p>또는 Redis 분산 락(Redlock 패턴)으로 크론 실행 자체를 단일화.</p>

<h2 id="aitaskservice-uploaded--processing-전환-실패-처리-부재">[AiTaskService] UPLOADED → PROCESSING 전환 실패 처리 부재</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for </span><span class="p">(</span><span class="kd">const</span> <span class="nx">doc</span> <span class="k">of</span> <span class="nx">targets</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">try</span> <span class="p">{</span>
    <span class="k">await</span> <span class="nx">redis</span><span class="p">.</span><span class="nf">rpush</span><span class="p">(</span><span class="dl">'</span><span class="s1">ai:job:queue</span><span class="dl">'</span><span class="p">,</span> <span class="nx">payload</span><span class="p">);</span>          <span class="c1">// Redis 성공</span>
    <span class="k">await</span> <span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">update</span><span class="p">({</span> <span class="na">status</span><span class="p">:</span> <span class="nx">PROCESSING</span> <span class="p">});</span> <span class="c1">// DB 실패?</span>
  <span class="p">}</span> <span class="k">catch </span><span class="p">(</span><span class="nx">error</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">this</span><span class="p">.</span><span class="nx">logger</span><span class="p">.</span><span class="nf">error</span><span class="p">(...);</span>  <span class="c1">// 로그만 남기고 계속 진행</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Redis에 푸시는 됐지만 DB 상태 업데이트가 실패하면 → 다음 크론 실행 때 같은 문서가 <strong>다시 UPLOADED로 조회되어 중복 디스패치</strong>된다. 트랜잭션 또는 보상 로직이 필요하다.</p>

<h2 id="knowledgeservice-파일-삭제와-db-삭제의-원자성-부재">[KnowledgeService] 파일 삭제와 DB 삭제의 원자성 부재</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="nf">deleteFile</span><span class="p">(</span><span class="nx">user</span><span class="p">,</span> <span class="nx">id</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">objectStorageService</span><span class="p">.</span><span class="nf">deleteFile</span><span class="p">(</span><span class="nx">doc</span><span class="p">.</span><span class="nx">minioKey</span><span class="p">);</span>  <span class="c1">// MinIO 삭제</span>
  <span class="c1">// ↑ 여기서 실패하면? 아래 DB 삭제가 안 됨</span>
  <span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="k">delete</span><span class="p">({</span> <span class="na">where</span><span class="p">:</span> <span class="p">{</span> <span class="nx">id</span> <span class="p">}</span> <span class="p">});</span>  <span class="c1">// DB 삭제</span>
<span class="p">}</span>
</code></pre></div></div>

<p>MinIO 삭제 성공 + DB 삭제 실패 → <strong>MinIO에는 없는데 DB에는 있는 좀비 레코드</strong> 발생 가능.
MinIO 삭제 실패 → <code class="language-plaintext highlighter-rouge">try/catch</code>로 무시하고 DB 삭제 진행 → <strong>DB는 삭제됐는데 MinIO에는 파일이 남는</strong> 고아 파일 발생.</p>

<p>현재 코드는 MinIO 삭제 실패를 <code class="language-plaintext highlighter-rouge">logger.error</code>로만 처리하고 DB 삭제는 계속 진행한다. 영구 저장소 기준으로 <strong>DB가 Source of Truth</strong>이므로 현재 방향은 맞지만, MinIO 고아 파일 정리 배치가 없다.</p>

<h2 id="schemaprisma-hnsw-인덱스-주석-처리">[schema.prisma] HNSW 인덱스 주석 처리</h2>

<pre><code class="language-prisma">// @@index([embedding], type: Hnsw) // pgvector indexing
</code></pre>

<p><code class="language-plaintext highlighter-rouge">migration.sql</code>에서 HNSW 인덱스를 생성했다가 두 번째 마이그레이션에서 DROP했다. 스키마에도 주석으로만 남아있다.</p>

<p><strong>영향</strong>: HNSW 인덱스 없이 코사인 유사도 검색 시 <strong>순차 스캔(Seq Scan)</strong> 발생 → 벡터 수가 많아질수록 성능 저하.</p>

<p><strong>이유 추정</strong>: HNSW 인덱스는 삽입 시 인덱스를 유지해야 하므로 쓰기 성능이 낮아진다. 데모 수준의 데이터량에서는 오버헤드가 더 클 수 있어 일시 제거한 것으로 보임. 프로덕션 데이터 증가 시 재활성화 필요.</p>

<h2 id="knowledgeservice-콘텐츠-중복-업로드-미검증">[KnowledgeService] 콘텐츠 중복 업로드 미검증</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="nf">processSingleFileUpload</span><span class="p">(</span><span class="nx">user</span><span class="p">,</span> <span class="nx">file</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">hash</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nf">calculateHash</span><span class="p">(</span><span class="nx">file</span><span class="p">.</span><span class="nx">buffer</span><span class="p">);</span>  <span class="c1">// SHA-256 계산</span>
  <span class="c1">// ... 해시를 DB에 저장하지만 중복 체크 쿼리가 없음</span>
  <span class="k">return</span> <span class="k">this</span><span class="p">.</span><span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">create</span><span class="p">({</span> <span class="na">data</span><span class="p">:</span> <span class="p">{</span> <span class="na">contentHash</span><span class="p">:</span> <span class="nx">hash</span><span class="p">,</span> <span class="p">...</span> <span class="p">}</span> <span class="p">});</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">contentHash</code>를 저장하지만 업로드 전 <strong>같은 해시가 이미 있는지 확인하지 않는다</strong>. 동일 파일을 여러 번 업로드해도 모두 저장된다.</p>

<h2 id="웹훅-실패-시-재시도-정책-부재-todo-명시">[웹훅] 실패 시 재시도 정책 부재 (TODO 명시)</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">update</span><span class="p">({</span>
  <span class="na">data</span><span class="p">:</span> <span class="p">{</span>
    <span class="nx">status</span><span class="p">,</span>   <span class="c1">// TODO: 실패 시 재요청 해야 하지 않을까?</span>
  <span class="p">}</span>
<span class="p">});</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">FAILED</code> 상태가 된 문서를 어떻게 재처리할지 정책이 없다. 수동 재업로드 외에 자동 재시도 메커니즘(Dead Letter Queue, 재처리 크론)이 없다.</p>

<hr />

<h1 id="4-핵심-메서드-및-라이브러리-함수-설명">4. 핵심 메서드 및 라이브러리 함수 설명</h1>

<h2 id="prisma-client-주요-메서드">Prisma Client 주요 메서드</h2>

<p>| 메서드 | 설명 | 예시 |
|—|—|—|
| <code class="language-plaintext highlighter-rouge">findUnique</code> | PK 또는 unique 필드로 단건 조회. 없으면 <code class="language-plaintext highlighter-rouge">null</code> | <code class="language-plaintext highlighter-rouge">prisma.user.findUnique({ where: { email } })</code> |
| <code class="language-plaintext highlighter-rouge">findMany</code> | 조건에 맞는 다수 조회. <code class="language-plaintext highlighter-rouge">take</code>, <code class="language-plaintext highlighter-rouge">orderBy</code>, <code class="language-plaintext highlighter-rouge">where</code> 활용 | <code class="language-plaintext highlighter-rouge">prisma.knowledgeDoc.findMany({ take: 10, orderBy: { createdAt: 'asc' } })</code> |
| <code class="language-plaintext highlighter-rouge">create</code> | 레코드 생성 | <code class="language-plaintext highlighter-rouge">prisma.knowledgeDoc.create({ data: { ... } })</code> |
| <code class="language-plaintext highlighter-rouge">update</code> | 조건에 맞는 레코드 수정. <code class="language-plaintext highlighter-rouge">{ increment: 1 }</code> 원자적 증가 가능 | <code class="language-plaintext highlighter-rouge">prisma.knowledgeDoc.update({ where: { id }, data: { version: { increment: 1 } } })</code> |
| <code class="language-plaintext highlighter-rouge">delete</code> | 단건 삭제 | <code class="language-plaintext highlighter-rouge">prisma.knowledgeDoc.delete({ where: { id } })</code> |
| <code class="language-plaintext highlighter-rouge">count</code> | 조건에 맞는 수 반환 | <code class="language-plaintext highlighter-rouge">prisma.knowledgeDoc.count({ where: { uploaderId } })</code> |
| <code class="language-plaintext highlighter-rouge">$connect</code> | 명시적 DB 연결 | <code class="language-plaintext highlighter-rouge">await this.$connect()</code> |
| <code class="language-plaintext highlighter-rouge">$disconnect</code> | 명시적 DB 연결 종료 | <code class="language-plaintext highlighter-rouge">await this.$disconnect()</code> |
| <code class="language-plaintext highlighter-rouge">$queryRaw</code> | 원시 SQL 실행 | <code class="language-plaintext highlighter-rouge">await this.$queryRaw\</code>SELECT 1# NestJS DB Layer 분석</p>
<h2 id="prisma--postgresqlpgvector--redis-1">Prisma + PostgreSQL(pgvector) + Redis</h2>

<blockquote>
  <p>이 파일은 데이터 저장·조회에 관여하는 모든 파일을 대상으로 한다.</p>

  <table>
    <thead>
      <tr>
        <th>분류</th>
        <th>파일</th>
        <th>역할</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>ORM 설정</td>
        <td>&lt;!JEKYLL@5140@0&gt;</td>
        <td>스키마 선언, 모델 정의</td>
      </tr>
      <tr>
        <td>ORM 설정</td>
        <td>&lt;!JEKYLL@5140@1&gt;</td>
        <td>DDL 변경 이력</td>
      </tr>
      <tr>
        <td>ORM 서비스</td>
        <td>&lt;!JEKYLL@5140@2&gt;</td>
        <td>전역 모듈 등록</td>
      </tr>
      <tr>
        <td>ORM 서비스</td>
        <td>&lt;!JEKYLL@5140@3&gt;</td>
        <td>커넥션 풀 + 생명주기 관리</td>
      </tr>
      <tr>
        <td>Redis</td>
        <td>&lt;!JEKYLL@5140@4&gt;</td>
        <td>Redis 클라이언트 전역 공급</td>
      </tr>
      <tr>
        <td>도메인</td>
        <td>&lt;!JEKYLL@5140@5&gt;</td>
        <td>User CRUD + 인증</td>
      </tr>
      <tr>
        <td>도메인</td>
        <td>&lt;!JEKYLL@5140@6&gt;</td>
        <td>KnowledgeDoc CRUD</td>
      </tr>
      <tr>
        <td>도메인</td>
        <td>&lt;!JEKYLL@5140@7&gt;</td>
        <td>DB 폴링 → Redis 큐 디스패치</td>
      </tr>
      <tr>
        <td>응답 제어</td>
        <td>&lt;!JEKYLL@5140@8&gt;</td>
        <td>DB 모델 → 응답 DTO 변환</td>
      </tr>
    </tbody>
  </table>
</blockquote>

<hr />

<h1 id="1-주요-개념-1">1. 주요 개념</h1>

<h2 id="데이터-저장소-구성-1">데이터 저장소 구성</h2>

<p>이 프로젝트는 두 개의 데이터 저장소를 역할에 따라 분리한다.</p>

<p>&lt;!JEKYLL@5140@9&gt;</p>

<h2 id="prisma-orm-1">Prisma ORM</h2>

<ul>
  <li>TypeScript 네이티브 ORM. &lt;!JEKYLL@5140@10&gt;에서 모델을 선언하면 자동으로 타입 안전한 클라이언트를 생성함.</li>
  <li><strong>Prisma Migrate</strong>: 스키마 변경 → SQL 마이그레이션 파일 자동 생성 → &lt;!JEKYLL@5140@11&gt;로 적용</li>
  <li><strong>Prisma Client</strong>: 생성된 타입을 기반으로 &lt;!JEKYLL@5140@12&gt; 같은 타입 안전 쿼리 제공</li>
</ul>

<h2 id="pgvector-확장-1">pgvector 확장</h2>

<p>PostgreSQL에 벡터 연산을 추가하는 확장. 별도 벡터 DB(Pinecone, Qdrant) 없이 <strong>RDBMS 안에서 임베딩 저장·유사도 검색</strong>이 가능하다.</p>

<p>&lt;!JEKYLL@5140@13&gt;</p>

<h2 id="스키마-모델-구조-1">스키마 모델 구조</h2>

<p>&lt;!JEKYLL@5140@14&gt;</p>

<table>
  <thead>
    <tr>
      <th>모델</th>
      <th>PK 타입</th>
      <th>주요 특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>&lt;!JEKYLL@5140@15&gt;</td>
      <td>UUID (String)</td>
      <td>email unique, role enum(ADMIN/STARGAZER/PROTOSTAR)</td>
    </tr>
    <tr>
      <td>&lt;!JEKYLL@5140@16&gt;</td>
      <td>UUID (String)</td>
      <td>status 상태머신, contentHash(SHA-256), version 증가</td>
    </tr>
    <tr>
      <td>&lt;!JEKYLL@5140@17&gt;</td>
      <td>UUID (String)</td>
      <td>&lt;!JEKYLL@5140@18&gt; 타입, chunkIndex, tokenCount</td>
    </tr>
  </tbody>
</table>

<h2 id="redis-모듈-jekyll514019">Redis 모듈 (&lt;!JEKYLL@5140@19&gt;)</h2>

<p>&lt;!JEKYLL@5140@20&gt;</p>

<hr />

<h1 id="2-핵심-로직-흐름-1">2. 핵심 로직 흐름</h1>

<h2 id="prismaservice-생명주기-1">PrismaService 생명주기</h2>

<p>&lt;!JEKYLL@5140@21&gt;</p>

<blockquote>
  <p>[!note] 왜 &lt;!JEKYLL@5140@22&gt; 어댑터를 쓰는가?
Prisma 기본 모드는 자체 바이너리 엔진을 사용한다. &lt;!JEKYLL@5140@23&gt;는 이를 <strong>Node.js pg 드라이버로 대체</strong>한다.
장점: 서버리스/엣지 환경 대응, 커넥션 풀(&lt;!JEKYLL@5140@24&gt;) 직접 제어 가능.
이 프로젝트에서는 pgvector 연동 안정성과 커넥션 풀 직접 관리를 위해 선택.</p>
</blockquote>

<h2 id="문서-업로드--벡터화-전체-흐름-1">문서 업로드 → 벡터화 전체 흐름</h2>

<p>&lt;!JEKYLL@5140@25&gt;</p>

<h2 id="auth-흐름에서의-db-사용-1">Auth 흐름에서의 DB 사용</h2>

<p>&lt;!JEKYLL@5140@26&gt;</p>

<h2 id="docstatus-상태머신-1">DocStatus 상태머신</h2>

<p>&lt;!JEKYLL@5140@27&gt;</p>

<table>
  <thead>
    <tr>
      <th>상태</th>
      <th>전환 주체</th>
      <th>시점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>&lt;!JEKYLL@5140@28&gt;</td>
      <td>&lt;!JEKYLL@5140@29&gt;</td>
      <td>파일 업로드 완료 직후</td>
    </tr>
    <tr>
      <td>&lt;!JEKYLL@5140@30&gt;</td>
      <td>&lt;!JEKYLL@5140@31&gt; (크론)</td>
      <td>Redis 큐에 디스패치 시</td>
    </tr>
    <tr>
      <td>&lt;!JEKYLL@5140@32&gt;</td>
      <td>웹훅 (&lt;!JEKYLL@5140@33&gt;)</td>
      <td>FastAPI 워커 처리 완료</td>
    </tr>
    <tr>
      <td>&lt;!JEKYLL@5140@34&gt;</td>
      <td>웹훅 (&lt;!JEKYLL@5140@35&gt;)</td>
      <td>FastAPI 워커 처리 실패</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>[!note] 마이그레이션과 스키마의 상태 불일치
&lt;!JEKYLL@5140@36&gt;에 &lt;!JEKYLL@5140@37&gt; 에 &lt;!JEKYLL@5140@38&gt;가 있지만, 현재 &lt;!JEKYLL@5140@39&gt;에는 없다.
이후 마이그레이션에서 제거한 것으로 보이며, &lt;!JEKYLL@5140@40&gt;가 크론으로 &lt;!JEKYLL@5140@41&gt;를 직접 폴링하는 방식으로 QUEUED 상태를 대체한 설계.</p>
</blockquote>

<h2 id="userdto---db-모델--응답-변환-1">UserDto - DB 모델 → 응답 변환</h2>

<p>&lt;!JEKYLL@5140@42&gt;</p>

<p>&lt;!JEKYLL@5140@43&gt;가 응답을 직렬화할 때 &lt;!JEKYLL@5140@44&gt; 필드를 자동으로 제거한다. &lt;!JEKYLL@5140@45&gt;를 생성자에서 할당하는 이유는 TypeScript 컴파일러의 strict 체크를 통과하기 위함이며, 실제 응답에는 포함되지 않는다.</p>

<hr />

<h1 id="3-구조적-취약점--개선-방향-1">3. 구조적 취약점 / 개선 방향</h1>

<h2 id="aitaskservice-race-condition---직접-코드에-todo로-명시-1">[AiTaskService] Race Condition - 직접 코드에 TODO로 명시</h2>

<p>&lt;!JEKYLL@5140@46&gt;</p>

<p><strong>문제</strong>: &lt;!JEKYLL@5140@47&gt; 이후 &lt;!JEKYLL@5140@48&gt; 전 사이에 다른 인스턴스(수평 확장 시)가 동일 문서를 가져갈 수 있다. 결과: <strong>같은 문서가 중복 처리</strong>됨.</p>

<p><strong>개선 방향</strong>:
&lt;!JEKYLL@5140@49&gt;
또는 Redis 분산 락(Redlock 패턴)으로 크론 실행 자체를 단일화.</p>

<h2 id="aitaskservice-uploaded--processing-전환-실패-처리-부재-1">[AiTaskService] UPLOADED → PROCESSING 전환 실패 처리 부재</h2>

<p>&lt;!JEKYLL@5140@50&gt;</p>

<p>Redis에 푸시는 됐지만 DB 상태 업데이트가 실패하면 → 다음 크론 실행 때 같은 문서가 <strong>다시 UPLOADED로 조회되어 중복 디스패치</strong>된다. 트랜잭션 또는 보상 로직이 필요하다.</p>

<h2 id="knowledgeservice-파일-삭제와-db-삭제의-원자성-부재-1">[KnowledgeService] 파일 삭제와 DB 삭제의 원자성 부재</h2>

<p>&lt;!JEKYLL@5140@51&gt;</p>

<p>MinIO 삭제 성공 + DB 삭제 실패 → <strong>MinIO에는 없는데 DB에는 있는 좀비 레코드</strong> 발생 가능.
MinIO 삭제 실패 → &lt;!JEKYLL@5140@52&gt;로 무시하고 DB 삭제 진행 → <strong>DB는 삭제됐는데 MinIO에는 파일이 남는</strong> 고아 파일 발생.</p>

<p>현재 코드는 MinIO 삭제 실패를 &lt;!JEKYLL@5140@53&gt;로만 처리하고 DB 삭제는 계속 진행한다. 영구 저장소 기준으로 <strong>DB가 Source of Truth</strong>이므로 현재 방향은 맞지만, MinIO 고아 파일 정리 배치가 없다.</p>

<h2 id="schemaprisma-hnsw-인덱스-주석-처리-1">[schema.prisma] HNSW 인덱스 주석 처리</h2>

<p>&lt;!JEKYLL@5140@54&gt;</p>

<p>&lt;!JEKYLL@5140@55&gt;에서 HNSW 인덱스를 생성했다가 두 번째 마이그레이션에서 DROP했다. 스키마에도 주석으로만 남아있다.</p>

<p><strong>영향</strong>: HNSW 인덱스 없이 코사인 유사도 검색 시 <strong>순차 스캔(Seq Scan)</strong> 발생 → 벡터 수가 많아질수록 성능 저하.</p>

<p><strong>이유 추정</strong>: HNSW 인덱스는 삽입 시 인덱스를 유지해야 하므로 쓰기 성능이 낮아진다. 데모 수준의 데이터량에서는 오버헤드가 더 클 수 있어 일시 제거한 것으로 보임. 프로덕션 데이터 증가 시 재활성화 필요.</p>

<h2 id="knowledgeservice-콘텐츠-중복-업로드-미검증-1">[KnowledgeService] 콘텐츠 중복 업로드 미검증</h2>

<p>&lt;!JEKYLL@5140@56&gt;</p>

<p>&lt;!JEKYLL@5140@57&gt;를 저장하지만 업로드 전 <strong>같은 해시가 이미 있는지 확인하지 않는다</strong>. 동일 파일을 여러 번 업로드해도 모두 저장된다.</p>

<h2 id="웹훅-실패-시-재시도-정책-부재-todo-명시-1">[웹훅] 실패 시 재시도 정책 부재 (TODO 명시)</h2>

<p>&lt;!JEKYLL@5140@58&gt;</p>

<p>&lt;!JEKYLL@5140@59&gt; 상태가 된 문서를 어떻게 재처리할지 정책이 없다. 수동 재업로드 외에 자동 재시도 메커니즘(Dead Letter Queue, 재처리 크론)이 없다.</p>

<hr />

<h1 id="4-핵심-메서드-및-라이브러리-함수-설명-1">4. 핵심 메서드 및 라이브러리 함수 설명</h1>

<h2 id="prisma-client-주요-메서드-1">Prisma Client 주요 메서드</h2>

<table>
  <thead>
    <tr>
      <th>메서드</th>
      <th>설명</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>&lt;!JEKYLL@5140@60&gt;</td>
      <td>PK 또는 unique 필드로 단건 조회. 없으면 &lt;!JEKYLL@5140@61&gt;</td>
      <td>&lt;!JEKYLL@5140@62&gt;</td>
    </tr>
    <tr>
      <td>&lt;!JEKYLL@5140@63&gt;</td>
      <td>조건에 맞는 다수 조회. &lt;!JEKYLL@5140@64&gt;, &lt;!JEKYLL@5140@65&gt;, &lt;!JEKYLL@5140@66&gt; 활용</td>
      <td>&lt;!JEKYLL@5140@67&gt;</td>
    </tr>
    <tr>
      <td>&lt;!JEKYLL@5140@68&gt;</td>
      <td>레코드 생성</td>
      <td>&lt;!JEKYLL@5140@69&gt;</td>
    </tr>
    <tr>
      <td>&lt;!JEKYLL@5140@70&gt;</td>
      <td>조건에 맞는 레코드 수정. &lt;!JEKYLL@5140@71&gt; 원자적 증가 가능</td>
      <td>&lt;!JEKYLL@5140@72&gt;</td>
    </tr>
    <tr>
      <td>&lt;!JEKYLL@5140@73&gt;</td>
      <td>단건 삭제</td>
      <td>&lt;!JEKYLL@5140@74&gt;</td>
    </tr>
    <tr>
      <td>&lt;!JEKYLL@5140@75&gt;</td>
      <td>조건에 맞는 수 반환</td>
      <td>&lt;!JEKYLL@5140@76&gt;</td>
    </tr>
    <tr>
      <td>&lt;!JEKYLL@5140@77&gt;</td>
      <td>명시적 DB 연결</td>
      <td>&lt;!JEKYLL@5140@78&gt;</td>
    </tr>
    <tr>
      <td>&lt;!JEKYLL@5140@79&gt;</td>
      <td>명시적 DB 연결 종료</td>
      <td>&lt;!JEKYLL@5140@80&gt;</td>
    </tr>
    <tr>
      <td>`</td>
      <td> </td>
      <td> </td>
    </tr>
  </tbody>
</table>

<h2 id="prismaadapter-pg--pgpool"><code class="language-plaintext highlighter-rouge">@prisma/adapter-pg</code> + <code class="language-plaintext highlighter-rouge">pg.Pool</code></h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">pool</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Pool</span><span class="p">({</span> <span class="na">connectionString</span><span class="p">:</span> <span class="nx">databaseUrl</span> <span class="p">});</span>
<span class="kd">const</span> <span class="nx">adapter</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">PrismaPg</span><span class="p">(</span><span class="nx">pool</span><span class="p">);</span>
<span class="k">super</span><span class="p">({</span> <span class="nx">adapter</span> <span class="p">});</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">pg.Pool</code>: Node.js PostgreSQL 드라이버의 커넥션 풀. 최대 동시 연결 수 관리.</li>
  <li><code class="language-plaintext highlighter-rouge">PrismaPg</code>: Prisma가 자체 엔진 대신 <code class="language-plaintext highlighter-rouge">pg</code> 드라이버를 사용하도록 하는 어댑터.</li>
  <li><code class="language-plaintext highlighter-rouge">onModuleDestroy</code>에서 <strong><code class="language-plaintext highlighter-rouge">pool.end\()</code>를 별도로 호출</strong>해야 한다. <code class="language-plaintext highlighter-rouge">\$disconnect\()</code>만으로는 <code class="language-plaintext highlighter-rouge">pg.Pool</code>이 닫히지 않아 프로세스가 정상 종료되지 않는다.</li>
</ul>

<h2 id="bcrypt"><code class="language-plaintext highlighter-rouge">bcrypt</code></h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 회원가입: 비밀번호 해싱</span>
<span class="kd">const</span> <span class="nx">hashedPassword</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">bcrypt</span><span class="p">.</span><span class="nf">hash</span><span class="p">(</span><span class="nx">password</span><span class="p">,</span> <span class="mi">10</span><span class="p">);</span>  <span class="c1">// saltRounds: 10</span>

<span class="c1">// 로그인: 해시 비교</span>
<span class="k">await</span> <span class="nx">bcrypt</span><span class="p">.</span><span class="nf">compare</span><span class="p">(</span><span class="nx">password</span><span class="p">,</span> <span class="nx">user</span><span class="p">.</span><span class="nx">password</span><span class="p">)</span>  <span class="c1">// boolean 반환</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">saltRounds: 10</code>: 해싱 비용. 2^10 = 1024번 반복. 높을수록 안전하지만 느림.</li>
  <li><code class="language-plaintext highlighter-rouge">bcrypt.compare</code>는 <strong>상수 시간 비교</strong>를 수행해 타이밍 공격을 방지한다. 일반 <code class="language-plaintext highlighter-rouge">===</code> 비교를 쓰면 안 된다.</li>
</ul>

<h2 id="cryptocreatehashsha256"><code class="language-plaintext highlighter-rouge">crypto.createHash\('sha256')</code></h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">private</span> <span class="nf">calculateHash</span><span class="p">(</span><span class="nx">buffer</span><span class="p">:</span> <span class="nx">Buffer</span><span class="p">):</span> <span class="kr">string</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">crypto</span><span class="p">.</span><span class="nf">createHash</span><span class="p">(</span><span class="dl">'</span><span class="s1">sha256</span><span class="dl">'</span><span class="p">).</span><span class="nf">update</span><span class="p">(</span><span class="nx">buffer</span><span class="p">).</span><span class="nf">digest</span><span class="p">(</span><span class="dl">'</span><span class="s1">hex</span><span class="dl">'</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>파일 내용의 SHA-256 해시를 <code class="language-plaintext highlighter-rouge">contentHash</code>로 저장. 향후 중복 파일 감지, 무결성 검증에 활용 가능하도록 설계된 필드.</p>

<h2 id="pgvector-관련-sql"><code class="language-plaintext highlighter-rouge">pgvector</code> 관련 SQL</h2>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- 확장 활성화</span>
<span class="k">CREATE</span> <span class="n">EXTENSION</span> <span class="n">IF</span> <span class="k">NOT</span> <span class="k">EXISTS</span> <span class="nv">"vector"</span><span class="p">;</span>

<span class="c1">-- 벡터 컬럼 정의</span>
<span class="nv">"embedding"</span> <span class="n">vector</span><span class="p">(</span><span class="mi">1536</span><span class="p">)</span>

<span class="c1">-- HNSW 인덱스 (코사인 유사도 기준)</span>
<span class="k">CREATE</span> <span class="k">INDEX</span> <span class="k">USING</span> <span class="n">hnsw</span> <span class="p">(</span><span class="nv">"embedding"</span> <span class="n">vector_cosine_ops</span><span class="p">);</span>

<span class="c1">-- 유사도 검색 (FastAPI 워커 측에서 실행)</span>
<span class="k">SELECT</span> <span class="o">*</span> <span class="k">FROM</span> <span class="n">vectorized_docs</span>
<span class="k">ORDER</span> <span class="k">BY</span> <span class="n">embedding</span> <span class="o">&lt;=&gt;</span> <span class="s1">'[0.1, 0.2, ...]'</span>  <span class="c1">-- cosine distance</span>
<span class="k">LIMIT</span> <span class="mi">5</span><span class="p">;</span>
</code></pre></div></div>

<h2 id="unsupportedvector1536-in-prisma-schema"><code class="language-plaintext highlighter-rouge">Unsupported\(\"vector\(1536)\")</code> in Prisma Schema</h2>

<pre><code class="language-prisma">embedding Unsupported("vector(1536)")?
</code></pre>

<p>Prisma가 공식적으로 <code class="language-plaintext highlighter-rouge">vector</code> 타입을 지원하지 않아 <code class="language-plaintext highlighter-rouge">Unsupported</code> 타입으로 선언. 이 필드는 Prisma Client에서 직접 쓰기/읽기가 불가능하고, <code class="language-plaintext highlighter-rouge">\$queryRaw</code>나 별도 클라이언트(FastAPI + psycopg2)를 통해서만 접근 가능하다.</p>

<h2 id="version--increment-1----원자적-증가"><code class="language-plaintext highlighter-rouge">version: { increment: 1 }</code> - 원자적 증가</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">update</span><span class="p">({</span>
  <span class="na">data</span><span class="p">:</span> <span class="p">{</span>
    <span class="na">version</span><span class="p">:</span> <span class="p">{</span> <span class="na">increment</span><span class="p">:</span> <span class="mi">1</span> <span class="p">},</span>  <span class="c1">// Prisma 원자적 연산</span>
  <span class="p">}</span>
<span class="p">})</span>
</code></pre></div></div>

<p>Prisma의 원자적 연산 문법. <code class="language-plaintext highlighter-rouge">version = version + 1</code>을 SQL 레벨에서 실행하므로 <strong>애플리케이션에서 현재 값을 읽어 +1 하는 방식보다 안전</strong>하다 (read-modify-write race 방지).</p>

<hr />

<h1 id="5-대체-가능한-라이브러리-및-트레이드오프">5. 대체 가능한 라이브러리 및 트레이드오프</h1>

<h2 id="orm-prisma-vs-대안">ORM: Prisma vs 대안</h2>

<table>
  <thead>
    <tr>
      <th>라이브러리</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Prisma</strong> (현재)</td>
      <td>타입 자동 생성, 스키마 중심 개발, 마이그레이션 자동화</td>
      <td>복잡한 쿼리 표현력 제한, <code class="language-plaintext highlighter-rouge">Unsupported</code> 타입 문제</td>
    </tr>
    <tr>
      <td><strong>TypeORM</strong></td>
      <td>NestJS와 오랜 통합 역사, 데코레이터 기반</td>
      <td>타입 안전성이 Prisma보다 약함, 설정 복잡</td>
    </tr>
    <tr>
      <td><strong>Drizzle ORM</strong></td>
      <td>최신 TS 친화적, SQL에 가까운 표현, 경량</td>
      <td>상대적으로 생태계 미성숙</td>
    </tr>
    <tr>
      <td><strong>Knex.js</strong></td>
      <td>SQL 쿼리 빌더, 유연성 최대</td>
      <td>타입 추론 없음, 보일러플레이트 많음</td>
    </tr>
    <tr>
      <td><strong>Raw SQL (pg)</strong></td>
      <td>최대 유연성, 최고 성능</td>
      <td>타입 안전성 없음, 마이그레이션 수동 관리</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>pgvector처럼 Prisma가 공식 지원하지 않는 타입을 많이 사용한다면 <strong>Drizzle ORM</strong> 또는 <strong>TypeORM + @Column({ type: ‘vector’ })</strong> 커스텀이 더 자연스럽다.</p>
</blockquote>

<h2 id="비밀번호-해싱-bcrypt-vs-대안">비밀번호 해싱: bcrypt vs 대안</h2>

<table>
  <thead>
    <tr>
      <th>라이브러리</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>bcrypt</strong> (현재)</td>
      <td>가장 검증된 방식, 광범위한 채택</td>
      <td>CPU 바운드, Node.js에서 비동기 처리 필요</td>
    </tr>
    <tr>
      <td><strong>Argon2</strong></td>
      <td>OWASP 권장, 메모리 하드 함수 (GPU 공격 저항)</td>
      <td>상대적으로 낮은 범용 채택률</td>
    </tr>
    <tr>
      <td><strong>scrypt</strong></td>
      <td>Node.js 내장 crypto 지원</td>
      <td>설정 복잡</td>
    </tr>
    <tr>
      <td><strong>PBKDF2</strong></td>
      <td>FIPS 인증, Node.js 내장</td>
      <td>bcrypt보다 GPU 공격에 취약</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>2024년 이후 OWASP는 <strong>Argon2id</strong>를 1순위로 권장한다. 신규 프로젝트라면 <code class="language-plaintext highlighter-rouge">argon2</code> 패키지 사용이 더 현대적.</p>
</blockquote>

<h2 id="벡터-저장소-pgvector-vs-전용-벡터-db">벡터 저장소: pgvector vs 전용 벡터 DB</h2>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>pgvector</strong> (현재)</td>
      <td>별도 인프라 없음, RDBMS와 동일 트랜잭션</td>
      <td>대규모에서 전용 DB 대비 성능 제한</td>
    </tr>
    <tr>
      <td><strong>Pinecone</strong></td>
      <td>완전 관리형, 대규모 ANN 검색 최적화</td>
      <td>비용, 외부 API 의존, 벤더 잠금</td>
    </tr>
    <tr>
      <td><strong>Qdrant</strong></td>
      <td>오픈소스, 고성능, 다양한 인덱스 타입</td>
      <td>별도 인프라 운영 필요</td>
    </tr>
    <tr>
      <td><strong>Weaviate</strong></td>
      <td>멀티모달 지원, GraphQL API</td>
      <td>복잡한 설정</td>
    </tr>
    <tr>
      <td><strong>Chroma</strong></td>
      <td>로컬 개발 친화적, Python 네이티브</td>
      <td>프로덕션 안정성 아직 검증 중</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>이 프로젝트 규모(1인 서비스, 수백~수천 개 벡터)에서는 <strong>pgvector</strong>가 적절한 선택. 문서 수만~수백만이 되면 Qdrant나 Pinecone 전환을 고려해야 한다.</p>
</blockquote>

<h2 id="redis-클라이언트-ioredis-vs-대안">Redis 클라이언트: ioredis vs 대안</h2>

<table>
  <thead>
    <tr>
      <th>라이브러리</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>ioredis</strong> (현재)</td>
      <td>Sentinel/Cluster 지원, Pub/Sub, 파이프라인</td>
      <td>설정이 다소 복잡</td>
    </tr>
    <tr>
      <td><strong>node-redis</strong></td>
      <td>Node.js 공식 Redis 클라이언트, Promise 네이티브</td>
      <td>ioredis보다 생태계 좁음</td>
    </tr>
    <tr>
      <td><strong>@nestjs/bull (BullMQ)</strong></td>
      <td>NestJS 전용 Redis 큐, 재시도/DLQ 내장</td>
      <td>큐 기능만 사용 시 과도한 추상화</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>이 프로젝트는 Redis를 큐 + Pub/Sub + Rate Limit Storage + 헬스체크 모두에 사용한다. <strong>BullMQ</strong>를 도입하면 큐 재시도, Dead Letter Queue 등을 프레임워크 수준에서 지원받을 수 있다.</p>
</blockquote>

<hr />

<h1 id="6-개발자로서-알아야-할-영역">6. 개발자로서 알아야 할 영역</h1>

<h2 id="a-커넥션-풀-connection-pool-이해">A. 커넥션 풀 (Connection Pool) 이해</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">pool</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Pool</span><span class="p">({</span> <span class="na">connectionString</span><span class="p">:</span> <span class="nx">databaseUrl</span> <span class="p">});</span>
<span class="c1">// 기본값: max 10개 커넥션</span>
</code></pre></div></div>

<ul>
  <li>커넥션 풀 없이 요청마다 DB 연결을 새로 맺으면 <strong>TCP 핸드셰이크 + DB 인증 오버헤드</strong>가 누적됨</li>
  <li>풀 크기를 너무 크게 설정하면 PostgreSQL 서버의 max_connections를 초과해 연결 거부 발생</li>
  <li><code class="language-plaintext highlighter-rouge">onModuleDestroy</code>에서 <strong><code class="language-plaintext highlighter-rouge">pool.end\()</code>까지 호출해야 프로세스가 깨끗하게 종료</strong>된다. 누락 시 Node.js 이벤트 루프가 열린 소켓 때문에 종료되지 않는다.</li>
</ul>

<h2 id="b-마이그레이션-전략">B. 마이그레이션 전략</h2>

<p>Prisma Migrate의 워크플로:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 개발 환경: 스키마 변경 후 마이그레이션 파일 생성</span>
prisma migrate dev <span class="nt">--name</span> <span class="s2">"add_chunk_index"</span>

<span class="c"># 프로덕션 환경: 생성된 마이그레이션 적용만</span>
prisma migrate deploy

<span class="c"># 현재 DB 상태 확인</span>
prisma migrate status
</code></pre></div></div>

<p><strong>이 프로젝트에서 확인된 마이그레이션 이력</strong>:</p>
<ol>
  <li><code class="language-plaintext highlighter-rouge">20260113083301\_init\_schema</code>: 초기 스키마 + HNSW 인덱스 생성</li>
  <li><code class="language-plaintext highlighter-rouge">20260113083332</code>: HNSW 인덱스 DROP (성능/쓰기 트레이드오프 재검토)</li>
</ol>

<p>→ 마이그레이션은 <strong>순방향만</strong> 지원. DROP INDEX도 새 마이그레이션 파일로 이력을 남기는 것이 Prisma의 방식.</p>

<h2 id="c-상태머신-설계와-데이터-일관성">C. 상태머신 설계와 데이터 일관성</h2>

<p><code class="language-plaintext highlighter-rouge">DocStatus</code> enum은 <strong>상태머신</strong>이다. 상태 전환에는 아래 원칙이 필요하다:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. 허용된 전환만 가능해야 한다
   COMPLETED → PROCESSING 같은 역행은 불가

2. 전환 실패 시 보상 트랜잭션이 있어야 한다
   Redis push 성공 + DB update 실패 → 중복 처리 위험

3. 터미널 상태(COMPLETED, FAILED)에서의 처리 정책
   FAILED → 재시도 정책이 있어야 한다
</code></pre></div></div>

<p>현재 코드에서 2, 3번이 TODO로 남아있다. 프로덕션 수준에서는 <strong>Outbox 패턴</strong> 또는 <strong>Two-Phase Commit</strong> 고려가 필요하다.</p>

<h2 id="d-n1-쿼리-문제-이해">D. N+1 쿼리 문제 이해</h2>

<p>Prisma에서 쉽게 발생하는 안티패턴:</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// ❌ N+1: 문서 10개를 가져온 후 각각 uploader를 조회</span>
<span class="kd">const</span> <span class="nx">docs</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">findMany</span><span class="p">({</span> <span class="na">take</span><span class="p">:</span> <span class="mi">10</span> <span class="p">});</span>
<span class="k">for </span><span class="p">(</span><span class="kd">const</span> <span class="nx">doc</span> <span class="k">of</span> <span class="nx">docs</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">user</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">prisma</span><span class="p">.</span><span class="nx">user</span><span class="p">.</span><span class="nf">findUnique</span><span class="p">({</span> <span class="na">where</span><span class="p">:</span> <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="nx">doc</span><span class="p">.</span><span class="nx">uploaderId</span> <span class="p">}</span> <span class="p">});</span>
<span class="p">}</span>

<span class="c1">// ✅ include로 한 번에 JOIN</span>
<span class="kd">const</span> <span class="nx">docs</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">findMany</span><span class="p">({</span>
  <span class="na">take</span><span class="p">:</span> <span class="mi">10</span><span class="p">,</span>
  <span class="na">include</span><span class="p">:</span> <span class="p">{</span> <span class="na">uploader</span><span class="p">:</span> <span class="kc">true</span> <span class="p">},</span>   <span class="c1">// LEFT JOIN users</span>
<span class="p">});</span>
</code></pre></div></div>

<p>이 프로젝트의 <code class="language-plaintext highlighter-rouge">findAll</code>, <code class="language-plaintext highlighter-rouge">uploadFiles</code> 등에서는 현재 단순 조회만 하고 있어 N+1 위험이 낮지만, 관계 데이터를 함께 조회할 경우 반드시 <code class="language-plaintext highlighter-rouge">include</code> 또는 <code class="language-plaintext highlighter-rouge">select</code>로 JOIN을 명시해야 한다.</p>

<h2 id="e-global-모듈-설계-원칙">E. <code class="language-plaintext highlighter-rouge">\$Global\()</code> 모듈 설계 원칙</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Global</span><span class="p">()</span>  <span class="c1">// PrismaModule, RedisModule 모두 Global 등록</span>
<span class="p">@</span><span class="nd">Module</span><span class="p">({</span> <span class="na">exports</span><span class="p">:</span> <span class="p">[</span><span class="nx">PrismaService</span><span class="p">]</span> <span class="p">})</span>
<span class="k">export</span> <span class="kd">class</span> <span class="nc">PrismaModule</span> <span class="p">{}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">@Global\()</code>로 등록하면 다른 모듈에서 <code class="language-plaintext highlighter-rouge">imports: \[PrismaModule]</code> 없이 <code class="language-plaintext highlighter-rouge">PrismaService</code>를 주입받을 수 있다.</li>
  <li><strong>남용 금지</strong>: 모든 모듈을 Global로 만들면 의존성 그래프가 불투명해진다.</li>
  <li><strong>적절한 사용</strong>: <code class="language-plaintext highlighter-rouge">PrismaService</code>, <code class="language-plaintext highlighter-rouge">RedisModule</code>처럼 앱 전체에서 필수로 사용하는 인프라성 모듈에만 적용.</li>
</ul>

<h2 id="f-소프트-삭제soft-delete-vs-하드-삭제hard-delete">F. 소프트 삭제(Soft Delete) vs 하드 삭제(Hard Delete)</h2>

<p>현재 코드는 <code class="language-plaintext highlighter-rouge">prisma.knowledgeDoc.delete\()</code>로 <strong>하드 삭제</strong>를 한다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 현재: 하드 삭제</span>
<span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="k">delete</span><span class="p">({</span> <span class="na">where</span><span class="p">:</span> <span class="p">{</span> <span class="nx">id</span> <span class="p">}</span> <span class="p">});</span>
</code></pre></div></div>

<p>프로덕션에서 고려할 소프트 삭제 패턴:</p>

<pre><code class="language-prisma">// schema.prisma에 삭제 시각 추가
model KnowledgeDoc {
  deletedAt DateTime? @map("deleted_at")
}
</code></pre>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 소프트 삭제</span>
<span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">update</span><span class="p">({</span>
  <span class="na">where</span><span class="p">:</span> <span class="p">{</span> <span class="nx">id</span> <span class="p">},</span>
  <span class="na">data</span><span class="p">:</span> <span class="p">{</span> <span class="na">deletedAt</span><span class="p">:</span> <span class="k">new</span> <span class="nc">Date</span><span class="p">()</span> <span class="p">},</span>
<span class="p">});</span>

<span class="c1">// 조회 시 필터</span>
<span class="nx">prisma</span><span class="p">.</span><span class="nx">knowledgeDoc</span><span class="p">.</span><span class="nf">findMany</span><span class="p">({</span> <span class="na">where</span><span class="p">:</span> <span class="p">{</span> <span class="na">deletedAt</span><span class="p">:</span> <span class="kc">null</span> <span class="p">}</span> <span class="p">});</span>
</code></pre></div></div>

<p><strong>VectorizedDoc</strong>은 <code class="language-plaintext highlighter-rouge">onDelete: Cascade</code>이므로 <code class="language-plaintext highlighter-rouge">KnowledgeDoc</code> 삭제 시 자동 연쇄 삭제된다. 소프트 삭제 도입 시 벡터 데이터 정리 정책도 함께 설계해야 한다.</p>

<h2 id="g-비밀번호-처리의-응용-레이어-통제">G. 비밀번호 처리의 응용 레이어 통제</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// UserDto</span>
<span class="p">@</span><span class="nd">Exclude</span><span class="p">()</span>
<span class="nx">password</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>   <span class="c1">// 응답 직렬화 시 ClassSerializerInterceptor가 제거</span>

<span class="nf">constructor</span><span class="p">(</span><span class="nx">dto</span><span class="p">:</span> <span class="nx">User</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">this</span><span class="p">.</span><span class="nx">password</span> <span class="o">=</span> <span class="nx">dto</span><span class="p">.</span><span class="nx">password</span><span class="p">;</span>  <span class="c1">// 내부적으로는 할당 (TS strict 통과용)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>DB에서 조회한 Prisma 모델(<code class="language-plaintext highlighter-rouge">User</code>)에는 <code class="language-plaintext highlighter-rouge">password</code>가 포함된다. 이를 응답에 포함시키지 않으려면:</p>
<ol>
  <li><code class="language-plaintext highlighter-rouge">select</code> 쿼리에서 제외: <code class="language-plaintext highlighter-rouge">prisma.user.findUnique\({ select: { password: false } })</code></li>
  <li>DTO + <code class="language-plaintext highlighter-rouge">@Exclude\()</code>: 현재 방식. 쿼리는 가져오되 직렬화 시 제거.</li>
</ol>

<p>방식 1이 DB 레벨에서 원천 차단이지만, 방식 2는 응용 레이어에서 통제하는 NestJS 표준 패턴이다.</p>

<hr />

<h1 id="핵심-요약-카드">핵심 요약 카드</h1>

<blockquote>
  <p>[!abstract] 데이터 저장소 역할 분리</p>
  <ul>
    <li><strong>PostgreSQL</strong>: 영구 데이터 (유저, 문서 메타데이터, 벡터)</li>
    <li><strong>Redis</strong>: 임시·중간 계층 (작업 큐, SSE 채널, Rate Limit 카운터, AI 생존 신호)</li>
    <li><strong>MinIO</strong>: 원본 파일 바이너리 (오브젝트 스토리지)</li>
  </ul>
</blockquote>

<blockquote>
  <p>[!abstract] 핵심 설계 패턴</p>
  <ul>
    <li><code class="language-plaintext highlighter-rouge">PrismaService</code> → <code class="language-plaintext highlighter-rouge">extends PrismaClient</code> + <code class="language-plaintext highlighter-rouge">OnModuleInit/Destroy</code> → 커넥션 생명주기 관리</li>
    <li><code class="language-plaintext highlighter-rouge">DocStatus</code> 상태머신 → 크론 폴링 + 웹훅 콜백으로 분산 처리 상태 동기화</li>
    <li><code class="language-plaintext highlighter-rouge">@Global\()</code> 모듈 → 인프라 서비스의 전역 공급</li>
    <li><code class="language-plaintext highlighter-rouge">UserDto</code> + <code class="language-plaintext highlighter-rouge">@Exclude\()</code> → 응용 레이어에서 민감 데이터 통제</li>
  </ul>
</blockquote>

<blockquote>
  <p>[!question] 설명할 수 있어야 하는 것</p>
  <ol>
    <li><code class="language-plaintext highlighter-rouge">PrismaPg</code> 어댑터를 쓰는 이유와 <code class="language-plaintext highlighter-rouge">pool.end\()</code>를 별도 호출해야 하는 이유</li>
    <li><code class="language-plaintext highlighter-rouge">AiTaskService</code>에 Race Condition이 발생할 수 있는 시나리오와 해결책</li>
    <li>HNSW 인덱스가 없을 때 벡터 검색 성능에 어떤 영향이 있는가</li>
    <li><code class="language-plaintext highlighter-rouge">bcrypt.compare</code>가 일반 <code class="language-plaintext highlighter-rouge">===</code> 비교보다 안전한 이유</li>
    <li><code class="language-plaintext highlighter-rouge">version: { increment: 1 }</code>을 read-modify-write 대신 쓰는 이유</li>
    <li><code class="language-plaintext highlighter-rouge">@Exclude\()</code> 필드가 생성자에서 할당되지만 응답에 노출되지 않는 원리</li>
  </ol>
</blockquote>]]></content><author><name>Paul2021-R</name></author><category term="문제해결" /><category term="Backend" /><category term="개발" /><category term="Protostar" /><category term="Project" /><category term="Review" /><category term="NestJS" /><category term="AI" /><summary type="html"><![CDATA[NestJS DB Layer 분석 Prisma + PostgreSQL(pgvector) + Redis]]></summary></entry><entry><title type="html">Protostar review note - 08 NestJS Middlewares &amp;amp; Guards &amp;amp; Interceptors &amp;amp; Pipes</title><link href="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/19/00-protostar-review.html" rel="alternate" type="text/html" title="Protostar review note - 08 NestJS Middlewares &amp;amp; Guards &amp;amp; Interceptors &amp;amp; Pipes" /><published>2026-02-19T00:00:00+00:00</published><updated>2026-02-19T00:00:00+00:00</updated><id>http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/19/00-protostar-review</id><content type="html" xml:base="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/19/00-protostar-review.html"><![CDATA[<h1 id="nestjs-파이프라인-구성요소-분석">NestJS 파이프라인 구성요소 분석</h1>
<h2 id="middleware--guards--interceptors--pipes">Middleware → Guards → Interceptors → Pipes</h2>

<blockquote>
  <p>이 파일은 요청이 컨트롤러에 도달하기 전까지의 처리 계층을 <strong>실행 순서</strong>대로 분석한다.
프로젝트에 존재하는 구성 파일 목록:</p>

  <table>
    <thead>
      <tr>
        <th>분류</th>
        <th>파일</th>
        <th>등록 방식</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Middleware</td>
        <td>_(별도 클래스 없음, Express 직접 사용)_</td>
        <td><code class="language-plaintext highlighter-rouge">app.use()</code> in main.ts</td>
      </tr>
      <tr>
        <td>Guard</td>
        <td><code class="language-plaintext highlighter-rouge">origin.guard.ts</code></td>
        <td><code class="language-plaintext highlighter-rouge">useGlobalGuards()</code> in main.ts</td>
      </tr>
      <tr>
        <td>Guard</td>
        <td><code class="language-plaintext highlighter-rouge">jwt-auth.guard.ts</code></td>
        <td><code class="language-plaintext highlighter-rouge">APP_GUARD</code> in AppModule</td>
      </tr>
      <tr>
        <td>Guard</td>
        <td><code class="language-plaintext highlighter-rouge">ai-circuit.guard.ts</code></td>
        <td><code class="language-plaintext highlighter-rouge">@UseGuards()</code> in Controller</td>
      </tr>
      <tr>
        <td>Interceptor</td>
        <td><code class="language-plaintext highlighter-rouge">logging.interceptor.ts</code></td>
        <td><code class="language-plaintext highlighter-rouge">useGlobalInterceptors()</code> in main.ts</td>
      </tr>
      <tr>
        <td>Interceptor</td>
        <td><code class="language-plaintext highlighter-rouge">knowledge-upload-busy-check.interceptor.ts</code></td>
        <td><code class="language-plaintext highlighter-rouge">@UseInterceptors()</code> in Controller</td>
      </tr>
      <tr>
        <td>Pipe</td>
        <td><code class="language-plaintext highlighter-rouge">ValidationPipe</code></td>
        <td><code class="language-plaintext highlighter-rouge">useGlobalPipes()</code> in main.ts</td>
      </tr>
      <tr>
        <td>Pipe</td>
        <td><code class="language-plaintext highlighter-rouge">ClassSerializerInterceptor</code></td>
        <td><code class="language-plaintext highlighter-rouge">useGlobalInterceptors()</code> in main.ts</td>
      </tr>
    </tbody>
  </table>
</blockquote>

<hr />

<h1 id="1-주요-개념">1. 주요 개념</h1>

<h2 id="middleware">Middleware</h2>

<p>NestJS의 Middleware는 <strong>Express/Fastify 레벨</strong>에서 동작한다. 라우터보다 먼저 실행되며, 요청·응답 객체에 직접 접근할 수 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Request] → Middleware → Guard → Interceptor → Pipe → Controller
</code></pre></div></div>

<p><strong>이 프로젝트에서의 선택</strong>: 별도의 NestJS Middleware 클래스 없이 <code class="language-plaintext highlighter-rouge">app.use()</code>로 Express 미들웨어를 직접 등록.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// main.ts</span>
<span class="nx">app</span><span class="p">.</span><span class="nf">use</span><span class="p">(</span><span class="nf">json</span><span class="p">({</span> <span class="na">limit</span><span class="p">:</span> <span class="dl">'</span><span class="s1">50mb</span><span class="dl">'</span> <span class="p">}));</span>
<span class="nx">app</span><span class="p">.</span><span class="nf">use</span><span class="p">(</span><span class="nf">urlencoded</span><span class="p">({</span> <span class="na">extended</span><span class="p">:</span> <span class="kc">true</span><span class="p">,</span> <span class="na">limit</span><span class="p">:</span> <span class="dl">'</span><span class="s1">50mb</span><span class="dl">'</span> <span class="p">}));</span>
</code></pre></div></div>

<p>→ bodyParser만 미들웨어 레이어에서 처리하고, 인증·Origin 검사 등은 Guard 레이어로 위임한 설계로 구성되어있다.</p>

<h2 id="guards">Guards</h2>

<table>
  <thead>
    <tr>
      <th>가드</th>
      <th>등록 방식</th>
      <th>적용 범위</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">OriginGuard</code></td>
      <td><code class="language-plaintext highlighter-rouge">useGlobalGuards()</code></td>
      <td>전체 요청</td>
      <td>HTTP Origin 화이트리스트 검사</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ThrottlerGuard</code></td>
      <td><code class="language-plaintext highlighter-rouge">APP_GUARD</code></td>
      <td>전체 요청</td>
      <td>Rate Limit (burst/sustained)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">JwtAuthGuard</code></td>
      <td><code class="language-plaintext highlighter-rouge">APP_GUARD</code></td>
      <td>전체 요청 (<code class="language-plaintext highlighter-rouge">@Public()</code> 제외)</td>
      <td>JWT 토큰 인증</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">AiCircuitGuard</code></td>
      <td><code class="language-plaintext highlighter-rouge">@UseGuards()</code></td>
      <td>채팅·지식 엔드포인트</td>
      <td>AI 서비스 생존 여부 확인</td>
    </tr>
  </tbody>
</table>

<p>NestJS Guard의 핵심: <code class="language-plaintext highlighter-rouge">canActivate(context: ExecutionContext): boolean | Promise&lt;boolean&gt; | Observable&lt;boolean&gt;</code>
→ <code class="language-plaintext highlighter-rouge">false</code> 또는 예외를 던지면 요청을 차단, <code class="language-plaintext highlighter-rouge">true</code>를 반환해야 다음 단계 진행이 된다.</p>

<h2 id="interceptors">Interceptors</h2>

<table>
  <thead>
    <tr>
      <th>인터셉터</th>
      <th>등록 방식</th>
      <th>적용 범위</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">LoggingInterceptor</code></td>
      <td><code class="language-plaintext highlighter-rouge">useGlobalInterceptors()</code></td>
      <td>전체 요청</td>
      <td>HTTP 트래픽 구조화 로깅</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ClassSerializerInterceptor</code></td>
      <td><code class="language-plaintext highlighter-rouge">useGlobalInterceptors()</code></td>
      <td>전체 응답</td>
      <td><code class="language-plaintext highlighter-rouge">@Exclude()/@Expose()</code> 직렬화 적용</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">KnowledgeUploadBusyCheckInterceptor</code></td>
      <td><code class="language-plaintext highlighter-rouge">@UseInterceptors()</code></td>
      <td>업로드 엔드포인트</td>
      <td>큐 포화 상태 사전 차단</td>
    </tr>
  </tbody>
</table>

<p>NestJS Interceptor의 핵심: <code class="language-plaintext highlighter-rouge">intercept(context, next: CallHandler): Observable&lt;any&gt;</code>
→ <code class="language-plaintext highlighter-rouge">next.handle()</code> 호출 전: 요청 전처리 / 호출 후 <code class="language-plaintext highlighter-rouge">pipe()</code>: 응답 후처리.</p>

<h2 id="pipes">Pipes</h2>

<table>
  <thead>
    <tr>
      <th>파이프</th>
      <th>등록 방식</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ValidationPipe</code></td>
      <td><code class="language-plaintext highlighter-rouge">useGlobalPipes()</code></td>
      <td>DTO 필드 검증 + 타입 변환</td>
    </tr>
    <tr>
      <td>_(ClassSerializerInterceptor)_</td>
      <td><code class="language-plaintext highlighter-rouge">useGlobalInterceptors()</code></td>
      <td>응답 직렬화 (Pipe는 아니지만 유사 역할)</td>
    </tr>
  </tbody>
</table>

<p>파이프는 Guards·Interceptors 이후, Controller 직전에 실행된다. 인증된 요청의 데이터만 검증하는 것이 목적으로 존재한다.</p>

<hr />

<h1 id="2-핵심-로직-흐름">2. 핵심 로직 흐름</h1>

<h2 id="전체-요청-처리-흐름-실행-순서">전체 요청 처리 흐름 (실행 순서)</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[HTTP Request]
      │
      ▼
 app.use(json/urlencoded)          ← Express Middleware (bodyParser)
      │
      ▼
 OriginGuard                       ← 전역 가드 1 (main.ts useGlobalGuards)
  - origin 헤더 없음 → 통과
  - 화이트리스트 포함 → 통과
  - 그 외 → ForbiddenException
      │
      ▼
 ThrottlerGuard                    ← 전역 가드 2 (APP_GUARD, AppModule)
  - burst: 초당 2000 req
  - sustained: 분당 60000 req
  - Redis에 카운터 저장
      │
      ▼
 JwtAuthGuard                      ← 전역 가드 3 (APP_GUARD, AppModule)
  - @Public() → 바이패스
  - Reflector로 메타데이터 확인
  - JWT 유효 → request.user 세팅
  - JWT 무효 → UnauthorizedException
      │
      ▼
 [AiCircuitGuard]                  ← 라우트 가드 (@UseGuards, 해당 엔드포인트만)
  - Redis heartbeat 확인
  - AI ONLINE → 통과
  - AI OFFLINE → ServiceUnavailableException
      │
      ▼
 LoggingInterceptor                ← 전역 인터셉터 (요청 시각 기록)
      │
      ▼
 [KnowledgeUploadBusyCheckInterceptor] ← 라우트 인터셉터 (업로드 엔드포인트만)
  - QueueService.isBusy() 확인
  - 큐 여유 → 통과
  - 큐 포화 → ServiceUnavailableException
      │
      ▼
 ValidationPipe                    ← 전역 파이프
  - DTO 인스턴스로 변환 (transform: true)
  - 미선언 필드 제거 (whitelist: true)
  - 검증 실패 → BadRequestException
      │
      ▼
 Controller → Service              ← 비즈니스 로직
      │
      ▼
 LoggingInterceptor (tap)          ← 응답 후처리 (응답 시간 측정·로깅)
 ClassSerializerInterceptor        ← @Exclude/@Expose 직렬화
      │
      ▼
 GlobalExceptionFilter             ← 예외 발생 시 일괄 처리
      │
      ▼
 [HTTP Response]
</code></pre></div></div>

<h2 id="originguard-흐름">OriginGuard 흐름</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">canActivate</span><span class="p">(</span><span class="nx">context</span><span class="p">:</span> <span class="nx">ExecutionContext</span><span class="p">):</span> <span class="nx">boolean</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">origin</span> <span class="o">=</span> <span class="nx">request</span><span class="p">.</span><span class="nx">headers</span><span class="p">.</span><span class="nx">origin</span><span class="p">;</span>

  <span class="k">if </span><span class="p">(</span><span class="o">!</span><span class="nx">origin</span><span class="p">)</span> <span class="k">return</span> <span class="kc">true</span><span class="p">;</span>          <span class="c1">// Server-to-Server, Postman 등 → 통과</span>
  <span class="k">if </span><span class="p">(</span><span class="nx">whitelist</span><span class="p">.</span><span class="nf">includes</span><span class="p">(</span><span class="nx">origin</span><span class="p">))</span> <span class="k">return</span> <span class="kc">true</span><span class="p">;</span>
  <span class="k">throw</span> <span class="k">new</span> <span class="nc">ForbiddenException</span><span class="p">();</span>    <span class="c1">// 미등록 브라우저 origin → 403</span>
<span class="p">}</span>
</code></pre></div></div>

<blockquote>
  <p>[!note] <code class="language-plaintext highlighter-rouge">origin</code> 헤더가 없는 경우 무조건 통과하는 이유
<code class="language-plaintext highlighter-rouge">origin</code> 헤더는 <strong>브라우저가 보내는 CORS 헤더</strong>다.
서버 간 통신(FastAPI 워커 웹훅), Postman, curl 등에는 origin이 없다.
이 엔드포인트들을 차단하지 않으려면 <code class="language-plaintext highlighter-rouge">!origin</code> 일 때 통과가 맞다.
단, 이 경우 웹훅 엔드포인트는 별도의 시크릿 키 검증으로 보완하고 있다.</p>
</blockquote>

<h2 id="jwtauthguard-흐름">JwtAuthGuard 흐름</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">canActivate</span><span class="p">(</span><span class="nx">context</span><span class="p">:</span> <span class="nx">ExecutionContext</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">isPublic</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">reflector</span><span class="p">.</span><span class="nx">getAllAndOverride</span><span class="o">&lt;</span><span class="nx">boolean</span><span class="o">&gt;</span><span class="p">(</span><span class="nx">IS_PUBLIC_KEY</span><span class="p">,</span> <span class="p">[</span>
    <span class="nx">context</span><span class="p">.</span><span class="nf">getHandler</span><span class="p">(),</span>   <span class="c1">// 메서드 레벨 메타데이터 우선</span>
    <span class="nx">context</span><span class="p">.</span><span class="nf">getClass</span><span class="p">(),</span>     <span class="c1">// 클래스 레벨 메타데이터 차선</span>
  <span class="p">]);</span>
  <span class="k">if </span><span class="p">(</span><span class="nx">isPublic</span><span class="p">)</span> <span class="k">return</span> <span class="kc">true</span><span class="p">;</span>

  <span class="k">return</span> <span class="k">super</span><span class="p">.</span><span class="nf">canActivate</span><span class="p">(</span><span class="nx">context</span><span class="p">);</span>  <span class="c1">// Passport jwt 전략 실행</span>
  <span class="c1">// → JwtStrategy.validate() → request.user 세팅</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="logginginterceptor-흐름">LoggingInterceptor 흐름</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">intercept</span><span class="p">(</span><span class="nx">context</span><span class="p">,</span> <span class="nx">next</span><span class="p">:</span> <span class="nx">CallHandler</span><span class="p">):</span> <span class="nx">Observable</span><span class="o">&lt;</span><span class="kr">any</span><span class="o">&gt;</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">now</span> <span class="o">=</span> <span class="nb">Date</span><span class="p">.</span><span class="nf">now</span><span class="p">();</span>                <span class="c1">// 요청 진입 시각 기록</span>

  <span class="k">return</span> <span class="nx">next</span><span class="p">.</span><span class="nf">handle</span><span class="p">().</span><span class="nf">pipe</span><span class="p">(</span>
    <span class="nf">tap</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>                          <span class="c1">// 응답 완료 후 실행</span>
      <span class="kd">const</span> <span class="nx">duration</span> <span class="o">=</span> <span class="nb">Date</span><span class="p">.</span><span class="nf">now</span><span class="p">()</span> <span class="o">-</span> <span class="nx">now</span><span class="p">;</span>
      <span class="c1">// 4xx 이상은 로깅 안 함 (GlobalExceptionFilter에서 처리)</span>
      <span class="k">if </span><span class="p">(</span><span class="nx">statusCode</span> <span class="o">&lt;</span> <span class="mi">400</span><span class="p">)</span> <span class="k">this</span><span class="p">.</span><span class="nx">logger</span><span class="p">.</span><span class="nf">log</span><span class="p">({</span> <span class="na">type</span><span class="p">:</span> <span class="dl">'</span><span class="s1">http_traffic_protostar</span><span class="dl">'</span><span class="p">,</span> <span class="nx">duration_ms</span><span class="p">,</span> <span class="p">...</span> <span class="p">});</span>
    <span class="p">})</span>
  <span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="knowledgeuploadbusycheckinterceptor-흐름">KnowledgeUploadBusyCheckInterceptor 흐름</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">intercept</span><span class="p">(</span><span class="nx">context</span><span class="p">,</span> <span class="nx">next</span><span class="p">:</span> <span class="nx">CallHandler</span><span class="p">):</span> <span class="nx">Observable</span><span class="o">&lt;</span><span class="kr">any</span><span class="o">&gt;</span> <span class="p">{</span>
  <span class="k">if </span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">queueService</span><span class="p">.</span><span class="nf">isBusy</span><span class="p">())</span> <span class="p">{</span>
    <span class="c1">// 큐가 꽉 찼으면 next.handle() 자체를 호출하지 않음</span>
    <span class="k">throw</span> <span class="k">new</span> <span class="nc">ServiceUnavailableException</span><span class="p">({</span> <span class="na">data</span><span class="p">:</span> <span class="k">this</span><span class="p">.</span><span class="nx">queueService</span><span class="p">.</span><span class="nf">getStatus</span><span class="p">()</span> <span class="p">});</span>
  <span class="p">}</span>
  <span class="k">return</span> <span class="nx">next</span><span class="p">.</span><span class="nf">handle</span><span class="p">();</span>   <span class="c1">// 큐 여유 있으면 그냥 통과</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="validationpipe-흐름">ValidationPipe 흐름</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// main.ts</span>
<span class="k">new</span> <span class="nc">ValidationPipe</span><span class="p">({</span>
  <span class="na">transform</span><span class="p">:</span> <span class="kc">true</span><span class="p">,</span>    <span class="c1">// JSON string → DTO 클래스 인스턴스</span>
  <span class="na">whitelist</span><span class="p">:</span> <span class="kc">true</span><span class="p">,</span>    <span class="c1">// DTO에 없는 필드 자동 제거</span>
<span class="p">})</span>

<span class="c1">// 예시 DTO</span>
<span class="kd">class</span> <span class="nc">CreateChatDto</span> <span class="p">{</span>
  <span class="p">@</span><span class="nd">IsString</span><span class="p">()</span> <span class="p">@</span><span class="nd">IsNotEmpty</span><span class="p">()</span>
  <span class="nx">content</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>

  <span class="p">@</span><span class="nd">IsString</span><span class="p">()</span>
  <span class="nx">sessionId</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>

  <span class="p">@</span><span class="nd">IsOptional</span><span class="p">()</span>
  <span class="nx">context</span><span class="p">?:</span> <span class="kr">string</span><span class="p">;</span>
<span class="p">}</span>
<span class="c1">// content, sessionId, context 외 필드는 자동 제거</span>
<span class="c1">// content가 없으면 BadRequestException</span>
</code></pre></div></div>

<hr />

<h1 id="3-구조적-취약점--개선-방향">3. 구조적 취약점 / 개선 방향</h1>

<h2 id="originguard-origin-없는-요청을-무조건-통과">[OriginGuard] <code class="language-plaintext highlighter-rouge">origin</code> 없는 요청을 무조건 통과</h2>

<p>서버 내부 통신, curl, Postman을 허용하기 위한 의도적 설계이지만, <strong>서버가 인터넷에 직접 노출된 경우</strong> <code class="language-plaintext highlighter-rouge">origin</code> 헤더를 생략한 악의적 요청도 통과될 수 있게 되어 있다.</p>

<p><strong>현재 보완책</strong>: 웹훅 엔드포인트는 <code class="language-plaintext highlighter-rouge">x-webhook-secret</code> 헤더로 추가 검증을 통해 막아내고 있고, API 사용시 이를 추가하도록 해둠. 
<strong>추가 보완 방향</strong>: 보다 섬세한 방어를 위해선 IP 화이트리스트 또는 네트워크 레벨 격리 (Docker network 분리).</p>

<h2 id="jwtauthguard-useglobalguards-vs-app_guard-혼용">[JwtAuthGuard] <code class="language-plaintext highlighter-rouge">useGlobalGuards</code> vs <code class="language-plaintext highlighter-rouge">APP_GUARD</code> 혼용</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// main.ts: DI 컨테이너 밖</span>
<span class="nx">app</span><span class="p">.</span><span class="nf">useGlobalGuards</span><span class="p">(</span><span class="k">new</span> <span class="nc">OriginGuard</span><span class="p">());</span>

<span class="c1">// AppModule: DI 컨테이너 안</span>
<span class="p">{</span> <span class="na">provide</span><span class="p">:</span> <span class="nx">APP_GUARD</span><span class="p">,</span> <span class="na">useClass</span><span class="p">:</span> <span class="nx">JwtAuthGuard</span> <span class="p">}</span>
</code></pre></div></div>

<p><strong>문제</strong>: 두 방식이 혼용되어 전역 가드 목록을 한 곳에서 파악하기 어렵다.</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">main.ts</code>의 <code class="language-plaintext highlighter-rouge">OriginGuard</code>는 생성자 주입이 없어서 두 방식 다 가능하지만, <code class="language-plaintext highlighter-rouge">APP_GUARD</code>로 통일하면 가드 관리 포인트가 <code class="language-plaintext highlighter-rouge">AppModule</code> 하나로 집중된다. 추후 개선이 필요한 영역</li>
</ul>

<h2 id="knowledgeuploadbusycheckinterceptor-큐-상태를-에러-응답으로-노출">[KnowledgeUploadBusyCheckInterceptor] 큐 상태를 에러 응답으로 노출</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">throw</span> <span class="k">new</span> <span class="nc">ServiceUnavailableException</span><span class="p">({</span>
  <span class="na">data</span><span class="p">:</span> <span class="k">this</span><span class="p">.</span><span class="nx">queueService</span><span class="p">.</span><span class="nf">getStatus</span><span class="p">(),</span>  <span class="c1">// active, pending, concurrency, maxPending 노출</span>
<span class="p">});</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">getStatus()</code>가 반환하는 내부 처리량 정보(동시 실행 수, 대기열 크기)가 외부에 그대로 노출되도록 해두었던것을 발견. 해당 지표는 내부 운영 지표로 모니터링 시스템에서 보고(logging, loki), 외부 응답에서는 단순 재시도 안내만 해주는 것으로 해두는 것이 좋아 보인다.</p>

<hr />

<h1 id="4-핵심-메서드-및-라이브러리-함수-설명">4. 핵심 메서드 및 라이브러리 함수 설명</h1>

<h2 id="executioncontext"><code class="language-plaintext highlighter-rouge">ExecutionContext</code></h2>

<p>가드·인터셉터·필터에서 현재 실행 컨텍스트에 접근하는 객체. HTTP 외에도 WebSocket, RPC를 지원하기 때문에 <strong>프로토콜에 맞는 캐스팅</strong>이 필요하다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">context</span><span class="p">.</span><span class="nf">switchToHttp</span><span class="p">().</span><span class="nf">getRequest</span><span class="p">()</span>   <span class="c1">// HTTP Request 객체</span>
<span class="nx">context</span><span class="p">.</span><span class="nf">switchToHttp</span><span class="p">().</span><span class="nf">getResponse</span><span class="p">()</span>  <span class="c1">// HTTP Response 객체</span>
<span class="nx">context</span><span class="p">.</span><span class="nf">getHandler</span><span class="p">()</span>   <span class="c1">// 현재 라우트 핸들러 메서드 참조</span>
<span class="nx">context</span><span class="p">.</span><span class="nf">getClass</span><span class="p">()</span>     <span class="c1">// 현재 컨트롤러 클래스 참조</span>
</code></pre></div></div>

<h2 id="reflectorgetallandoverridekey-handler-class"><code class="language-plaintext highlighter-rouge">Reflector.getAllAndOverride(key, [handler, class])</code></h2>

<p>메타데이터를 읽는 NestJS 유틸리티. <code class="language-plaintext highlighter-rouge">getAllAndOverride</code>는 <strong>먼저 나열된 대상에서 값을 찾으면 거기서 반환</strong>한다 (오버라이드 우선순위 적용).</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 메서드에 @Public()이 있으면 그 값 사용</span>
<span class="c1">// 없으면 클래스에 @Public()이 있는지 확인</span>
<span class="k">this</span><span class="p">.</span><span class="nx">reflector</span><span class="p">.</span><span class="nx">getAllAndOverride</span><span class="o">&lt;</span><span class="nx">boolean</span><span class="o">&gt;</span><span class="p">(</span><span class="nx">IS_PUBLIC_KEY</span><span class="p">,</span> <span class="p">[</span>
  <span class="nx">context</span><span class="p">.</span><span class="nf">getHandler</span><span class="p">(),</span>  <span class="c1">// 1순위: 메서드 레벨</span>
  <span class="nx">context</span><span class="p">.</span><span class="nf">getClass</span><span class="p">(),</span>    <span class="c1">// 2순위: 클래스 레벨</span>
<span class="p">]);</span>
</code></pre></div></div>

<h2 id="authguardjwt-passport"><code class="language-plaintext highlighter-rouge">AuthGuard('jwt')</code> (Passport)</h2>

<p><code class="language-plaintext highlighter-rouge">@nestjs/passport</code>가 제공하는 추상 가드. 내부적으로 <code class="language-plaintext highlighter-rouge">PassportStrategy</code>를 실행하고, 성공 시 <code class="language-plaintext highlighter-rouge">request.user</code>에 <code class="language-plaintext highlighter-rouge">validate()</code> 반환값을 세팅한다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 상속 구조</span>
<span class="nx">JwtAuthGuard</span> <span class="kd">extends</span> <span class="nc">AuthGuard</span><span class="p">(</span><span class="dl">'</span><span class="s1">jwt</span><span class="dl">'</span><span class="p">)</span>
  <span class="err">→</span> <span class="nx">Passport가</span> <span class="nx">jwt</span> <span class="nx">전략을</span> <span class="nx">찾아</span> <span class="nx">실행</span>
  <span class="err">→</span> <span class="nx">JwtStrategy</span><span class="p">.</span><span class="nf">validate</span><span class="p">(</span><span class="nx">payload</span><span class="p">)</span> <span class="nx">호출</span>
  <span class="err">→</span> <span class="nx">반환값</span> <span class="err">→</span> <span class="nx">request</span><span class="p">.</span><span class="nx">user</span>
</code></pre></div></div>

<h2 id="createparamdecorator-validateuser"><code class="language-plaintext highlighter-rouge">createParamDecorator</code> (<code class="language-plaintext highlighter-rouge">@ValidateUser()</code>)</h2>

<p>컨트롤러 파라미터에서 값을 추출하는 커스텀 데코레이터 생성 함수. 인가된 유저에 대하여 토큰 속에 포함된 데이터를 기반으로 접근하여 빠른 이용자 확인, 유저 정보 이용이 가능하다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">export</span> <span class="kd">const</span> <span class="nx">ValidateUser</span> <span class="o">=</span> <span class="nf">createParamDecorator</span><span class="p">(</span>
  <span class="p">(</span><span class="nx">data</span><span class="p">:</span> <span class="nx">unknown</span><span class="p">,</span> <span class="nx">ctx</span><span class="p">:</span> <span class="nx">ExecutionContext</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="kd">const</span> <span class="nx">request</span> <span class="o">=</span> <span class="nx">ctx</span><span class="p">.</span><span class="nf">switchToHttp</span><span class="p">().</span><span class="nf">getRequest</span><span class="p">();</span>
    <span class="k">return</span> <span class="nx">request</span><span class="p">.</span><span class="nx">user</span><span class="p">;</span>   <span class="c1">// JwtStrategy.validate()가 세팅한 값</span>
  <span class="p">},</span>
<span class="p">);</span>

<span class="c1">// 사용</span>
<span class="k">async</span> <span class="nf">uploadDocs</span><span class="p">(@</span><span class="nd">ValidateUser</span><span class="p">()</span> <span class="nx">user</span><span class="p">:</span> <span class="nx">User</span><span class="p">)</span> <span class="p">{</span> <span class="p">...</span> <span class="p">}</span>
</code></pre></div></div>

<h2 id="nexthandle--callhandler"><code class="language-plaintext highlighter-rouge">next.handle()</code> / <code class="language-plaintext highlighter-rouge">CallHandler</code></h2>

<p>인터셉터에서 다음 단계(컨트롤러 또는 다음 인터셉터)를 실행하는 메서드. <strong>호출하지 않으면 컨트롤러 실행 자체가 막힌다</strong>. <code class="language-plaintext highlighter-rouge">KnowledgeUploadBusyCheckInterceptor</code>는 큐가 꽉 찼을 때 <code class="language-plaintext highlighter-rouge">next.handle()</code>을 호출하지 않고 예외를 던져 흐름을 차단한다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 요청 전처리만 (응답 후처리 없음)</span>
<span class="k">return</span> <span class="nx">next</span><span class="p">.</span><span class="nf">handle</span><span class="p">();</span>

<span class="c1">// 요청 전처리 + 응답 후처리</span>
<span class="k">return</span> <span class="nx">next</span><span class="p">.</span><span class="nf">handle</span><span class="p">().</span><span class="nf">pipe</span><span class="p">(</span>
  <span class="nf">tap</span><span class="p">(</span><span class="nx">data</span> <span class="o">=&gt;</span> <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="nx">data</span><span class="p">)),</span>      <span class="c1">// 사이드 이펙트</span>
  <span class="nf">map</span><span class="p">(</span><span class="nx">data</span> <span class="o">=&gt;</span> <span class="nf">transform</span><span class="p">(</span><span class="nx">data</span><span class="p">)),</span>        <span class="c1">// 응답 변환</span>
  <span class="nf">catchError</span><span class="p">(</span><span class="nx">err</span> <span class="o">=&gt;</span> <span class="p">...)</span>               <span class="c1">// 에러 처리</span>
<span class="p">);</span>
</code></pre></div></div>

<h2 id="setmetadatakey-value"><code class="language-plaintext highlighter-rouge">SetMetadata(key, value)</code></h2>

<p>클래스 또는 메서드에 메타데이터를 첨부하는 NestJS 유틸리티.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// @Public() 데코레이터 구현</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">IS_PUBLIC_KEY</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">isPublic</span><span class="dl">'</span><span class="p">;</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">Public</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="nc">SetMetadata</span><span class="p">(</span><span class="nx">IS_PUBLIC_KEY</span><span class="p">,</span> <span class="kc">true</span><span class="p">);</span>
</code></pre></div></div>

<p>Reflect API(<code class="language-plaintext highlighter-rouge">Reflect.defineMetadata</code>)를 래핑한 것으로, <code class="language-plaintext highlighter-rouge">Reflector</code>로 런타임에 조회할 수 있다.</p>

<h2 id="skipthrottle-chatcontroller-sse"><code class="language-plaintext highlighter-rouge">@SkipThrottle()</code> (ChatController SSE)</h2>

<p><code class="language-plaintext highlighter-rouge">@nestjs/throttler</code>가 제공하는 데코레이터. 특정 라우트에서 Rate Limit을 우회한다. 지정된 RateLimit 과 관계가 없는 장기 연결에서 자칫 RateLimit 이 발생하지 않기 위한 처리이다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Sse</span><span class="p">(</span><span class="dl">'</span><span class="s1">stream/:sessionId</span><span class="dl">'</span><span class="p">)</span>
<span class="p">@</span><span class="nd">SkipThrottle</span><span class="p">()</span>   <span class="c1">// SSE는 장기 연결이므로 요청 카운트 제외</span>
<span class="nf">stream</span><span class="p">(...)</span> <span class="p">{}</span>
</code></pre></div></div>

<hr />

<h1 id="5-대체-가능한-라이브러리-및-트레이드오프">5. 대체 가능한 라이브러리 및 트레이드오프</h1>

<h2 id="guards-대안">Guards 대안</h2>

<h3 id="passport-jwt-vs-직접-구현-jwt-검증"><code class="language-plaintext highlighter-rouge">passport-jwt</code> vs 직접 구현 JWT 검증</h3>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**passport-jwt** (현재)</td>
      <td>NestJS 공식 통합, 전략 패턴으로 확장 용이</td>
      <td>추상화가 많아 흐름 파악이 어려움</td>
    </tr>
    <tr>
      <td>**jose / jsonwebtoken 직접**</td>
      <td>흐름이 투명, 의존성 최소</td>
      <td>검증 로직 직접 구현 필요</td>
    </tr>
    <tr>
      <td>**@fastify/jwt**</td>
      <td>Fastify 어댑터 사용 시 성능 우위</td>
      <td>Express 어댑터와 혼용 불가</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Passport는 전략(Strategy) 패턴을 사용해 OAuth, Local, JWT 등 인증 방식을 교체하기 쉽다는 장점이 있다. 단일 JWT만 쓴다면 <code class="language-plaintext highlighter-rouge">jsonwebtoken</code> 직접 사용이 더 투명하다.</p>
</blockquote>

<h3 id="originguard의-cors-처리-위치">OriginGuard의 CORS 처리 위치</h3>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**OriginGuard** (현재)</td>
      <td>NestJS 파이프라인 내 처리, DI 활용 가능</td>
      <td>Guard에서 CORS를 처리하면 역할이 애매해짐</td>
    </tr>
    <tr>
      <td>**enableCors() 단독**</td>
      <td>CORS 표준 방식, 브라우저 프리플라이트 처리</td>
      <td>비브라우저 요청에 대한 서버 측 검증 없음</td>
    </tr>
    <tr>
      <td>**Nginx 레벨**</td>
      <td>앱 레이어 도달 전 차단, 성능 우수</td>
      <td>동적 도메인 관리 어려움</td>
    </tr>
  </tbody>
</table>

<h2 id="interceptors-대안">Interceptors 대안</h2>

<h3 id="tap-vs-map-vs-switchmap"><code class="language-plaintext highlighter-rouge">tap()</code> vs <code class="language-plaintext highlighter-rouge">map()</code> vs <code class="language-plaintext highlighter-rouge">switchMap()</code></h3>

<table>
  <thead>
    <tr>
      <th>연산자</th>
      <th>용도</th>
      <th>특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**<code class="language-plaintext highlighter-rouge">tap()</code>** (현재 로깅)</td>
      <td>사이드 이펙트 (로깅, 캐싱 등)</td>
      <td>스트림 값을 변경하지 않음</td>
    </tr>
    <tr>
      <td>**<code class="language-plaintext highlighter-rouge">map()</code>**</td>
      <td>응답 데이터 변환</td>
      <td>값을 변환해서 반환</td>
    </tr>
    <tr>
      <td>**<code class="language-plaintext highlighter-rouge">switchMap()</code>**</td>
      <td>비동기 변환 (DB 조회 등)</td>
      <td>내부 Observable을 교체</td>
    </tr>
    <tr>
      <td>**<code class="language-plaintext highlighter-rouge">catchError()</code>**</td>
      <td>에러 처리</td>
      <td>에러를 잡아 대체 Observable 반환</td>
    </tr>
  </tbody>
</table>

<h3 id="로깅-인터셉터-대안">로깅 인터셉터 대안</h3>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**LoggingInterceptor** (현재)</td>
      <td>NestJS 파이프라인 내 통합</td>
      <td>Guard 이전 로그 불가</td>
    </tr>
    <tr>
      <td>**Express Middleware**</td>
      <td>Guard 이전 포함 전체 요청 로깅</td>
      <td>NestJS 추상화 우회</td>
    </tr>
    <tr>
      <td>**APM 도구 (Datadog, New Relic)**</td>
      <td>자동 계측, 분산 추적</td>
      <td>비용, 벤더 잠금</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">LoggingInterceptor</code>는 Guard에서 차단된 요청(401, 403)은 로깅하지 않는다는 한계가 있다. 차단된 요청도 추적하려면 <code class="language-plaintext highlighter-rouge">GlobalExceptionFilter</code>에서 보완하거나 Express 미들웨어 레이어에서 처리해야 한다.</p>
</blockquote>

<h2 id="validationpipe-대안">ValidationPipe 대안</h2>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**class-validator** (현재)</td>
      <td>NestJS 공식, 데코레이터 기반, 직관적</td>
      <td>런타임 반영(reflect-metadata) 필요</td>
    </tr>
    <tr>
      <td>**zod + ZodPipe**</td>
      <td>타입 추론 강력, TS 친화적</td>
      <td>NestJS 기본 통합 아님</td>
    </tr>
    <tr>
      <td>**Joi + JoiPipe**</td>
      <td>성숙한 생태계</td>
      <td>class-decorator 방식과 혼용 불편</td>
    </tr>
    <tr>
      <td>**수동 검증**</td>
      <td>의존성 없음</td>
      <td>반복 코드, 유지보수 어려움</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p><strong>zod</strong>는 최근 TypeScript 생태계에서 강세. 스키마 정의와 타입 추론이 동시에 되어 <code class="language-plaintext highlighter-rouge">z.infer&lt;typeof schema&gt;</code>로 별도 DTO 클래스 없이 타입을 뽑을 수 있다. 단, <code class="language-plaintext highlighter-rouge">class-validator</code>와 달리 데코레이터 방식이 아니라 파이프 코드를 직접 작성해야 한다.</p>
</blockquote>

<hr />

<h1 id="6-개발자로서-알아야-할-영역">6. 개발자로서 알아야 할 영역</h1>

<h2 id="a-각-레이어의-책임-경계-이해">A. 각 레이어의 책임 경계 이해</h2>

<p>파이프라인 각 단계가 <strong>왜 그 위치에 있는지</strong> 설명할 수 있어야 한다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Middleware  → HTTP 레벨 처리. NestJS 추상화 없이 요청·응답 객체 직접 조작.
              인증이나 로직보다는 rawBody 파싱, 압축, 요청 변환에 적합.

Guard       → "이 요청이 들어올 자격이 있는가?" 를 판단.
              인증(Authentication), 인가(Authorization), 서비스 가용성 확인.
              boolean을 반환하므로 로직이 단순해야 한다.

Interceptor → "요청/응답 주변에 무엇을 할 것인가?" 를 처리.
              로깅, 캐싱, 응답 변환, 타임아웃, 큐 체크 등.
              Observable을 반환하므로 RxJS 연산자 활용 가능.

Pipe        → "데이터가 올바른 형태인가?" 를 검증·변환.
              타입 변환, 유효성 검사. 인증 이후에만 실행.
</code></pre></div></div>

<h2 id="b-canactivate가-false를-반환하는-것과-예외를-던지는-것의-차이">B. <code class="language-plaintext highlighter-rouge">canActivate</code>가 <code class="language-plaintext highlighter-rouge">false</code>를 반환하는 것과 예외를 던지는 것의 차이</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// false 반환 → NestJS가 기본 UnauthorizedException 생성</span>
<span class="nf">canActivate</span><span class="p">()</span> <span class="p">{</span> <span class="k">return</span> <span class="kc">false</span><span class="p">;</span> <span class="p">}</span>

<span class="c1">// 예외 직접 throw → 원하는 상태코드·메시지 제어 가능</span>
<span class="nf">canActivate</span><span class="p">()</span> <span class="p">{</span> <span class="k">throw</span> <span class="k">new</span> <span class="nc">ForbiddenException</span><span class="p">(</span><span class="dl">'</span><span class="s1">Not allowed origin</span><span class="dl">'</span><span class="p">);</span> <span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">false</code>를 반환하면 NestJS가 자동으로 <code class="language-plaintext highlighter-rouge">401 Unauthorized</code>를 응답한다. 하지만 이 프로젝트처럼 이유가 다를 때(Origin 차단은 <code class="language-plaintext highlighter-rouge">403 Forbidden</code>, AI 서비스 불가는 <code class="language-plaintext highlighter-rouge">503 Service Unavailable</code>)는 <strong>예외를 직접 던져야 원하는 상태코드</strong>를 줄 수 있다.</p>

<h2 id="c-전역-등록-vs-라우트-등록의-설계-원칙">C. 전역 등록 vs 라우트 등록의 설계 원칙</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>전역 등록</th>
      <th>라우트 등록</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**기준**</td>
      <td>모든 요청에 적용해야 하는가</td>
      <td>특정 도메인·엔드포인트에만 필요한가</td>
    </tr>
    <tr>
      <td>**가드 예시**</td>
      <td>JwtAuthGuard, ThrottlerGuard</td>
      <td>AiCircuitGuard</td>
    </tr>
    <tr>
      <td>**인터셉터 예시**</td>
      <td>LoggingInterceptor</td>
      <td>KnowledgeUploadBusyCheckInterceptor</td>
    </tr>
    <tr>
      <td>**장점**</td>
      <td>누락 위험 없음</td>
      <td>불필요한 실행 없음, 명시적 의도</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>이 프로젝트의 <code class="language-plaintext highlighter-rouge">JwtAuthGuard</code>는 전역으로 걸되 <code class="language-plaintext highlighter-rouge">@Public()</code>으로 예외를 허용하는 방식 → <strong>“기본 잠금, 필요 시 열기”</strong> 패턴. 
<code class="language-plaintext highlighter-rouge">AiCircuitGuard</code>는 AI 관련 라우트에만 명시적으로 적용 → <strong>“필요한 곳에만 잠금”</strong> 패턴. 
두 패턴을 기능적 특성, 상황에 맞게 선택할 수 있어야 한다.</p>
</blockquote>

<h2 id="d-interceptor에서-rxjs를-사용하는-이유">D. Interceptor에서 RxJS를 사용하는 이유</h2>

<p>NestJS Interceptor가 <code class="language-plaintext highlighter-rouge">Observable</code>을 반환하는 이유는 <strong>SSE·WebSocket·스트리밍 응답을 일관된 방식으로 처리</strong>하기 위해서다. 일반 HTTP 응답도 내부적으로 Observable로 래핑된다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 응답 완료 후 로직 (tap)</span>
<span class="nx">next</span><span class="p">.</span><span class="nf">handle</span><span class="p">().</span><span class="nf">pipe</span><span class="p">(</span><span class="nf">tap</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span> <span class="cm">/* 로깅 */</span> <span class="p">}))</span>

<span class="c1">// 응답 변환 (map)</span>
<span class="nx">next</span><span class="p">.</span><span class="nf">handle</span><span class="p">().</span><span class="nf">pipe</span><span class="p">(</span><span class="nf">map</span><span class="p">(</span><span class="nx">data</span> <span class="o">=&gt;</span> <span class="p">({</span> <span class="na">success</span><span class="p">:</span> <span class="kc">true</span><span class="p">,</span> <span class="nx">data</span> <span class="p">})))</span>

<span class="c1">// 에러 → 정상 응답으로 변환 (catchError)</span>
<span class="nx">next</span><span class="p">.</span><span class="nf">handle</span><span class="p">().</span><span class="nf">pipe</span><span class="p">(</span><span class="nf">catchError</span><span class="p">(</span><span class="nx">err</span> <span class="o">=&gt;</span> <span class="k">of</span><span class="p">({</span> <span class="na">success</span><span class="p">:</span> <span class="kc">false</span><span class="p">,</span> <span class="na">error</span><span class="p">:</span> <span class="nx">err</span><span class="p">.</span><span class="nx">message</span> <span class="p">})))</span>

<span class="c1">// 타임아웃 처리</span>
<span class="nx">next</span><span class="p">.</span><span class="nf">handle</span><span class="p">().</span><span class="nf">pipe</span><span class="p">(</span><span class="nf">timeout</span><span class="p">(</span><span class="mi">5000</span><span class="p">),</span> <span class="nf">catchError</span><span class="p">(</span><span class="nx">err</span> <span class="o">=&gt;</span> <span class="p">...))</span>
</code></pre></div></div>

<h2 id="e-public-패턴의-보안-함의">E. <code class="language-plaintext highlighter-rouge">@Public()</code> 패턴의 보안 함의</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Public</span><span class="p">()</span>
<span class="p">@</span><span class="nd">Post</span><span class="p">(</span><span class="dl">'</span><span class="s1">webhook</span><span class="dl">'</span><span class="p">)</span>
<span class="nf">handleWebhook</span><span class="p">(@</span><span class="nd">Headers</span><span class="p">(</span><span class="dl">'</span><span class="s1">x-webhook-secret</span><span class="dl">'</span><span class="p">)</span> <span class="nx">secret</span><span class="p">:</span> <span class="kr">string</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">if </span><span class="p">(</span><span class="nx">secret</span> <span class="o">!==</span> <span class="nx">INTERNAL_SECRET</span><span class="p">)</span> <span class="k">throw</span> <span class="k">new</span> <span class="nc">UnauthorizedException</span><span class="p">();</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@Public()</code>으로 JWT 인증을 우회한 엔드포인트는 <strong>반드시 다른 인증 수단</strong>으로 보완해야 한다. 
이 프로젝트의 웹훅은 <code class="language-plaintext highlighter-rouge">x-webhook-secret</code> 헤더 검증으로 보완하고 있다. 
<code class="language-plaintext highlighter-rouge">@Public()</code> 붙이고 아무 검증도 없는 엔드포인트는 의도치 않은 공개 API가 된다.</p>

<h2 id="f-파이프라인-디버깅-방법">F. 파이프라인 디버깅 방법</h2>

<p>요청이 어느 단계에서 막히는지 확인하는 실용적 방법:</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Guard에서 로그 추가</span>
<span class="nf">canActivate</span><span class="p">(</span><span class="nx">context</span><span class="p">)</span> <span class="p">{</span>
  <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">'</span><span class="s1">[Guard] 실행됨</span><span class="dl">'</span><span class="p">);</span>
  <span class="c1">// ...</span>
<span class="p">}</span>

<span class="c1">// Interceptor에서 로그 추가</span>
<span class="nf">intercept</span><span class="p">(</span><span class="nx">context</span><span class="p">,</span> <span class="nx">next</span><span class="p">)</span> <span class="p">{</span>
  <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">'</span><span class="s1">[Interceptor] before</span><span class="dl">'</span><span class="p">);</span>
  <span class="k">return</span> <span class="nx">next</span><span class="p">.</span><span class="nf">handle</span><span class="p">().</span><span class="nf">pipe</span><span class="p">(</span>
    <span class="nf">tap</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">'</span><span class="s1">[Interceptor] after</span><span class="dl">'</span><span class="p">))</span>
  <span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>또는 NestJS의 <code class="language-plaintext highlighter-rouge">Logger</code>를 활용해 각 단계 진입 로그를 남겨두면 운영 중에도 Loki에서 요청 흐름 추적이 가능하다.</p>

<hr />

<h1 id="핵심-요약-카드">핵심 요약 카드</h1>

<blockquote>
  <p>[!abstract] Guard 3종 역할 구분</p>
  <ul>
    <li><code class="language-plaintext highlighter-rouge">OriginGuard</code> → <strong>출처(Origin) 검사</strong> : 브라우저 기반 요청의 도메인 화이트리스트</li>
    <li><code class="language-plaintext highlighter-rouge">JwtAuthGuard</code> → <strong>신원(Authentication)</strong> : 토큰 유효성 확인 + request.user 세팅</li>
    <li><code class="language-plaintext highlighter-rouge">AiCircuitGuard</code> → <strong>서비스 가용성</strong> : AI 워커 생존 여부에 따른 서킷브레이커</li>
  </ul>
</blockquote>

<blockquote>
  <p>[!abstract] Interceptor 3종 역할 구분</p>
  <ul>
    <li><code class="language-plaintext highlighter-rouge">LoggingInterceptor</code> → 요청 전/응답 후 구조화 로그 수집</li>
    <li><code class="language-plaintext highlighter-rouge">KnowledgeUploadBusyCheckInterceptor</code> → 컨트롤러 도달 전 큐 포화 차단</li>
    <li><code class="language-plaintext highlighter-rouge">ClassSerializerInterceptor</code> → 응답 객체의 민감 필드 직렬화 제어</li>
  </ul>
</blockquote>

<blockquote>
  <p>[!question] 설명할 수 있어야 하는 것</p>
  <ol>
    <li>Middleware가 Guard보다 먼저 실행되는 이유</li>
    <li><code class="language-plaintext highlighter-rouge">OriginGuard</code>에서 <code class="language-plaintext highlighter-rouge">origin</code>이 없을 때 통과시키는 이유와 보완책</li>
    <li><code class="language-plaintext highlighter-rouge">next.handle()</code>을 호출하지 않으면 어떻게 되는가</li>
    <li><code class="language-plaintext highlighter-rouge">getAllAndOverride</code> vs <code class="language-plaintext highlighter-rouge">getAllAndMerge</code>의 차이</li>
    <li>인터셉터가 <code class="language-plaintext highlighter-rouge">Observable</code>을 반환해야 하는 이유</li>
    <li><code class="language-plaintext highlighter-rouge">ValidationPipe</code>의 <code class="language-plaintext highlighter-rouge">whitelist</code>와 <code class="language-plaintext highlighter-rouge">forbidNonWhitelisted</code>의 차이</li>
  </ol>
</blockquote>]]></content><author><name>Paul2021-R</name></author><category term="문제해결" /><category term="Backend" /><category term="개발" /><category term="Protostar" /><category term="Project" /><category term="Review" /><category term="NestJS" /><category term="AI" /><summary type="html"><![CDATA[NestJS 파이프라인 구성요소 분석 Middleware → Guards → Interceptors → Pipes]]></summary></entry><entry><title type="html">Protostar review note - 06 NestJS Intro</title><link href="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/18/00-protostar-review.html" rel="alternate" type="text/html" title="Protostar review note - 06 NestJS Intro" /><published>2026-02-18T00:00:00+00:00</published><updated>2026-02-18T00:00:00+00:00</updated><id>http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/18/00-protostar-review</id><content type="html" xml:base="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/18/00-protostar-review.html"><![CDATA[<h1 id="nestjs-개념-정리">NestJS 개념 정리</h1>
<p>본 정리 내용은, 프레임워크에 대한 이해도를 복기 하면서도 짜게 되었던 구성요소들을 더 심화시켜 바라보고 풀스택을 하다보니 놓치거나 좀더 좋은 코드를 만들기 위해 필요한 개념 복습, 코드 구성에 대한 고민을 위한 추가 복습이다.</p>

<p>개괄적인 전체 내용을 복습한 이후, 특정 파일 구성요소 중심으로 복습을 기록할 예정이다.</p>

<ul>
  <li>Node.js 위에서 동작하는 <strong>구조화된 백엔드 프레임워크</strong></li>
  <li>TypeScript 네이티브 지원, Angular에서 영감을 받은 <strong>모듈/DI 기반 아키텍처</strong></li>
  <li>Express(기본) 또는 Fastify를 HTTP 어댑터로 사용</li>
  <li>데코레이터(<code class="language-plaintext highlighter-rouge">@Module</code>, <code class="language-plaintext highlighter-rouge">@Controller</code>, <code class="language-plaintext highlighter-rouge">@Injectable</code> 등)로 메타데이터를 선언하고, IoC 컨테이너가 의존성을 관리함</li>
</ul>

<h2 id="핵심-철학-및-특징">핵심 철학 및 특징</h2>
<ul>
  <li><strong>모듈성(Modularity)</strong>: 기능 단위로 모듈을 나누고, 각 모듈이 자신의 책임 범위를 캡슐화</li>
  <li><strong>의존성 주입(DI)</strong>: NestJS IoC 컨테이너가 Provider 인스턴스를 생성·관리하며, 생성자에서 자동 주입</li>
  <li><strong>데코레이터 기반</strong>: 클래스와 메서드에 데코레이터를 붙여 역할을 선언적으로 표현</li>
  <li><strong>레이어드 파이프라인</strong>: 요청이 들어오면 정해진 순서(Lifecycle)대로 처리 계층을 통과</li>
</ul>

<h2 id="fastapi와의-비교">FastAPI와의 비교</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>NestJS</th>
      <th>FastAPI</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**언어**</td>
      <td>TypeScript</td>
      <td>Python</td>
    </tr>
    <tr>
      <td>**DI 방식**</td>
      <td>IoC 컨테이너(자동 관리)</td>
      <td>함수 기반 Depends(명시적 추적)</td>
    </tr>
    <tr>
      <td>**구조화**</td>
      <td>강제적(Module/Controller/Service 분리)</td>
      <td>자유로움(도메인 설계는 개발자 몫)</td>
    </tr>
    <tr>
      <td>**비동기**</td>
      <td>Promise / RxJS Observable</td>
      <td>async/await (ASGI 기반)</td>
    </tr>
    <tr>
      <td>**주요 용도**</td>
      <td>복잡한 비즈니스 로직, 엔터프라이즈</td>
      <td>AI 서빙, 경량 마이크로서비스</td>
    </tr>
  </tbody>
</table>

<hr />

<h1 id="요청-생명주기-request-lifecycle">요청 생명주기 (Request Lifecycle)</h1>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">main.ts</code> 주석에 직접 남겨둔 순서. NestJS의 파이프라인은 <strong>절대적으로 지켜지는 순서</strong>이므로 설계 시 이 흐름을 먼저 이해해야 한다.</p>
</blockquote>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. 요청 진입 (HTTP Request)
2. Middleware     ─ 요청 자체가 이상하면 미리 컷
3. Guards        ─ 인증/인가 (검증 실패 시 Pipe 불필요)
4. Interceptors  ─ 요청 전처리 (로깅, 변환 등)
5. Pipes         ─ 인증된 사용자만 데이터 검사·변환
6. Controller &amp; Service ─ 실제 비즈니스 로직
7. Interceptors  ─ 응답 후처리 (직렬화, 포맷 등)
8. Exception Filters ─ 에러 일괄 처리
9. Response
</code></pre></div></div>

<blockquote>
  <p>[!tip] 핵심 포인트
Guards 다음에 Pipe가 오는 이유: <strong>인증이 안 된 요청의 데이터를 굳이 검증할 필요가 없기 때문</strong>. 순서에 논리적 이유가 있다.</p>
</blockquote>

<hr />

<h1 id="프로젝트-구조-protostar-nestjs">프로젝트 구조 (Protostar NestJS)</h1>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>src/
├── app.module.ts          # 루트 모듈 (전체 조립)
├── app.controller.ts      # 루트 컨트롤러 (헬스체크 등)
├── app.service.ts
├── main.ts                # 진입점 (글로벌 파이프라인 설정)
│
├── common/                # 공통 인프라 레이어
│   ├── constants.ts       # 전역 상수
│   ├── decorators/        # 커스텀 데코레이터
│   │   ├── public.decorator.ts        # @Public() - JWT 인증 우회
│   │   └── validate-user.decorator.ts # @ValidateUser() - 유저 추출
│   ├── filters/
│   │   └── http-exception.filter.ts   # 전역 예외 필터
│   ├── guards/
│   │   ├── jwt-auth.guard.ts          # 전역 JWT 인증 가드
│   │   ├── ai-circuit.guard.ts        # AI 서비스 상태 서킷브레이커
│   │   └── origin.guard.ts            # Origin 화이트리스트 검사
│   ├── interceptors/
│   │   ├── logging.interceptor.ts            # HTTP 트래픽 로깅
│   │   └── knowledge-upload-busy-check.interceptor.ts
│   ├── monitoring/
│   │   ├── ai-status-monitoring.service.ts   # AI 상태 폴링 (1초 크론)
│   │   └── system-monitoring.service.ts
│   ├── objectStorage/     # MinIO 연동
│   ├── prisma/            # Prisma ORM 모듈
│   ├── queue/
│   │   └── queue.service.ts           # 인메모리 동시성 제어 큐
│   ├── redis/
│   │   └── redis.module.ts            # Redis 클라이언트 모듈
│   └── strategy/
│       └── jwt.strategy.ts            # Passport JWT 전략
│
└── features/              # 기능 도메인 레이어
    ├── auth/              # 인증 (로그인, 회원가입, JWT 발급)
    ├── chat/              # SSE 스트림 + Redis Pub/Sub
    └── knowledge/         # 파일 업로드 + RAG 웹훅
</code></pre></div></div>

<blockquote>
  <p>[!info] 구조 설계 원칙</p>
  <ul>
    <li><code class="language-plaintext highlighter-rouge">common/</code> : 어떤 도메인에도 종속되지 않는 인프라성 코드 (가드, 인터셉터, 필터, 공유 서비스)</li>
    <li><code class="language-plaintext highlighter-rouge">features/</code> : 실제 비즈니스 도메인 단위 (각 도메인이 자신의 module/controller/service/dto를 포함)</li>
  </ul>
</blockquote>

<hr />

<h1 id="모듈-시스템">모듈 시스템</h1>

<p>NestJS의 모든 구성 요소는 <strong>모듈로 조립</strong>된다. <code class="language-plaintext highlighter-rouge">@Module</code> 데코레이터의 4가지 필드를 이해하면 전체 구조가 보인다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Module</span><span class="p">({</span>
  <span class="na">imports</span><span class="p">:</span> <span class="p">[],</span>      <span class="c1">// 다른 모듈에서 가져올 것들</span>
  <span class="na">controllers</span><span class="p">:</span> <span class="p">[],</span>  <span class="c1">// 이 모듈의 HTTP 엔드포인트</span>
  <span class="na">providers</span><span class="p">:</span> <span class="p">[],</span>    <span class="c1">// 이 모듈에서 DI로 관리할 서비스/가드 등</span>
  <span class="na">exports</span><span class="p">:</span> <span class="p">[],</span>      <span class="c1">// 외부 모듈에서 사용할 수 있도록 공개</span>
<span class="p">})</span>
</code></pre></div></div>

<h3 id="appmodule-분석-appmodulets">AppModule 분석 (<code class="language-plaintext highlighter-rouge">app.module.ts</code>)</h3>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Module</span><span class="p">({</span>
  <span class="na">imports</span><span class="p">:</span> <span class="p">[</span>
    <span class="nx">ConfigModule</span><span class="p">.</span><span class="nf">forRoot</span><span class="p">({</span> <span class="na">isGlobal</span><span class="p">:</span> <span class="kc">true</span> <span class="p">}),</span>   <span class="c1">// 환경변수 전역 등록</span>
    <span class="nx">RedisModule</span><span class="p">,</span>
    <span class="nx">ChatModule</span><span class="p">,</span>
    <span class="nx">ThrottlerModule</span><span class="p">.</span><span class="nf">forRootAsync</span><span class="p">({</span> <span class="p">...</span> <span class="p">}),</span>       <span class="c1">// Redis 기반 Rate Limit</span>
    <span class="nx">ObjectStorageModule</span><span class="p">,</span>
    <span class="nx">AuthModule</span><span class="p">,</span>
    <span class="nx">PrismaModule</span><span class="p">,</span>
    <span class="nx">KnowledgeModule</span><span class="p">,</span>
  <span class="p">],</span>
  <span class="na">providers</span><span class="p">:</span> <span class="p">[</span>
    <span class="nx">AppService</span><span class="p">,</span>
    <span class="p">{</span> <span class="na">provide</span><span class="p">:</span> <span class="nx">APP_GUARD</span><span class="p">,</span> <span class="na">useClass</span><span class="p">:</span> <span class="nx">ThrottlerGuard</span> <span class="p">},</span>  <span class="c1">// 전역 Rate Limit 가드</span>
    <span class="p">{</span> <span class="na">provide</span><span class="p">:</span> <span class="nx">APP_GUARD</span><span class="p">,</span> <span class="na">useClass</span><span class="p">:</span> <span class="nx">JwtAuthGuard</span> <span class="p">},</span>    <span class="c1">// 전역 JWT 인증 가드</span>
  <span class="p">],</span>
<span class="p">})</span>
<span class="k">export</span> <span class="kd">class</span> <span class="nc">AppModule</span> <span class="p">{}</span>
</code></pre></div></div>

<blockquote>
  <p>[!note] APP_GUARD 토큰
<code class="language-plaintext highlighter-rouge">APP_GUARD</code> 는 NestJS가 전역 가드를 등록하는 특수 토큰. 모든 요청에 자동 적용된다.
<code class="language-plaintext highlighter-rouge">ThrottlerGuard</code>와 <code class="language-plaintext highlighter-rouge">JwtAuthGuard</code> 두 개를 등록하면 <strong>모든 요청이 두 가드를 순서대로 통과</strong>한다.</p>
</blockquote>

<hr />

<h1 id="guards-가드">Guards (가드)</h1>

<p>요청을 <strong>허용할지 차단할지 결정</strong>하는 계층. <code class="language-plaintext highlighter-rouge">canActivate()</code> 가 <code class="language-plaintext highlighter-rouge">true</code>를 반환해야 다음 단계로 진행된다.</p>

<h2 id="jwtauthguard---전역-jwt-인증">JwtAuthGuard - 전역 JWT 인증</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Injectable</span><span class="p">()</span>
<span class="k">export</span> <span class="kd">class</span> <span class="nc">JwtAuthGuard</span> <span class="kd">extends</span> <span class="nc">AuthGuard</span><span class="p">(</span><span class="dl">'</span><span class="s1">jwt</span><span class="dl">'</span><span class="p">)</span> <span class="p">{</span>
  <span class="nf">constructor</span><span class="p">(</span><span class="k">private</span> <span class="nx">reflector</span><span class="p">:</span> <span class="nx">Reflector</span><span class="p">)</span> <span class="p">{</span> <span class="k">super</span><span class="p">();</span> <span class="p">}</span>

  <span class="nf">canActivate</span><span class="p">(</span><span class="nx">context</span><span class="p">:</span> <span class="nx">ExecutionContext</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Reflector로 메타데이터 확인</span>
    <span class="kd">const</span> <span class="nx">isPublic</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">reflector</span><span class="p">.</span><span class="nx">getAllAndOverride</span><span class="o">&lt;</span><span class="nx">boolean</span><span class="o">&gt;</span><span class="p">(</span><span class="nx">IS_PUBLIC_KEY</span><span class="p">,</span> <span class="p">[</span>
      <span class="nx">context</span><span class="p">.</span><span class="nf">getHandler</span><span class="p">(),</span>  <span class="c1">// 메서드 레벨 메타데이터</span>
      <span class="nx">context</span><span class="p">.</span><span class="nf">getClass</span><span class="p">(),</span>    <span class="c1">// 클래스 레벨 메타데이터</span>
    <span class="p">]);</span>

    <span class="k">if </span><span class="p">(</span><span class="nx">isPublic</span><span class="p">)</span> <span class="k">return</span> <span class="kc">true</span><span class="p">;</span>  <span class="c1">// @Public() 이면 통과</span>
    <span class="k">return</span> <span class="k">super</span><span class="p">.</span><span class="nf">canActivate</span><span class="p">(</span><span class="nx">context</span><span class="p">);</span>  <span class="c1">// 아니면 JWT 검증</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>핵심 패턴</strong>: 전역으로 JWT 인증을 걸되, <code class="language-plaintext highlighter-rouge">@Public()</code> 데코레이터로 특정 엔드포인트만 우회.
→ 기본값이 “인증 필요”이므로 <strong>새 엔드포인트 추가 시 인증을 까먹을 위험이 없다</strong>.</p>

<h2 id="aicircuitguard---서킷-브레이커">AiCircuitGuard - 서킷 브레이커</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Injectable</span><span class="p">()</span>
<span class="k">export</span> <span class="kd">class</span> <span class="nc">AiCircuitGuard</span> <span class="k">implements</span> <span class="nx">CanActivate</span> <span class="p">{</span>
  <span class="nf">canActivate</span><span class="p">(</span><span class="nx">context</span><span class="p">:</span> <span class="nx">ExecutionContext</span><span class="p">):</span> <span class="nx">boolean</span> <span class="p">{</span>
    <span class="kd">const</span> <span class="nx">isAvailable</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">aiStatusMonitoringService</span><span class="p">.</span><span class="nf">isAvailable</span><span class="p">();</span>
    <span class="k">if </span><span class="p">(</span><span class="o">!</span><span class="nx">isAvailable</span><span class="p">)</span> <span class="k">throw</span> <span class="k">new</span> <span class="nc">ServiceUnavailableException</span><span class="p">(</span><span class="dl">'</span><span class="s1">AI Service Unavailable.</span><span class="dl">'</span><span class="p">);</span>
    <span class="k">return</span> <span class="kc">true</span><span class="p">;</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<ul>
  <li>AI 서비스(FastAPI 워커)가 살아있는지 확인 후 요청 차단</li>
  <li>서킷브레이커 패턴을 Guard 레이어에서 구현</li>
  <li><code class="language-plaintext highlighter-rouge">@UseGuards(AiCircuitGuard)</code> 로 채팅/지식 엔드포인트에만 적용</li>
</ul>

<hr />

<h1 id="interceptors-인터셉터">Interceptors (인터셉터)</h1>

<p>요청/응답 <strong>전후를 모두 가로채는</strong> 계층. RxJS <code class="language-plaintext highlighter-rouge">Observable</code>을 반환해야 한다.</p>

<h2 id="logginginterceptor---http-트래픽-로깅">LoggingInterceptor - HTTP 트래픽 로깅</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Injectable</span><span class="p">()</span>
<span class="k">export</span> <span class="kd">class</span> <span class="nc">LoggingInterceptor</span> <span class="k">implements</span> <span class="nx">NestInterceptor</span> <span class="p">{</span>
  <span class="nf">intercept</span><span class="p">(</span><span class="nx">context</span><span class="p">:</span> <span class="nx">ExecutionContext</span><span class="p">,</span> <span class="nx">next</span><span class="p">:</span> <span class="nx">CallHandler</span><span class="p">):</span> <span class="nx">Observable</span><span class="o">&lt;</span><span class="kr">any</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="kd">const</span> <span class="nx">now</span> <span class="o">=</span> <span class="nb">Date</span><span class="p">.</span><span class="nf">now</span><span class="p">();</span>

    <span class="k">return</span> <span class="nx">next</span><span class="p">.</span><span class="nf">handle</span><span class="p">().</span><span class="nf">pipe</span><span class="p">(</span>
      <span class="nf">tap</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
        <span class="c1">// 응답이 나간 후 실행 (afterResponse)</span>
        <span class="kd">const</span> <span class="nx">responseTime</span> <span class="o">=</span> <span class="nb">Date</span><span class="p">.</span><span class="nf">now</span><span class="p">()</span> <span class="o">-</span> <span class="nx">now</span><span class="p">;</span>
        <span class="c1">// Promtail/Loki 수집용 구조화 로그 출력</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">logger</span><span class="p">.</span><span class="nf">log</span><span class="p">({</span> <span class="na">type</span><span class="p">:</span> <span class="dl">'</span><span class="s1">http_traffic_protostar</span><span class="dl">'</span><span class="p">,</span> <span class="na">duration_ms</span><span class="p">:</span> <span class="nx">responseTime</span><span class="p">,</span> <span class="p">...</span> <span class="p">});</span>
      <span class="p">}),</span>
    <span class="p">);</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<blockquote>
  <p>[!tip] <code class="language-plaintext highlighter-rouge">next.handle().pipe(tap(...))</code></p>
  <ul>
    <li><code class="language-plaintext highlighter-rouge">next.handle()</code> : 다음 핸들러(컨트롤러)를 실행하는 Observable</li>
    <li><code class="language-plaintext highlighter-rouge">tap()</code> : 스트림을 변환하지 않고 <strong>사이드 이펙트만</strong> 실행 (로깅에 적합)</li>
    <li>요청 전 로직은 <code class="language-plaintext highlighter-rouge">next.handle()</code> 호출 <strong>전</strong>, 응답 후 로직은 <code class="language-plaintext highlighter-rouge">pipe()</code> 안에 작성</li>
  </ul>
</blockquote>

<hr />

<h1 id="exception-filters-예외-필터">Exception Filters (예외 필터)</h1>

<p>파이프라인의 <strong>마지막 방어선</strong>. 처리되지 않은 예외를 잡아 일관된 에러 응답으로 변환한다.</p>

<h2 id="globalexceptionfilter">GlobalExceptionFilter</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Catch</span><span class="p">()</span>  <span class="c1">// 인자 없으면 모든 예외 캐치</span>
<span class="k">export</span> <span class="kd">class</span> <span class="nc">GlobalExceptionFilter</span> <span class="k">implements</span> <span class="nx">ExceptionFilter</span> <span class="p">{</span>
  <span class="k">catch</span><span class="p">(</span><span class="nx">exception</span><span class="p">:</span> <span class="nx">unknown</span><span class="p">,</span> <span class="nx">host</span><span class="p">:</span> <span class="nx">ArgumentsHost</span><span class="p">)</span> <span class="p">{</span>
    <span class="kd">const</span> <span class="nx">status</span> <span class="o">=</span> <span class="nx">exception</span> <span class="k">instanceof</span> <span class="nx">HttpException</span>
      <span class="p">?</span> <span class="nx">exception</span><span class="p">.</span><span class="nf">getStatus</span><span class="p">()</span>
      <span class="p">:</span> <span class="nx">HttpStatus</span><span class="p">.</span><span class="nx">INTERNAL_SERVER_ERROR</span><span class="p">;</span>

    <span class="c1">// 5xx: logger.error / 4xx: logger.warn 으로 Loki 레벨 분리</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">status</span> <span class="o">&gt;=</span> <span class="mi">500</span><span class="p">)</span> <span class="k">this</span><span class="p">.</span><span class="nx">logger</span><span class="p">.</span><span class="nf">error</span><span class="p">(</span><span class="nx">JSON</span><span class="p">.</span><span class="nf">stringify</span><span class="p">(</span><span class="nx">logData</span><span class="p">));</span>
    <span class="k">else</span> <span class="k">this</span><span class="p">.</span><span class="nx">logger</span><span class="p">.</span><span class="nf">warn</span><span class="p">(</span><span class="nx">JSON</span><span class="p">.</span><span class="nf">stringify</span><span class="p">(</span><span class="nx">logData</span><span class="p">));</span>

    <span class="nx">response</span><span class="p">.</span><span class="nf">status</span><span class="p">(</span><span class="nx">status</span><span class="p">).</span><span class="nf">json</span><span class="p">({</span>
      <span class="na">success</span><span class="p">:</span> <span class="kc">false</span><span class="p">,</span>
      <span class="na">statusCode</span><span class="p">:</span> <span class="nx">status</span><span class="p">,</span>
      <span class="na">timestamp</span><span class="p">:</span> <span class="p">...,</span>
      <span class="na">path</span><span class="p">:</span> <span class="nx">request</span><span class="p">.</span><span class="nx">url</span><span class="p">,</span>
      <span class="na">message</span><span class="p">:</span> <span class="p">...,</span>
    <span class="p">});</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>설계 포인트</strong>:</p>
<ul>
  <li>모든 에러의 응답 형태를 <code class="language-plaintext highlighter-rouge">{ success: false, statusCode, timestamp, path, message }</code> 로 통일</li>
  <li>5xx / 4xx를 로그 레벨로 분리해 Loki(로그 시스템)에서 알람 기준을 다르게 설정</li>
</ul>

<hr />

<h1 id="커스텀-데코레이터">커스텀 데코레이터</h1>

<p>NestJS에서 메타데이터를 활용한 커스텀 데코레이터를 만드는 두 가지 패턴.</p>

<h2 id="public---메타데이터-태깅">@Public() - 메타데이터 태깅</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 정의</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">IS_PUBLIC_KEY</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">isPublic</span><span class="dl">'</span><span class="p">;</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">Public</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="nc">SetMetadata</span><span class="p">(</span><span class="nx">IS_PUBLIC_KEY</span><span class="p">,</span> <span class="kc">true</span><span class="p">);</span>

<span class="c1">// 사용</span>
<span class="p">@</span><span class="nd">Public</span><span class="p">()</span>
<span class="p">@</span><span class="nd">Get</span><span class="p">(</span><span class="dl">'</span><span class="s1">stream/:sessionId</span><span class="dl">'</span><span class="p">)</span>
<span class="nf">stream</span><span class="p">(...)</span> <span class="p">{}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">SetMetadata(key, value)</code> : 클래스/메서드에 메타데이터를 심는다</li>
  <li><code class="language-plaintext highlighter-rouge">Reflector.getAllAndOverride(key, [handler, class])</code> : 메서드 → 클래스 순서로 메타데이터를 읽는다</li>
  <li>Guard에서 이 값을 읽어 분기처리</li>
</ul>

<h2 id="validateuser---파라미터-데코레이터">@ValidateUser() - 파라미터 데코레이터</h2>

<p>요청 컨텍스트에서 JWT 검증 후 주입된 유저 객체를 꺼내는 파라미터 데코레이터.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 사용</span>
<span class="p">@</span><span class="nd">Post</span><span class="p">()</span>
<span class="k">async</span> <span class="nf">uploadDocs</span><span class="p">(@</span><span class="nd">ValidateUser</span><span class="p">()</span> <span class="nx">user</span><span class="p">:</span> <span class="nx">User</span><span class="p">,</span> <span class="p">...)</span> <span class="p">{</span> <span class="p">...</span> <span class="p">}</span>
</code></pre></div></div>

<hr />

<h1 id="jwt-인증-흐름">JWT 인증 흐름</h1>

<h2 id="jwtstrategy---passport-전략">JwtStrategy - Passport 전략</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Injectable</span><span class="p">()</span>
<span class="k">export</span> <span class="kd">class</span> <span class="nc">JwtStrategy</span> <span class="kd">extends</span> <span class="nc">PassportStrategy</span><span class="p">(</span><span class="nx">Strategy</span><span class="p">)</span> <span class="p">{</span>
  <span class="nf">constructor</span><span class="p">(</span><span class="nx">configService</span><span class="p">:</span> <span class="nx">ConfigService</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">super</span><span class="p">({</span>
      <span class="na">jwtFromRequest</span><span class="p">:</span> <span class="nx">ExtractJwt</span><span class="p">.</span><span class="nf">fromAuthHeaderAsBearerToken</span><span class="p">(),</span> <span class="c1">// Authorization: Bearer ...</span>
      <span class="na">ignoreExpiration</span><span class="p">:</span> <span class="kc">false</span><span class="p">,</span>
      <span class="na">secretOrKey</span><span class="p">:</span> <span class="nx">configService</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="dl">'</span><span class="s1">JWT_SECRET</span><span class="dl">'</span><span class="p">),</span>
    <span class="p">});</span>
  <span class="p">}</span>

  <span class="k">async</span> <span class="nf">validate</span><span class="p">(</span><span class="nx">payload</span><span class="p">:</span> <span class="nx">JwtPayload</span><span class="p">):</span> <span class="nb">Promise</span><span class="o">&lt;</span><span class="nb">Partial</span><span class="o">&lt;</span><span class="nx">User</span><span class="o">&gt;&gt;</span> <span class="p">{</span>
    <span class="c1">// stateless: DB 조회 없이 토큰 payload만으로 유저 정보 반환</span>
    <span class="k">return</span> <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="nx">payload</span><span class="p">.</span><span class="nx">sub</span><span class="p">,</span> <span class="na">email</span><span class="p">:</span> <span class="nx">payload</span><span class="p">.</span><span class="nx">email</span><span class="p">,</span> <span class="na">role</span><span class="p">:</span> <span class="nx">payload</span><span class="p">.</span><span class="nx">role</span> <span class="p">};</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="authmodule---비동기-설정-패턴">AuthModule - 비동기 설정 패턴</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">JwtModule</span><span class="p">.</span><span class="nf">registerAsync</span><span class="p">({</span>
  <span class="na">imports</span><span class="p">:</span> <span class="p">[</span><span class="nx">ConfigModule</span><span class="p">],</span>
  <span class="na">inject</span><span class="p">:</span> <span class="p">[</span><span class="nx">ConfigService</span><span class="p">],</span>
  <span class="na">useFactory</span><span class="p">:</span> <span class="k">async </span><span class="p">(</span><span class="na">configService</span><span class="p">:</span> <span class="nx">ConfigService</span><span class="p">):</span> <span class="nb">Promise</span><span class="o">&lt;</span><span class="nx">JwtModuleOptions</span><span class="o">&gt;</span> <span class="o">=&gt;</span> <span class="p">({</span>
    <span class="na">secret</span><span class="p">:</span> <span class="nx">configService</span><span class="p">.</span><span class="kd">get</span><span class="o">&lt;</span><span class="kr">string</span><span class="o">&gt;</span><span class="p">(</span><span class="dl">'</span><span class="s1">JWT_SECRET</span><span class="dl">'</span><span class="p">),</span>
    <span class="na">signOptions</span><span class="p">:</span> <span class="p">{</span> <span class="na">expiresIn</span><span class="p">:</span> <span class="nc">Number</span><span class="p">(</span><span class="nx">configService</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="dl">'</span><span class="s1">JWT_EXPIRATION</span><span class="dl">'</span><span class="p">))</span> <span class="p">},</span>
  <span class="p">}),</span>
<span class="p">})</span>
</code></pre></div></div>

<blockquote>
  <p>[!warning] 타입 단언 주의
<code class="language-plaintext highlighter-rouge">configService.get&lt;string&gt;('KEY')</code> 의 제네릭은 <strong>형변환이 아닌 타입 단언</strong>이다.
실제로 <code class="language-plaintext highlighter-rouge">string</code>이 아닌 값이 들어와도 런타임 에러가 발생하지 않는다.
확실한 검증이 필요하면 <strong>Joi</strong> 로 환경변수 스키마를 검증해야 한다.</p>
</blockquote>

<hr />

<h1 id="sse-server-sent-events--rxjs">SSE (Server-Sent Events) + RxJS</h1>

<p>채팅 스트림은 <code class="language-plaintext highlighter-rouge">@Sse</code> 데코레이터와 RxJS <code class="language-plaintext highlighter-rouge">Observable</code>로 구현되어 있다.</p>

<h2 id="chatcontroller---sse-엔드포인트">ChatController - SSE 엔드포인트</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Sse</span><span class="p">(</span><span class="dl">'</span><span class="s1">stream/:sessionId</span><span class="dl">'</span><span class="p">)</span>
<span class="p">@</span><span class="nd">SkipThrottle</span><span class="p">()</span>            <span class="c1">// Rate Limit 우회 (스트리밍은 장기 연결)</span>
<span class="p">@</span><span class="nd">Header</span><span class="p">(</span><span class="dl">'</span><span class="s1">X-Accel-Buffering</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">no</span><span class="dl">'</span><span class="p">)</span>  <span class="c1">// Nginx 버퍼링 비활성화</span>
<span class="nf">stream</span><span class="p">(@</span><span class="nd">Param</span><span class="p">(</span><span class="dl">'</span><span class="s1">sessionId</span><span class="dl">'</span><span class="p">)</span> <span class="nx">sessionId</span><span class="p">:</span> <span class="kr">string</span><span class="p">):</span> <span class="nx">Observable</span><span class="o">&lt;</span><span class="nx">MessageEvent</span><span class="o">&lt;</span><span class="kr">any</span><span class="o">&gt;&gt;</span> <span class="p">{</span>

  <span class="kd">const</span> <span class="nx">userStreamSubject</span> <span class="o">=</span> <span class="k">this</span><span class="p">.</span><span class="nx">chatService</span><span class="p">.</span><span class="nf">addClient</span><span class="p">(</span><span class="nx">uuid</span><span class="p">,</span> <span class="nx">sessionId</span><span class="p">);</span>

  <span class="kd">const</span> <span class="nx">initEvent</span> <span class="o">=</span> <span class="k">from</span><span class="p">([{</span> <span class="na">data</span><span class="p">:</span> <span class="p">{</span> <span class="na">type</span><span class="p">:</span> <span class="dl">'</span><span class="s1">init</span><span class="dl">'</span><span class="p">,</span> <span class="nx">uuid</span><span class="p">,</span> <span class="p">...</span> <span class="p">}</span> <span class="p">}]);</span>      <span class="c1">// 연결 즉시 전송</span>
  <span class="kd">const</span> <span class="nx">heartbeatEvent</span> <span class="o">=</span> <span class="nf">interval</span><span class="p">(</span><span class="mi">5000</span><span class="p">).</span><span class="nf">pipe</span><span class="p">(</span><span class="nf">map</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">({</span> <span class="na">data</span><span class="p">:</span> <span class="p">{</span> <span class="na">type</span><span class="p">:</span> <span class="dl">'</span><span class="s1">heartbeat</span><span class="dl">'</span> <span class="p">}</span> <span class="p">})));</span>
  <span class="kd">const</span> <span class="nx">messageStream</span> <span class="o">=</span> <span class="nx">userStreamSubject</span><span class="p">.</span><span class="nf">asObservable</span><span class="p">().</span><span class="nf">pipe</span><span class="p">(</span><span class="nf">map</span><span class="p">(</span><span class="nx">payload</span> <span class="o">=&gt;</span> <span class="p">({</span> <span class="na">data</span><span class="p">:</span> <span class="nx">payload</span> <span class="p">})));</span>

  <span class="k">return</span> <span class="nf">merge</span><span class="p">(</span><span class="nx">initEvent</span><span class="p">,</span> <span class="nx">heartbeatEvent</span><span class="p">,</span> <span class="nx">messageStream</span><span class="p">).</span><span class="nf">pipe</span><span class="p">(</span>
    <span class="nf">finalize</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="k">this</span><span class="p">.</span><span class="nx">chatService</span><span class="p">.</span><span class="nf">removeClient</span><span class="p">(</span><span class="nx">uuid</span><span class="p">,</span> <span class="nx">sessionId</span><span class="p">))</span>  <span class="c1">// 연결 종료 시 정리</span>
  <span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="chatservice---redis-pubsub-연동">ChatService - Redis Pub/Sub 연동</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Redis 구독자와 발행자는 별도 커넥션을 사용해야 함</span>
<span class="nf">constructor</span><span class="p">(@</span><span class="nd">Inject</span><span class="p">(</span><span class="nx">REDIS_CLIENT</span><span class="p">)</span> <span class="k">private</span> <span class="k">readonly</span> <span class="nx">redisPublisher</span><span class="p">:</span> <span class="nx">Redis</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">this</span><span class="p">.</span><span class="nx">redisSubscriber</span> <span class="o">=</span> <span class="nx">redisPublisher</span><span class="p">.</span><span class="nf">duplicate</span><span class="p">();</span>  <span class="c1">// 커넥션 복제</span>
<span class="p">}</span>

<span class="k">async</span> <span class="nf">onModuleInit</span><span class="p">()</span> <span class="p">{</span>
  <span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">redisSubscriber</span><span class="p">.</span><span class="nf">psubscribe</span><span class="p">(</span><span class="dl">'</span><span class="s1">chat:stream:*</span><span class="dl">'</span><span class="p">);</span>  <span class="c1">// 패턴 구독</span>
  <span class="k">this</span><span class="p">.</span><span class="nx">redisSubscriber</span><span class="p">.</span><span class="nf">on</span><span class="p">(</span><span class="dl">'</span><span class="s1">pmessage</span><span class="dl">'</span><span class="p">,</span> <span class="p">(</span><span class="nx">pattern</span><span class="p">,</span> <span class="nx">channel</span><span class="p">,</span> <span class="nx">message</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="k">this</span><span class="p">.</span><span class="nf">routeMessageToUser</span><span class="p">(</span><span class="nx">channel</span><span class="p">,</span> <span class="nx">message</span><span class="p">);</span>  <span class="c1">// 해당 유저의 Subject에 push</span>
  <span class="p">});</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>전체 흐름</strong>:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client SSE연결 → Subject 생성 → Redis 구독
→ POST /message → Redis에 Job 적재
→ FastAPI 워커가 처리 후 Redis Publish
→ NestJS가 수신 → Subject.next() → SSE 전송
</code></pre></div></div>

<hr />

<h1 id="ai-서비스-상태-모니터링-서킷-브레이커">AI 서비스 상태 모니터링 (서킷 브레이커)</h1>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Injectable</span><span class="p">()</span>
<span class="k">export</span> <span class="kd">class</span> <span class="nc">AiStatusMonitoringService</span> <span class="k">implements</span> <span class="nx">OnModuleInit</span> <span class="p">{</span>
  <span class="k">private</span> <span class="nx">isAiAvailable</span><span class="p">:</span> <span class="nx">boolean</span> <span class="o">=</span> <span class="kc">false</span><span class="p">;</span>

  <span class="p">@</span><span class="nd">Cron</span><span class="p">(</span><span class="dl">'</span><span class="s1">*/1 * * * * *</span><span class="dl">'</span><span class="p">)</span>   <span class="c1">// 1초마다 실행</span>
  <span class="k">async</span> <span class="nf">syncStatus</span><span class="p">()</span> <span class="p">{</span>
    <span class="kd">const</span> <span class="nx">threshold</span> <span class="o">=</span> <span class="nb">Date</span><span class="p">.</span><span class="nf">now</span><span class="p">()</span> <span class="o">/</span> <span class="mi">1000</span> <span class="o">-</span> <span class="mi">5</span><span class="p">;</span>  <span class="c1">// 5초 이내 heartbeat만 유효</span>

    <span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">redis</span><span class="p">.</span><span class="nf">zremrangebyscore</span><span class="p">(</span><span class="dl">'</span><span class="s1">cluster:heartbeats</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">-inf</span><span class="dl">'</span><span class="p">,</span> <span class="nx">threshold</span><span class="p">);</span>  <span class="c1">// 좀비 제거</span>
    <span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">redis</span><span class="p">.</span><span class="nf">zcard</span><span class="p">(</span><span class="dl">'</span><span class="s1">cluster:heartbeats</span><span class="dl">'</span><span class="p">);</span>   <span class="c1">// 생존 서버 수 확인</span>
    <span class="k">this</span><span class="p">.</span><span class="nx">isAiAvailable</span> <span class="o">=</span> <span class="nx">count</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<blockquote>
  <p>[!warning] Redis Timeout 설정 필요
ioredis 기본 설정은 명령어 타임아웃이 <code class="language-plaintext highlighter-rouge">undefined</code>. Redis가 죽으면 <code class="language-plaintext highlighter-rouge">await</code> 이하에서 영구 대기.
크론 작업에서 이가 반복되면 <strong>Promise 스택 누적 → 힙 메모리 고갈 → 이벤트 루프 고갈</strong>로 이어진다.
<code class="language-plaintext highlighter-rouge">redis.module.ts</code>에서 <code class="language-plaintext highlighter-rouge">commandTimeout</code> 옵션으로 해결함.</p>
</blockquote>

<hr />

<h1 id="validationpipe--dto">ValidationPipe &amp; DTO</h1>

<p>요청 바디를 자동으로 검증하고 변환하는 파이프라인.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// main.ts</span>
<span class="nx">app</span><span class="p">.</span><span class="nf">useGlobalPipes</span><span class="p">(</span><span class="k">new</span> <span class="nc">ValidationPipe</span><span class="p">({</span>
  <span class="na">transform</span><span class="p">:</span> <span class="kc">true</span><span class="p">,</span>    <span class="c1">// 요청 데이터를 DTO 클래스 인스턴스로 자동 변환</span>
  <span class="na">whitelist</span><span class="p">:</span> <span class="kc">true</span><span class="p">,</span>    <span class="c1">// DTO에 없는 필드는 자동 제거 (보안)</span>
<span class="p">}));</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">class-validator</code> 데코레이터 (<code class="language-plaintext highlighter-rouge">@IsString()</code>, <code class="language-plaintext highlighter-rouge">@IsEmail()</code> 등)로 DTO에서 검증 선언</li>
  <li><code class="language-plaintext highlighter-rouge">class-transformer</code>가 JSON → 클래스 인스턴스 변환</li>
  <li><code class="language-plaintext highlighter-rouge">whitelist: true</code> 로 DTO에 정의되지 않은 필드를 자동으로 제거 → <strong>의도치 않은 필드 주입 방지</strong></li>
</ul>

<hr />

<h1 id="lifecycle-hooks">Lifecycle Hooks</h1>

<p>모듈/서비스의 초기화·종료 시점을 후킹하는 인터페이스.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Injectable</span><span class="p">()</span>
<span class="k">export</span> <span class="kd">class</span> <span class="nc">ChatService</span> <span class="k">implements</span> <span class="nx">OnModuleInit</span><span class="p">,</span> <span class="nx">OnModuleDestroy</span> <span class="p">{</span>
  <span class="k">async</span> <span class="nf">onModuleInit</span><span class="p">()</span> <span class="p">{</span>
    <span class="c1">// NestJS 앱 완전 시작 직후 실행</span>
    <span class="k">await</span> <span class="k">this</span><span class="p">.</span><span class="nx">redisSubscriber</span><span class="p">.</span><span class="nf">psubscribe</span><span class="p">(</span><span class="dl">'</span><span class="s1">chat:stream:*</span><span class="dl">'</span><span class="p">);</span>
  <span class="p">}</span>

  <span class="nf">onModuleDestroy</span><span class="p">()</span> <span class="p">{</span>
    <span class="c1">// 앱 종료 직전 실행 (Graceful Shutdown)</span>
    <span class="k">this</span><span class="p">.</span><span class="nx">redisSubscriber</span><span class="p">.</span><span class="nf">quit</span><span class="p">();</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">main.ts</code>의 <code class="language-plaintext highlighter-rouge">app.enableShutdownHooks()</code> 와 함께 사용하면 <strong>Docker 종료 시 연결된 소켓이 안전하게 닫힌다</strong>.</p>

<hr />

<h1 id="인메모리-동시성-제어-큐-queueservice">인메모리 동시성 제어 큐 (QueueService)</h1>

<p>지식 업로드 같은 무거운 작업의 동시 처리 수를 제한하는 <strong>직접 구현 큐</strong> 했다. 이러한 설계는 AI 를 위한 RAG 의 실시간 처리에서 과포화 상태를 만들거나 하여 서비스 안정성을 해치는 것을 막기 위해서다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">@</span><span class="nd">Injectable</span><span class="p">()</span>
<span class="k">export</span> <span class="kd">class</span> <span class="nc">QueueService</span> <span class="p">{</span>
  <span class="k">private</span> <span class="k">readonly</span> <span class="nx">concurrency</span> <span class="o">=</span> <span class="nx">CONSTANTS</span><span class="p">.</span><span class="nx">CONCURRENCY</span><span class="p">;</span>   <span class="c1">// 최대 동시 실행 수</span>
  <span class="k">private</span> <span class="k">readonly</span> <span class="nx">maxPending</span> <span class="o">=</span> <span class="nx">CONSTANTS</span><span class="p">.</span><span class="nx">MAX_PENDING</span><span class="p">;</span>    <span class="c1">// 최대 대기 수</span>
  <span class="k">private</span> <span class="k">readonly</span> <span class="nx">queue</span><span class="p">:</span> <span class="nx">Task</span><span class="p">[]</span> <span class="o">=</span> <span class="p">[];</span>
  <span class="k">private</span> <span class="nx">activeCount</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

  <span class="k">public</span> <span class="nx">add</span><span class="o">&lt;</span><span class="nx">T</span><span class="o">&gt;</span><span class="p">(</span><span class="nx">task</span><span class="p">:</span> <span class="nx">Task</span><span class="o">&lt;</span><span class="nx">T</span><span class="o">&gt;</span><span class="p">):</span> <span class="nb">Promise</span><span class="o">&lt;</span><span class="nx">T</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="k">return</span> <span class="k">new</span> <span class="nc">Promise</span><span class="p">((</span><span class="nx">resolve</span><span class="p">,</span> <span class="nx">reject</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
      <span class="k">if </span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">queue</span><span class="p">.</span><span class="nx">length</span> <span class="o">&gt;=</span> <span class="k">this</span><span class="p">.</span><span class="nx">maxPending</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">return</span> <span class="nf">reject</span><span class="p">(</span><span class="k">new</span> <span class="nc">Error</span><span class="p">(</span><span class="dl">'</span><span class="s1">System queue is full</span><span class="dl">'</span><span class="p">));</span>  <span class="c1">// 큐가 꽉 찼으면 즉시 거절</span>
      <span class="p">}</span>

      <span class="kd">const</span> <span class="nx">wrappedTask</span> <span class="o">=</span> <span class="k">async </span><span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
        <span class="k">this</span><span class="p">.</span><span class="nx">activeCount</span><span class="o">++</span><span class="p">;</span>
        <span class="k">try</span> <span class="p">{</span> <span class="nf">resolve</span><span class="p">(</span><span class="k">await</span> <span class="nf">task</span><span class="p">());</span> <span class="p">}</span>
        <span class="k">catch </span><span class="p">(</span><span class="nx">error</span><span class="p">)</span> <span class="p">{</span> <span class="nf">reject</span><span class="p">(</span><span class="nx">error</span><span class="p">);</span> <span class="p">}</span>
        <span class="k">finally</span> <span class="p">{</span> <span class="k">this</span><span class="p">.</span><span class="nx">activeCount</span><span class="o">--</span><span class="p">;</span> <span class="k">this</span><span class="p">.</span><span class="nf">next</span><span class="p">();</span> <span class="p">}</span>  <span class="c1">// 작업 완료 후 다음 큐 실행</span>
      <span class="p">};</span>

      <span class="k">this</span><span class="p">.</span><span class="nx">queue</span><span class="p">.</span><span class="nf">push</span><span class="p">(</span><span class="nx">wrappedTask</span><span class="p">);</span>
      <span class="k">if </span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">activeCount</span> <span class="o">&lt;</span> <span class="k">this</span><span class="p">.</span><span class="nx">concurrency</span><span class="p">)</span> <span class="k">this</span><span class="p">.</span><span class="nf">next</span><span class="p">();</span>
    <span class="p">});</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>동작 원리</strong>:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">concurrency</code>: 동시에 실행 중인 작업 수 상한</li>
  <li><code class="language-plaintext highlighter-rouge">maxPending</code>: 대기열 최대 크기, 초과 시 즉시 거절(Back Pressure)</li>
  <li>Redis Throttler와는 다른 계층 - 이건 <strong>서버 내부 처리 리소스 보호</strong>가 목적</li>
</ul>

<hr />

<h1 id="maints-글로벌-설정-요약">main.ts 글로벌 설정 요약</h1>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="kd">function</span> <span class="nf">bootstrap</span><span class="p">()</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">app</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">NestFactory</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span><span class="nx">AppModule</span><span class="p">,</span> <span class="p">{</span>
    <span class="na">logger</span><span class="p">:</span> <span class="nx">WinstonModule</span><span class="p">.</span><span class="nf">createLogger</span><span class="p">(...),</span>  <span class="c1">// Winston으로 로거 교체</span>
    <span class="na">bodyParser</span><span class="p">:</span> <span class="kc">false</span><span class="p">,</span>                         <span class="c1">// express json/urlencoded 직접 설정 (50mb 제한)</span>
  <span class="p">});</span>

  <span class="nx">app</span><span class="p">.</span><span class="nf">enableCors</span><span class="p">({</span> <span class="na">origin</span><span class="p">:</span> <span class="nx">staticWhitelist</span><span class="p">,</span> <span class="na">credentials</span><span class="p">:</span> <span class="kc">true</span> <span class="p">});</span>

  <span class="nx">app</span><span class="p">.</span><span class="nf">useGlobalFilters</span><span class="p">(</span><span class="k">new</span> <span class="nc">GlobalExceptionFilter</span><span class="p">());</span>
  <span class="nx">app</span><span class="p">.</span><span class="nf">useGlobalInterceptors</span><span class="p">(</span><span class="k">new</span> <span class="nc">LoggingInterceptor</span><span class="p">());</span>
  <span class="nx">app</span><span class="p">.</span><span class="nf">enableShutdownHooks</span><span class="p">();</span>
  <span class="nx">app</span><span class="p">.</span><span class="nf">useGlobalGuards</span><span class="p">(</span><span class="k">new</span> <span class="nc">OriginGuard</span><span class="p">());</span>
  <span class="nx">app</span><span class="p">.</span><span class="nf">useGlobalPipes</span><span class="p">(</span><span class="k">new</span> <span class="nc">ValidationPipe</span><span class="p">({</span> <span class="na">transform</span><span class="p">:</span> <span class="kc">true</span><span class="p">,</span> <span class="na">whitelist</span><span class="p">:</span> <span class="kc">true</span> <span class="p">}));</span>
  <span class="nx">app</span><span class="p">.</span><span class="nf">useGlobalInterceptors</span><span class="p">(</span><span class="k">new</span> <span class="nc">ClassSerializerInterceptor</span><span class="p">(</span><span class="nx">app</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="nx">Reflector</span><span class="p">)));</span>

  <span class="k">await</span> <span class="nx">app</span><span class="p">.</span><span class="nf">listen</span><span class="p">(</span><span class="nx">process</span><span class="p">.</span><span class="nx">env</span><span class="p">.</span><span class="nx">PORT</span> <span class="o">??</span> <span class="mi">3000</span><span class="p">,</span> <span class="dl">'</span><span class="s1">0.0.0.0</span><span class="dl">'</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<blockquote>
  <p>[!note] <code class="language-plaintext highlighter-rouge">ClassSerializerInterceptor</code>
<code class="language-plaintext highlighter-rouge">class-transformer</code>의 <code class="language-plaintext highlighter-rouge">@Exclude()</code>, <code class="language-plaintext highlighter-rouge">@Expose()</code> 같은 데코레이터를 응답 직렬화 시 자동 적용.
예: 비밀번호 필드를 DTO에서 <code class="language-plaintext highlighter-rouge">@Exclude()</code> 하면 응답에서 자동으로 제거됨.</p>
</blockquote>]]></content><author><name>Paul2021-R</name></author><category term="문제해결" /><category term="Backend" /><category term="개발" /><category term="Protostar" /><category term="Project" /><category term="Review" /><category term="NestJS" /><category term="AI" /><summary type="html"><![CDATA[NestJS 개념 정리 본 정리 내용은, 프레임워크에 대한 이해도를 복기 하면서도 짜게 되었던 구성요소들을 더 심화시켜 바라보고 풀스택을 하다보니 놓치거나 좀더 좋은 코드를 만들기 위해 필요한 개념 복습, 코드 구성에 대한 고민을 위한 추가 복습이다.]]></summary></entry><entry><title type="html">Protostar review note - 07 NestJS main.ts &amp;amp; app.module.ts</title><link href="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/18/01-protostar-review.html" rel="alternate" type="text/html" title="Protostar review note - 07 NestJS main.ts &amp;amp; app.module.ts" /><published>2026-02-18T00:00:00+00:00</published><updated>2026-02-18T00:00:00+00:00</updated><id>http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/18/01-protostar-review</id><content type="html" xml:base="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/18/01-protostar-review.html"><![CDATA[<h1 id="nestjs-maints--appmodulets-분석">NestJS <code class="language-plaintext highlighter-rouge">main.ts</code> / <code class="language-plaintext highlighter-rouge">app.module.ts</code> 분석</h1>

<blockquote>
  <p>두 파일은 NestJS 앱의 <strong>조립 지점</strong>이다.</p>
  <ul>
    <li><code class="language-plaintext highlighter-rouge">app.module.ts</code>: 무엇을 연결할지 선언 (정적 구조)</li>
    <li><code class="language-plaintext highlighter-rouge">main.ts</code>: 어떻게 실행할지 설정 (동적 부트스트랩)</li>
  </ul>
</blockquote>

<hr />

<h1 id="1-주요-개념">1. 주요 개념</h1>

<h2 id="maints에-등장하는-핵심-개념"><code class="language-plaintext highlighter-rouge">main.ts</code>에 등장하는 핵심 개념</h2>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>사용처</th>
      <th>한 줄 설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**Bootstrap 함수**</td>
      <td><code class="language-plaintext highlighter-rouge">NestFactory.create()</code></td>
      <td>NestJS 앱 인스턴스를 생성하는 진입점</td>
    </tr>
    <tr>
      <td>**글로벌 파이프라인**</td>
      <td><code class="language-plaintext highlighter-rouge">useGlobal*</code> 메서드들</td>
      <td>모든 요청에 공통 적용되는 필터/인터셉터/가드/파이프</td>
    </tr>
    <tr>
      <td>**CORS**</td>
      <td><code class="language-plaintext highlighter-rouge">enableCors()</code></td>
      <td>교차 출처 요청 허용 범위 설정. 특히 메인 프론트엔드 서비스 포인트와, AI 챗봇의 엔드포인트 등에서 차이가 있다보니 신중한 개념 이해 및 적용이 필요하다.</td>
    </tr>
    <tr>
      <td>**Graceful Shutdown**</td>
      <td><code class="language-plaintext highlighter-rouge">enableShutdownHooks()</code></td>
      <td>OS 종료 신호(SIGTERM) 수신 시 연결 정리 후 종료. 고가용성을 위한 기본 중에 기본 개념. 컨테이너화 하기 전, 반드시 필요한 처리였다.</td>
    </tr>
    <tr>
      <td>**Winston 로거**</td>
      <td><code class="language-plaintext highlighter-rouge">WinstonModule.createLogger()</code></td>
      <td>NestJS 기본 로거를 Winston으로 교체 및 Loki 대응으로 변경.</td>
    </tr>
    <tr>
      <td>**bodyParser 직접 설정**</td>
      <td><code class="language-plaintext highlighter-rouge">json({ limit: '50mb' })</code></td>
      <td>파일 업로드 대응을 위한 요청 바디 크기 제한 확장(기본값으로 부족함 대비)</td>
    </tr>
  </tbody>
</table>

<h2 id="appmodulets에-등장하는-핵심-개념"><code class="language-plaintext highlighter-rouge">app.module.ts</code>에 등장하는 핵심 개념</h2>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>사용처</th>
      <th>한 줄 설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**루트 모듈**</td>
      <td><code class="language-plaintext highlighter-rouge">AppModule</code></td>
      <td>모든 기능 모듈을 조립하는 최상위 모듈</td>
    </tr>
    <tr>
      <td>**isGlobal**</td>
      <td><code class="language-plaintext highlighter-rouge">ConfigModule.forRoot({ isGlobal: true })</code></td>
      <td>다른 모듈에서 import 없이 ConfigService 사용 가능</td>
    </tr>
    <tr>
      <td>**forRootAsync**</td>
      <td><code class="language-plaintext highlighter-rouge">ThrottlerModule.forRootAsync()</code></td>
      <td>비동기로 다른 모듈(Redis)을 주입받아 설정</td>
    </tr>
    <tr>
      <td>**APP_GUARD 토큰**</td>
      <td><code class="language-plaintext highlighter-rouge">{ provide: APP_GUARD, useClass: ... }</code></td>
      <td>DI 컨테이너를 통해 전역 가드를 등록하는 NestJS 특수 토큰</td>
    </tr>
    <tr>
      <td>**Rate Limiting**</td>
      <td><code class="language-plaintext highlighter-rouge">ThrottlerModule</code></td>
      <td>burst/sustained 두 레벨의 요청 수 제한</td>
    </tr>
    <tr>
      <td>**Redis 기반 Throttler**</td>
      <td><code class="language-plaintext highlighter-rouge">ThrottlerStorageRedisService</code></td>
      <td>다중 인스턴스 환경에서 Rate Limit 상태를 Redis로 공유</td>
    </tr>
  </tbody>
</table>

<hr />

<h1 id="2-핵심-로직-흐름">2. 핵심 로직 흐름</h1>

<h2 id="maints---부트스트랩-순서"><code class="language-plaintext highlighter-rouge">main.ts</code> - 부트스트랩 순서</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NestFactory.create(AppModule, { logger: Winston, bodyParser: false })
  │
  ├─ CORS 설정 (Origin 화이트리스트 검사 콜백)
  │
  ├─ app.use(json({ limit: '50mb' }))        ← bodyParser 수동 설정
  ├─ app.use(urlencoded({ extended: true, limit: '50mb' }))
  │
  ├─ useGlobalFilters(GlobalExceptionFilter)   ← 예외 후처리
  ├─ useGlobalInterceptors(LoggingInterceptor) ← 요청/응답 로깅
  │
  ├─ enableShutdownHooks()                     ← SIGTERM 처리
  │
  ├─ useGlobalGuards(OriginGuard)              ← Origin 재검사
  ├─ useGlobalPipes(ValidationPipe)            ← DTO 검증/변환
  ├─ useGlobalInterceptors(ClassSerializerInterceptor) ← 직렬화
  │
  └─ app.listen(PORT, '0.0.0.0')
</code></pre></div></div>

<blockquote>
  <p>[!warning] 등록 순서가 실행 순서를 결정하지 않는다
<code class="language-plaintext highlighter-rouge">useGlobal*</code> 호출 순서는 파이프라인 실행 순서와 무관하다.
NestJS는 Lifecycle에 따라 <strong>Guard → Interceptor → Pipe → Controller → Interceptor → Filter</strong> 순서를 고정으로 실행한다.
<code class="language-plaintext highlighter-rouge">main.ts</code>의 등록 순서는 가독성을 위한 선언일 뿐이다.
하지만 그렇기 때문에 명시적으로 순서를 이해하고, 선언해둠은 요청이 어떻게 응답으로 나가는지를 유기적으로 이해할 수 있다.</p>
</blockquote>

<h2 id="appmodulets---모듈-조립-구조"><code class="language-plaintext highlighter-rouge">app.module.ts</code> - 모듈 조립 구조</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>AppModule
  │
  ├── imports
  │     ├── ConfigModule (isGlobal: true)      ─ 환경변수 전역 공급
  │     ├── RedisModule                        ─ Redis 클라이언트 공급
  │     ├── ChatModule                         ─ SSE 채팅 도메인
  │     ├── ThrottlerModule.forRootAsync()     ─ Redis로 상태 공유하는 Rate Limit
  │     │     └── inject: [REDIS_CLIENT]       ─ RedisModule에서 공급된 인스턴스 주입
  │     ├── ObjectStorageModule                ─ MinIO 연동
  │     ├── AuthModule                         ─ JWT 인증
  │     ├── PrismaModule                       ─ DB ORM
  │     └── KnowledgeModule                   ─ 문서 업로드/RAG 도메인
  │
  └── providers
        ├── AppService
        ├── APP_GUARD → ThrottlerGuard         ─ 전역 Rate Limit (1순위)
        └── APP_GUARD → JwtAuthGuard           ─ 전역 JWT 인증 (2순위)
</code></pre></div></div>

<h2 id="rate-limit-설정-상세">Rate Limit 설정 상세</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">throttlers</span><span class="p">:</span> <span class="p">[</span>
  <span class="p">{</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">burst</span><span class="dl">'</span><span class="p">,</span>     <span class="na">ttl</span><span class="p">:</span> <span class="nf">seconds</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span>  <span class="na">limit</span><span class="p">:</span> <span class="mi">2000</span>  <span class="p">},</span>  <span class="c1">// 초당 2000 req</span>
  <span class="p">{</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">sustained</span><span class="dl">'</span><span class="p">,</span> <span class="na">ttl</span><span class="p">:</span> <span class="nf">seconds</span><span class="p">(</span><span class="mi">60</span><span class="p">),</span> <span class="na">limit</span><span class="p">:</span> <span class="mi">60000</span> <span class="p">},</span>  <span class="c1">// 분당 60000 req</span>
<span class="p">]</span>
</code></pre></div></div>

<ul>
  <li><strong>burst</strong>: 순간적인 트래픽 스파이크 허용 (초당 2000)</li>
  <li><strong>sustained</strong>: 지속적 과부하 방지 (분당 60000 = 초당 평균 1000)</li>
  <li>두 조건 모두 통과해야 요청이 허용됨</li>
  <li>상태를 <code class="language-plaintext highlighter-rouge">ThrottlerStorageRedisService</code>로 Redis에 저장 방식을 적용 → <strong>수평 확장(Scale-out) 시에도 공유 가능</strong> Redis 이용 면에서 심도있게 다룰 예정</li>
</ul>

<hr />

<h1 id="3-구조적-취약점--개선-방향">3. 구조적 취약점 / 개선 방향</h1>

<h2 id="maints-cors-화이트리스트의-하드코딩">[main.ts] CORS 화이트리스트의 하드코딩</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">staticWhitelist</span> <span class="o">=</span> <span class="p">[</span>
  <span class="dl">'</span><span class="s1">https://paul2021-r.github.io</span><span class="dl">'</span><span class="p">,</span>
  <span class="dl">'</span><span class="s1">https://service-protostar.ddns.net</span><span class="dl">'</span><span class="p">,</span>
  <span class="dl">'</span><span class="s1">http://localhost:4000</span><span class="dl">'</span><span class="p">,</span>
  <span class="p">...</span>
<span class="p">];</span>
</code></pre></div></div>

<p><strong>문제</strong>: 허용 도메인이 코드에 박혀 있어 도메인 추가/변경 시 재배포 필요. 개발 시 가장 이상적인 형태는 ‘dynamic’한 관리가 맞다고 판단했었음. 하지만 일정 및 기획 축소로 하드코딩 방식으로 진행함.</p>

<p><strong>개선</strong>: 환경변수로 분리. 이후 DB 형태로 관리가 fine 한 관리가 필요.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">whitelist</span> <span class="o">=</span> <span class="nx">process</span><span class="p">.</span><span class="nx">env</span><span class="p">.</span><span class="nx">CORS_ORIGINS</span><span class="p">?.</span><span class="nf">split</span><span class="p">(</span><span class="dl">'</span><span class="s1">,</span><span class="dl">'</span><span class="p">)</span> <span class="o">??</span> <span class="p">[];</span>
</code></pre></div></div>

<h2 id="maints-전역-originguard와-cors의-중복-검사">[main.ts] 전역 OriginGuard와 CORS의 중복 검사</h2>

<ul>
  <li><code class="language-plaintext highlighter-rouge">enableCors()</code> 에서 한 번, <code class="language-plaintext highlighter-rouge">OriginGuard</code> 에서 한 번 더 Origin을 검사하고 있다.</li>
  <li>Origin 검사 로직을 한 곳으로 통합하거나, 역할을 명확히 분리(CORS = 브라우저 사전 검사, Guard = 서버 최종 검사)하는 것이 의도를 더 명확하게 만든다.</li>
</ul>

<h2 id="appmodulets-app_guard-등록-순서-의존성">[app.module.ts] APP_GUARD 등록 순서 의존성</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">providers</span><span class="p">:</span> <span class="p">[</span>
  <span class="p">{</span> <span class="na">provide</span><span class="p">:</span> <span class="nx">APP_GUARD</span><span class="p">,</span> <span class="na">useClass</span><span class="p">:</span> <span class="nx">ThrottlerGuard</span> <span class="p">},</span>  <span class="c1">// 1번</span>
  <span class="p">{</span> <span class="na">provide</span><span class="p">:</span> <span class="nx">APP_GUARD</span><span class="p">,</span> <span class="na">useClass</span><span class="p">:</span> <span class="nx">JwtAuthGuard</span> <span class="p">},</span>    <span class="c1">// 2번</span>
<span class="p">]</span>
</code></pre></div></div>

<ul>
  <li>NestJS는 <code class="language-plaintext highlighter-rouge">APP_GUARD</code>를 등록 순서대로 실행한다.</li>
  <li>현재 Rate Limit을 먼저 검사하는 것은 합리적이지만, <strong>문서화가 없으면 순서 변경 시 버그 발생</strong> 가능성이 있다고 함.</li>
  <li>이에 대해 AI와 답변, Best Practice 를 확인해본 결과 다음과 같은 방향성으로 해결 가능하다.
    <ol>
      <li>주석을 남겨 설명을 명료하게 만들어 둔다.</li>
      <li>하나의 단일한 가드 형태로 가드를 통합화 시켜서 적용시켜둠으로써, 맥락을 유지시킨다.</li>
    </ol>
  </li>
</ul>

<h2 id="appmodulets-redismodule-직접-의존">[app.module.ts] RedisModule 직접 의존</h2>

<ul>
  <li>지적 부분 : <code class="language-plaintext highlighter-rouge">ThrottlerModule</code>이 <code class="language-plaintext highlighter-rouge">RedisModule</code>을 직접 <code class="language-plaintext highlighter-rouge">inject</code>로 받는 구조는, <code class="language-plaintext highlighter-rouge">RedisModule</code>의 토큰(<code class="language-plaintext highlighter-rouge">REDIS_CLIENT</code>)이 바뀌거나 모듈이 분리되면 <code class="language-plaintext highlighter-rouge">AppModule</code>도 수정해야 한다.</li>
  <li>개선 제안 : Throttler 전용 Redis 설정을 <code class="language-plaintext highlighter-rouge">ThrottlerModule</code> 내부로 캡슐화하는 방법도 고려할 수 있다.</li>
</ul>

<h2 id="maints-bodyparser-비활성화-후-수동-설정의-맥락-누락">[main.ts] bodyParser 비활성화 후 수동 설정의 맥락 누락</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">NestFactory</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span><span class="nx">AppModule</span><span class="p">,</span> <span class="p">{</span> <span class="na">bodyParser</span><span class="p">:</span> <span class="kc">false</span> <span class="p">})</span>
<span class="c1">// ...</span>
<span class="nx">app</span><span class="p">.</span><span class="nf">use</span><span class="p">(</span><span class="nf">json</span><span class="p">({</span> <span class="na">limit</span><span class="p">:</span> <span class="dl">'</span><span class="s1">50mb</span><span class="dl">'</span> <span class="p">}));</span>
</code></pre></div></div>

<ul>
  <li>기본 bodyParser를 끄고 수동으로 다는 이유가 코드에 설명되어 있지 않다.</li>
  <li>실제 이유: <strong>파일 업로드 처리(Multer)와 충돌 방지 + 50MB 제한 확장</strong>. 주석이 없으면 다음 개발자가 이유를 모르고 제거할 위험이 있다.
    <ul>
      <li>이 부분은 개선이 필요하였으나, 개발 일정 고려 하드코딩으로 마무리 되었었음. 향후 개선이 필요하고, 특히나 이부분은 요청에 따라 다른 적용 내지는, 파일 확장자 별로도 고려할 사항들이 다소 존재한다.</li>
    </ul>
  </li>
</ul>

<hr />

<h1 id="4-핵심-메서드-및-라이브러리-함수-설명">4. 핵심 메서드 및 라이브러리 함수 설명</h1>

<h2 id="nestfactorycreatemodule-options"><code class="language-plaintext highlighter-rouge">NestFactory.create(module, options)</code></h2>

<p>NestJS 앱 인스턴스를 생성하는 팩토리 메서드.</p>

<table>
  <thead>
    <tr>
      <th>옵션</th>
      <th>타입</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">logger</code></td>
      <td><code class="language-plaintext highlighter-rouge">LoggerService</code></td>
      <td>기본 로거를 교체</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">bodyParser</code></td>
      <td><code class="language-plaintext highlighter-rouge">boolean</code></td>
      <td>Express bodyParser 자동 탑재 여부 (기본 <code class="language-plaintext highlighter-rouge">true</code>)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">cors</code></td>
      <td><code class="language-plaintext highlighter-rouge">boolean \| CorsOptions</code></td>
      <td>CORS 설정 (create 시점에 옵션으로도 가능)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">httpsOptions</code></td>
      <td><code class="language-plaintext highlighter-rouge">HttpsOptions</code></td>
      <td>HTTPS 인증서 설정</td>
    </tr>
  </tbody>
</table>

<h2 id="configmoduleforroot-isglobal-true-"><code class="language-plaintext highlighter-rouge">ConfigModule.forRoot({ isGlobal: true })</code></h2>

<ul>
  <li><code class="language-plaintext highlighter-rouge">isGlobal: true</code>: 이 모듈을 <strong>글로벌 모듈</strong>로 등록. 다른 모듈에서 <code class="language-plaintext highlighter-rouge">imports</code>에 추가 없이 <code class="language-plaintext highlighter-rouge">ConfigService</code>를 바로 주입받을 수 있다.</li>
  <li>내부적으로 <code class="language-plaintext highlighter-rouge">process.env</code>를 래핑해서 타입 안전하게 환경변수를 조회하는 <code class="language-plaintext highlighter-rouge">ConfigService</code>를 제공.</li>
</ul>

<h2 id="throttlermoduleforrootasync-usefactory-"><code class="language-plaintext highlighter-rouge">ThrottlerModule.forRootAsync({ useFactory })</code></h2>

<ul>
  <li><code class="language-plaintext highlighter-rouge">forRoot()</code>: 정적 설정 (값이 고정)</li>
  <li><code class="language-plaintext highlighter-rouge">forRootAsync()</code>: 비동기 설정 팩토리. 다른 Provider(여기서는 Redis 클라이언트)를 <strong>주입받아</strong> 설정값을 만들 때 사용.</li>
  <li><code class="language-plaintext highlighter-rouge">inject</code> 배열에 넣은 토큰들이 <code class="language-plaintext highlighter-rouge">useFactory</code> 함수의 인자로 순서대로 들어온다.</li>
</ul>

<h2 id="app_guard--app_filter--app_interceptor--app_pipe"><code class="language-plaintext highlighter-rouge">APP_GUARD</code> / <code class="language-plaintext highlighter-rouge">APP_FILTER</code> / <code class="language-plaintext highlighter-rouge">APP_INTERCEPTOR</code> / <code class="language-plaintext highlighter-rouge">APP_PIPE</code></h2>

<p>NestJS가 전역 파이프라인 요소를 DI 컨테이너를 통해 등록하기 위한 <strong>특수 토큰들</strong>.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 두 방식의 차이</span>
<span class="c1">// 방식 A: DI 컨테이너 밖 (다른 서비스 주입 불가)</span>
<span class="nx">app</span><span class="p">.</span><span class="nf">useGlobalGuards</span><span class="p">(</span><span class="k">new</span> <span class="nc">OriginGuard</span><span class="p">());</span>

<span class="c1">// 방식 B: DI 컨테이너 안 (다른 서비스 주입 가능)</span>
<span class="p">{</span> <span class="na">provide</span><span class="p">:</span> <span class="nx">APP_GUARD</span><span class="p">,</span> <span class="na">useClass</span><span class="p">:</span> <span class="nx">JwtAuthGuard</span> <span class="p">}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">OriginGuard</code>는 생성자에 주입받을 게 없어서 방식 A로도 괜찮다.</li>
  <li><code class="language-plaintext highlighter-rouge">JwtAuthGuard</code>는 <code class="language-plaintext highlighter-rouge">Reflector</code>를 주입받아야 하므로 <strong>반드시 방식 B</strong>여야 한다.</li>
</ul>

<h2 id="appenableshutdownhooks"><code class="language-plaintext highlighter-rouge">app.enableShutdownHooks()</code></h2>

<ul>
  <li>OS 프로세스 종료 신호(<code class="language-plaintext highlighter-rouge">SIGTERM</code>, <code class="language-plaintext highlighter-rouge">SIGINT</code>)를 NestJS가 감지하도록 활성화.</li>
  <li>신호 수신 시 <code class="language-plaintext highlighter-rouge">OnApplicationShutdown</code> 인터페이스를 구현한 서비스의 <code class="language-plaintext highlighter-rouge">onApplicationShutdown()</code> 메서드를 호출.</li>
  <li>Docker/Kubernetes 환경에서 컨테이너가 내려갈 때 <strong>진행 중인 요청을 마저 처리하고 종료</strong>하는 Graceful Shutdown에 필수.</li>
</ul>

<h2 id="classserializerinterceptor--reflector"><code class="language-plaintext highlighter-rouge">ClassSerializerInterceptor</code> + <code class="language-plaintext highlighter-rouge">Reflector</code></h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">app</span><span class="p">.</span><span class="nf">useGlobalInterceptors</span><span class="p">(</span><span class="k">new</span> <span class="nc">ClassSerializerInterceptor</span><span class="p">(</span><span class="nx">app</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="nx">Reflector</span><span class="p">)));</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">class-transformer</code>의 <code class="language-plaintext highlighter-rouge">@Exclude()</code>, <code class="language-plaintext highlighter-rouge">@Expose()</code>, <code class="language-plaintext highlighter-rouge">@Transform()</code> 데코레이터를 응답 직렬화에 자동 적용.</li>
  <li><code class="language-plaintext highlighter-rouge">app.get(Reflector)</code>: DI 컨테이너 밖에서 <code class="language-plaintext highlighter-rouge">Reflector</code> 인스턴스를 직접 꺼내는 방법.</li>
</ul>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 사용 예</span>
<span class="kd">class</span> <span class="nc">UserResponseDto</span> <span class="p">{</span>
  <span class="nl">email</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>

  <span class="p">@</span><span class="nd">Exclude</span><span class="p">()</span>
  <span class="nx">password</span><span class="p">:</span> <span class="kr">string</span><span class="p">;</span>  <span class="c1">// 응답에서 자동 제거됨</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="validationpipe-transform-true-whitelist-true-"><code class="language-plaintext highlighter-rouge">ValidationPipe({ transform: true, whitelist: true })</code></h2>

<table>
  <thead>
    <tr>
      <th>옵션</th>
      <th>효과</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">transform: true</code></td>
      <td>요청 데이터를 DTO 클래스 인스턴스로 변환 (타입 캐스팅 포함)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">whitelist: true</code></td>
      <td>DTO에 선언되지 않은 필드를 자동 제거</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">forbidNonWhitelisted: true</code></td>
      <td>(미사용) 선언 외 필드가 있으면 400 에러</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">disableErrorMessages: true</code></td>
      <td>(미사용) 에러 메시지 숨기기 (프로덕션 권장)</td>
    </tr>
  </tbody>
</table>

<hr />

<h1 id="5-대체-가능한-라이브러리-및-트레이드오프">5. 대체 가능한 라이브러리 및 트레이드오프</h1>

<h2 id="winston-로거-vs-대안">Winston 로거 vs 대안</h2>

<table>
  <thead>
    <tr>
      <th>라이브러리</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**Winston** (현재)</td>
      <td>성숙한 생태계, 다양한 Transport(파일/HTTP/Loki)</td>
      <td>설정이 장황, 번들 크기 큼</td>
    </tr>
    <tr>
      <td>**Pino**</td>
      <td>성능이 가장 빠름(JSON 직렬화 최적화), <code class="language-plaintext highlighter-rouge">nest-pino</code> 공식 지원</td>
      <td>커스터마이징이 상대적으로 제한적</td>
    </tr>
    <tr>
      <td>**Bunyan**</td>
      <td>구조화 로깅 특화</td>
      <td>유지보수 저조, 생태계 축소</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Loki + Promtail 스택을 쓰는 경우 <strong>Pino</strong>가 JSON 출력이 더 깔끔하고 성능도 우수해 선호되는 추세.</p>
</blockquote>

<h2 id="throttlermodule-vs-대안">ThrottlerModule vs 대안</h2>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**@nestjs/throttler + Redis** (현재)</td>
      <td>NestJS 네이티브, 데코레이터로 라우트별 커스텀 가능</td>
      <td>Redis 의존성 추가</td>
    </tr>
    <tr>
      <td>**Nginx Rate Limit**</td>
      <td>앱 레이어 밖에서 처리, 성능 우수</td>
      <td>세밀한 비즈니스 로직 적용 어려움</td>
    </tr>
    <tr>
      <td>**express-rate-limit**</td>
      <td>설정 간단</td>
      <td>다중 인스턴스 환경에서 메모리 공유 어려움</td>
    </tr>
    <tr>
      <td>**API Gateway (Kong 등)**</td>
      <td>중앙 집중 관리</td>
      <td>인프라 복잡도 증가</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>이미 Nginx Traffic Dam이 앞단에 있으므로, NestJS 레이어의 Throttler는 <strong>내부 서비스 보호(2차 방어선)</strong> 역할임을 고려하고, 이 역할 수준을 벗어나지 않도록 맥락이 유지되어야함.</p>
</blockquote>

<h2 id="configmodule-vs-대안">ConfigModule vs 대안</h2>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**@nestjs/config** (현재)</td>
      <td>NestJS 통합, <code class="language-plaintext highlighter-rouge">ConfigService</code> DI 가능</td>
      <td>런타임 타입 검증 없음</td>
    </tr>
    <tr>
      <td>**@nestjs/config + Joi**</td>
      <td>앱 시작 시 환경변수 스키마 검증</td>
      <td>Joi 라이브러리 추가</td>
    </tr>
    <tr>
      <td>**zod + env 파싱**</td>
      <td>타입 추론이 강력, TS 친화적</td>
      <td>수동 연동 필요</td>
    </tr>
    <tr>
      <td>**dotenv 직접**</td>
      <td>의존성 최소화</td>
      <td>NestJS DI와 통합 어려움</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>[!tip] <code class="language-plaintext highlighter-rouge">configService.get&lt;string&gt;('KEY')</code>는 타입 단언이지 검증이 아니다
환경변수가 없거나 잘못된 값이 들어와도 런타임에 <code class="language-plaintext highlighter-rouge">undefined</code>가 반환될 수 있다.
<strong>Joi 또는 zod로 시작 시점에 검증</strong>하는 것이 프로덕션 수준의 접근이라고 한다. 다음 프로젝트에서는 반드시 적용하면서 진행하면 좋으리라 본다.</p>
</blockquote>

<h2 id="bodyparser-50mb-제한-vs-대안">bodyParser 50MB 제한 vs 대안</h2>

<table>
  <thead>
    <tr>
      <th>접근</th>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**express json/urlencoded** (현재)</td>
      <td>익숙한 방식, 세밀한 제어</td>
      <td>파일 업로드는 별도 Multer 필요</td>
    </tr>
    <tr>
      <td>**Multer 전용**</td>
      <td>파일 업로드에 최적화</td>
      <td>일반 JSON 바디와 혼용 복잡</td>
    </tr>
    <tr>
      <td>**파일을 Object Storage에서 직접 수신**</td>
      <td>NestJS에 파일 부하 없음</td>
      <td>아키텍처 복잡도 증가</td>
    </tr>
  </tbody>
</table>

<hr />

<h1 id="6-개발자로서-알아야-할-영역">6. 개발자로서 알아야 할 영역</h1>

<h2 id="a-nestjs-ioc-컨테이너와-di-범위">A. NestJS IoC 컨테이너와 DI 범위</h2>

<p><code class="language-plaintext highlighter-rouge">APP_GUARD</code> 패턴을 이해하려면 NestJS의 DI가 어떻게 동작하는지 알아야 한다.</p>

<ul>
  <li><strong>Singleton scope</strong> (기본): 모듈 당 인스턴스 하나. 앱 전체에서 공유.</li>
  <li><strong>Request scope</strong>: 요청마다 새 인스턴스 생성. 성능 비용 있음.</li>
  <li><strong>Transient scope</strong>: 주입받을 때마다 새 인스턴스 생성.</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">APP_GUARD</code>로 등록된 <code class="language-plaintext highlighter-rouge">JwtAuthGuard</code>는 Singleton이다. <code class="language-plaintext highlighter-rouge">Reflector</code>도 Singleton으로 공유되므로, <code class="language-plaintext highlighter-rouge">app.get(Reflector)</code> 또는 생성자 주입 중 어느 방식이든 같은 인스턴스를 받는다.</p>

<h2 id="b-글로벌-등록-두-가지-방식의-차이">B. 글로벌 등록 두 가지 방식의 차이</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 방식 1: main.ts에서 useGlobal* (DI 컨테이너 외부)</span>
<span class="nx">app</span><span class="p">.</span><span class="nf">useGlobalGuards</span><span class="p">(</span><span class="k">new</span> <span class="nc">OriginGuard</span><span class="p">());</span>

<span class="c1">// 방식 2: AppModule에서 APP_* 토큰 (DI 컨테이너 내부)</span>
<span class="p">{</span> <span class="na">provide</span><span class="p">:</span> <span class="nx">APP_GUARD</span><span class="p">,</span> <span class="na">useClass</span><span class="p">:</span> <span class="nx">JwtAuthGuard</span> <span class="p">}</span>
</code></pre></div></div>

<p><strong>결정 기준</strong>: 해당 클래스가 다른 Provider를 주입받을 필요가 있는가?</p>
<ul>
  <li>주입이 필요 없다 → 방식 1 (단순)</li>
  <li>주입이 필요하다 → 반드시 방식 2</li>
</ul>

<h2 id="c-forroot-vs-forrootasync-패턴">C. <code class="language-plaintext highlighter-rouge">forRoot</code> vs <code class="language-plaintext highlighter-rouge">forRootAsync</code> 패턴</h2>

<p>NestJS 동적 모듈의 표준 패턴. 라이브러리를 직접 만들거나 커스텀 모듈을 설계할 때 필수로 이해해야 한다.</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 동기: 정적 값</span>
<span class="nx">ThrottlerModule</span><span class="p">.</span><span class="nf">forRoot</span><span class="p">({</span> <span class="na">ttl</span><span class="p">:</span> <span class="mi">60</span><span class="p">,</span> <span class="na">limit</span><span class="p">:</span> <span class="mi">100</span> <span class="p">})</span>

<span class="c1">// 비동기: 다른 Provider에서 값을 받아야 할 때</span>
<span class="nx">ThrottlerModule</span><span class="p">.</span><span class="nf">forRootAsync</span><span class="p">({</span>
  <span class="na">imports</span><span class="p">:</span> <span class="p">[</span><span class="nx">RedisModule</span><span class="p">],</span>    <span class="c1">// 필요한 모듈 등록</span>
  <span class="na">inject</span><span class="p">:</span> <span class="p">[</span><span class="nx">REDIS_CLIENT</span><span class="p">],</span>    <span class="c1">// 팩토리 인자로 주입할 토큰</span>
  <span class="na">useFactory</span><span class="p">:</span> <span class="p">(</span><span class="nx">redis</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">({</span> <span class="p">...</span> <span class="p">})</span>  <span class="c1">// 실제 설정 반환</span>
<span class="p">})</span>
</code></pre></div></div>

<h2 id="d-graceful-shutdown과-컨테이너-오케스트레이션">D. Graceful Shutdown과 컨테이너 오케스트레이션</h2>

<p>Docker/K8s 환경에서 배포 시 <code class="language-plaintext highlighter-rouge">SIGTERM</code> 신호가 날아온다. <code class="language-plaintext highlighter-rouge">enableShutdownHooks()</code>를 활성화하지 않으면:</p>
<ul>
  <li>진행 중인 HTTP 요청이 강제 종료됨</li>
  <li>Redis 구독 연결이 비정상 종료됨</li>
  <li>DB 커넥션 풀이 정리되지 않음</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">OnModuleDestroy</code>를 구현해 각 서비스가 자신의 리소스를 정리하는 패턴은 <strong>마이크로서비스 안정성의 기본</strong>이다.</p>

<h2 id="e-rate-limiting-계층-설계">E. Rate Limiting 계층 설계</h2>

<p>Rate Limit은 하나의 계층에서만 하는 것이 아니라 <strong>계층별로 역할이 다르다</strong>:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[클라이언트]
    │
[Nginx] ── Traffic Dam (토큰 버킷, Heavy/Light Zone 구분)
    │
[NestJS ThrottlerGuard] ── 서비스 레벨 보호 (burst/sustained 2단계)
    │
[내부 QueueService] ── 개별 작업 동시성 제어
    │
[FastAPI 워커] ── AI 연산 보호
</code></pre></div></div>

<p>각 계층이 왜 필요한지, 어느 계층에서 무엇을 막는지 설명할 수 있어야 한다.</p>

<h2 id="f-환경변수-관리의-프로덕션-수준">F. 환경변수 관리의 프로덕션 수준</h2>

<p>현재 코드의 <code class="language-plaintext highlighter-rouge">configService.get&lt;string&gt;('JWT_SECRET')</code>는 <strong>타입 단언</strong>이다. 이부분은 AI 의 지적이 뼈아픈데, 프로덕션에서는:</p>

<ol>
  <li><strong>시작 시 검증</strong>: Joi 스키마로 필수 환경변수가 빠지면 앱이 시작조차 안 되게</li>
  <li><strong>시크릿 관리</strong>: .env 파일 대신 AWS Secrets Manager, HashiCorp Vault, K8s Secret 사용</li>
  <li><strong>런타임 재로드</strong>: 일부 설정은 재배포 없이 변경 가능하도록 설계</li>
</ol>

<p>라는 조건이 필요함을 강조했다. 알긴 하는데 바쁘다고 안했지만 뼈아프긴 하다(…)</p>

<h2 id="g-구조화-로깅과-옵저버빌리티">G. 구조화 로깅과 옵저버빌리티</h2>

<p><code class="language-plaintext highlighter-rouge">main.ts</code>에서 Winston을 JSON 포맷으로 설정한 이유는 <strong>Promtail이 파싱하기 위해서</strong>다. 로그를 단순 텍스트로 출력하면 Loki에서 필터링이 어렵다.</p>

<p>개발자가 알아야 할 로깅 설계 원칙:</p>
<ul>
  <li>구조화된 JSON 로그 출력 (key-value로 파싱 가능하게)</li>
  <li>로그 레벨 분리 (<code class="language-plaintext highlighter-rouge">error</code> / <code class="language-plaintext highlighter-rouge">warn</code> / <code class="language-plaintext highlighter-rouge">info</code> / <code class="language-plaintext highlighter-rouge">debug</code>)</li>
  <li>요청 추적을 위한 Trace ID / Correlation ID 포함</li>
  <li>민감 정보(비밀번호, 토큰) 로그 미노출</li>
</ul>

<h2 id="h-cors와-보안-헤더">H. CORS와 보안 헤더</h2>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">app</span><span class="p">.</span><span class="nf">enableCors</span><span class="p">({</span>
  <span class="na">origin</span><span class="p">:</span> <span class="p">(</span><span class="nx">requestOrigin</span><span class="p">,</span> <span class="nx">callback</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span> <span class="p">...</span> <span class="p">},</span>
  <span class="na">methods</span><span class="p">:</span> <span class="dl">'</span><span class="s1">GET, HEAD, PUT, PATCH, POST, DELETE, OPTIONS</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">credentials</span><span class="p">:</span> <span class="kc">true</span><span class="p">,</span>  <span class="c1">// 쿠키/인증 헤더 허용</span>
<span class="p">});</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">credentials: true</code>를 쓸 때는 <code class="language-plaintext highlighter-rouge">origin: '*'</code> 가 불가능하다. 명시적 화이트리스트가 필요하다는 점, 그리고 <code class="language-plaintext highlighter-rouge">OPTIONS</code> 프리플라이트 요청까지 포함해야 한다는 점을 이해해야 한다.</p>

<p>추가로 알아야 할 보안 헤더: <code class="language-plaintext highlighter-rouge">helmet</code> 라이브러리로 <code class="language-plaintext highlighter-rouge">X-Content-Type-Options</code>, <code class="language-plaintext highlighter-rouge">X-Frame-Options</code>, <code class="language-plaintext highlighter-rouge">Strict-Transport-Security</code> 등 설정 가능. 현재 코드에는 미적용되어 있다.</p>

<hr />

<h1 id="핵심-요약-카드">핵심 요약 카드</h1>

<blockquote>
  <p>[!abstract] main.ts의 역할</p>
  <ul>
    <li><strong>어떻게 실행할지</strong> 결정하는 부트스트랩 파일</li>
    <li>글로벌 파이프라인 구성 (필터, 인터셉터, 가드, 파이프)</li>
    <li>런타임 설정 (CORS, bodyParser, 로거, 포트)</li>
    <li><code class="language-plaintext highlighter-rouge">useGlobal*</code> 순서 ≠ 실행 순서 (Lifecycle이 고정)</li>
  </ul>
</blockquote>

<blockquote>
  <p>[!abstract] app.module.ts의 역할</p>
  <ul>
    <li><strong>무엇을 연결할지</strong> 선언하는 조립 파일</li>
    <li>모든 도메인 모듈의 집합점</li>
    <li>DI 컨테이너를 통한 전역 가드 등록 (<code class="language-plaintext highlighter-rouge">APP_GUARD</code>)</li>
    <li><code class="language-plaintext highlighter-rouge">forRootAsync</code> 패턴으로 동적 모듈 구성</li>
  </ul>
</blockquote>

<blockquote>
  <p>[!question] 면접/리뷰에서 설명할 수 있어야 하는 것</p>
  <ol>
    <li><code class="language-plaintext highlighter-rouge">useGlobalGuards</code> vs <code class="language-plaintext highlighter-rouge">APP_GUARD</code> 토큰 방식의 차이와 선택 기준</li>
    <li><code class="language-plaintext highlighter-rouge">ThrottlerModule</code>이 Redis를 쓰는 이유 (단일 인스턴스 vs 다중 인스턴스)</li>
    <li><code class="language-plaintext highlighter-rouge">enableShutdownHooks()</code>를 켜야 하는 이유와 연결되는 인터페이스</li>
    <li><code class="language-plaintext highlighter-rouge">ValidationPipe whitelist: true</code>가 보안에 미치는 영향</li>
    <li><code class="language-plaintext highlighter-rouge">ClassSerializerInterceptor</code>와 <code class="language-plaintext highlighter-rouge">@Exclude()</code>의 관계</li>
  </ol>
</blockquote>]]></content><author><name>Paul2021-R</name></author><category term="문제해결" /><category term="Backend" /><category term="개발" /><category term="Protostar" /><category term="Project" /><category term="Review" /><category term="NestJS" /><category term="AI" /><summary type="html"><![CDATA[NestJS main.ts / app.module.ts 분석]]></summary></entry><entry><title type="html">Protostar review note - 05 FastAPI RAG</title><link href="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/17/00-protostar-review.html" rel="alternate" type="text/html" title="Protostar review note - 05 FastAPI RAG" /><published>2026-02-17T00:00:00+00:00</published><updated>2026-02-17T00:00:00+00:00</updated><id>http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/17/00-protostar-review</id><content type="html" xml:base="http://0.0.0.0:4000/%EB%AC%B8%EC%A0%9C%ED%95%B4%EA%B2%B0/2026/02/17/00-protostar-review.html"><![CDATA[<h2 id="corerag_servicepy">core/rag_service.py</h2>

<h3 id="1-주요-개념">1. 주요 개념</h3>

<ul>
  <li><strong>RAG (Retrieval-Augmented Generation)</strong>: 외부 지식 베이스(데이터베이스)에서 질문과 관련된 정보를 검색(Retrieval)하고, 그 정보를 대규모 언어 모델(LLM)에 전달하여 답변을 생성(Generation)하게 함으로써 모델의 내부 지식 한계를 극복하는 기법이다.</li>
  <li><strong>Vector Embedding (벡터 임베딩)</strong>: 텍스트 데이터를 고차원의 수치 벡터로 변환하는 과정이며, 이를 통해 단어나 문장 간의 의미적 유사도를 계산할 수 있게 한다.</li>
  <li><strong>Cosine Similarity (코사인 유사도)</strong>: 두 벡터 사이의 각도를 측정하여 유사성을 판단하는 방식이며, 본 코드에서는 <code class="language-plaintext highlighter-rouge">cosine_distance</code> 연산자를 활용해 질문과 가장 가까운 문서를 추출한다.</li>
</ul>

<h3 id="2-핵심-로직-흐름">2. 핵심 로직 흐름</h3>

<ul>
  <li><strong>질문 벡터화</strong>: 사용자의 쿼리를 <code class="language-plaintext highlighter-rouge">get_embeddings</code> 함수를 통해 수치 벡터로 변환하며, 실패 시 경고 로그를 남기고 빈 리스트를 반환한다.</li>
  <li><strong>유사 문서 검색</strong>: SQLAlchemy를 사용하여 <code class="language-plaintext highlighter-rouge">VectorizedDoc</code> 테이블에서 질문 벡터와 코사인 거리가 가장 짧은 상위 <code class="language-plaintext highlighter-rouge">top_k</code>개의 데이터를 조회한다.
    <ul>
      <li>default top_k = 3: 검색 문서가 너무 과해지면 프롬프트가 길어지며, 모델 응답 속도가 느려짐. 이에 주요 정보들까지 검색을 위하여 top_k 의 값을 표준적인 사이즈로 설정함.
        <ul>
          <li>1 ~ 2 : 응답속도 빨라지며, 비용 저렴해지나 상대적으로 복합 질문 답변 어려움</li>
          <li>중간 값</li>
          <li>5 ~ 10 : 폭 넓은 맥락 제공 가능, 단, 관련 없는 정보가 다소 포함되여 엉뚱한 답으로 변화될 가능성 있슴</li>
          <li>현재는 고정값으로 설정되어 구현하였으나, 필요시 다양한 방식으로 동적 혹은 상대적 정밀도로 구성 변경도 가능함.</li>
        </ul>
      </li>
    </ul>
  </li>
  <li><strong>컨텍스트 구조화</strong>: 검색된 문서 객체들에서 키워드, 요약문, 본문을 추출하여 LLM이 이해하기 쉬운 <code class="language-plaintext highlighter-rouge">[Document #n]</code> 형태의 텍스트 블록으로 포맷팅한다.</li>
</ul>

<h3 id="3-구조적-취약점-및-개선-방향">3. 구조적 취약점 및 개선 방향</h3>

<ul>
  <li><strong>취약점</strong>: 대규모 데이터 처리 시 검색 성능 저하 우려가 있다. 현재의 DB 기반 정렬 방식은 인덱스가 적절히 구성되지 않을 경우 검색 속도가 선형적으로 느려질 수 있다.</li>
  <li><strong>개선 방향</strong>: 전문 벡터 데이터베이스(ChromaDB, Pinecone 등)를 도입하거나, PostgreSQL의 <code class="language-plaintext highlighter-rouge">pgvector</code>를 쓰고 있으니 HNSW 인덱스를 적용하여 검색 속도를 최적화를 도입해봐도 된다.</li>
</ul>

<h3 id="4-핵심-메서드-및-라이브러리-함수">4. 핵심 메서드 및 라이브러리 함수</h3>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">search_similar_docs(query, top_k)</code></strong>: 입력된 문자열 질문을 벡터로 변환하고 DB에서 가장 유사한 문서를 찾는 핵심 비동기 함수이다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">format_rag_context(docs)</code></strong>: 검색 결과 리스트를 LLM 프롬프트에 주입하기 적합한 단일 문자열로 변환하는 유틸리티 함수이다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">VectorizedDoc.embedding.cosine_distance()</code></strong>: 데이터베이스 수준에서 벡터 간 유사도를 계산하는 SQLAlchemy 익스텐션 메서드이다.</li>
</ul>

<h3 id="5-대체-가능한-라이브러리-및-메서드트레이드-오프">5. 대체 가능한 라이브러리 및 메서드(트레이드 오프)</h3>

<ul>
  <li><strong>SQLAlchemy vs LangChain (VectorStore)</strong></li>
  <li><strong>차이</strong>: 현재는 SQLAlchemy로 직접 쿼리를 작성하지만, LangChain은 다양한 벡터 DB를 동일한 인터페이스로 다루는 추상화 레이어를 제공한다.</li>
  <li>
    <p><strong>트레이드 오프</strong>: 직접 구현 시 의존성이 적고 최적화가 자유롭지만, 기능 확장 시(필터링, 하이브리드 검색 등) 모든 로직을 수동으로 개발해야 하는 공수가 발생한다. 보다 다양한 포멧의 다양한 벡터화된 문서들이 도입시 이에 따라서 보다 복잡하고 세밀한 접근이 필요해진다.</p>
  </li>
  <li><strong>Cosine Distance vs L2 Distance (유클리드 거리)</strong></li>
  <li><strong>차이</strong>: 코사인 거리는 벡터의 방향(의미)에 집중하고, L2 거리는 벡터의 절대적인 위치(크기 포함)에 집중한다.</li>
  <li>
    <p><strong>트레이드 오프</strong>: 텍스트 분석에서는 문장 길이에 덜 민감한 코사인 유사도가 일반적으로 유리하지만, 특정 임베딩 모델에 따라 L2 거리가 더 높은 정확도를 보이기도 한다.</p>
  </li>
  <li><strong>pgvector vs Specialized Vector DB (Milvus, Qdrant)</strong></li>
  <li><strong>차이</strong>: pgvector는 기존 RDBMS 내부에서 관리하지만, 전용 DB는 벡터 연산에 최적화된 엔진을 별도로 가진다.</li>
  <li><strong>트레이드 오프</strong>: pgvector는 관리가 편하고 데이터 정관계를 유지하기 좋으나, 초고대규모 데이터셋에서는 전용 DB의 인덱싱 성능과 분산 처리 능력이 앞선다. 결과적으로 고가용성 서버를 위한 별도 데이터DB의 구축은 AI 서비스 트래픽을 위해 고려 대상이 될 수 있다.</li>
</ul>]]></content><author><name>Paul2021-R</name></author><category term="문제해결" /><category term="Backend" /><category term="개발" /><category term="Protostar" /><category term="Project" /><category term="Review" /><category term="FastAPI" /><category term="AI" /><summary type="html"><![CDATA[core/rag_service.py]]></summary></entry></feed>